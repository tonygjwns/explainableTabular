# 주입-패밀리 스윕 [D] — 저장소 검증 · 실행 계획 · 사전 예측

작성 2026-07-31 · 브랜치 `strict-shadow-fix` · HEAD `606eedb`
대상 문서: `PAPER_DRAFT_V4.md` / `_KO.md`, `PREREG_DEPLOYMENT_V2.md` §14
성격: **조사·검증·계획만.** 실험 미실행. PREREG·원고·임계값·캐스케이드·시드 프로토콜 무수정.

> 이 문서는 두 건의 독립 외부 적대 검토(원고만 열람, 저장소 미열람)가 제기한 6개 주장을
> 저장소 아티팩트에 대조 검증하고, `--inj-family` 스윕의 실행 계획과 실행 전 예측을 고정한다.
> 아티팩트가 검토를 반증하는 항목은 반증으로 기록한다.

---

## A. 저장소 상태

### A.1 스윕 구현 상태

| 항목 | 결과 |
|---|---|
| `--inj-family` 정의 | `scripts/run_deployment_decay.py:1083-1087` |
| 지원 패밀리 | `topvar`, `lowvar`, `interaction`, `subpop` (choices, :1084) |
| 전달 경로 | `main` :1174 → `assess(inj_family=)` :581 → `_injection_recovers(family=)` :746 → `_inject_concept(family=)` :402 |
| 기하 구현 | `:323-361` |
| 강도 인자 | `_inject_concept(strength=2.5)` :323 — 호출부는 모듈 상수 `INJ_STRENGTH = 2.5` (:80)을 넘김 (:402). **CLI 플래그 없음** |
| 스모크 산출물 | **없음.** 커밋 메시지 `ffe1e56`와 `REVIEW_ROUND6_2026-07-18.md:13`의 산문 기록("4패밀리 전부 학습가능 AUC 0.95±, subpop 라벨 20% 분기, topvar 회복 재현")뿐. JSON·로그·테스트 스크립트 어느 것도 없음 |
| 학습가능성 게이트 | **전역 상수** `LEARN_AUC=0.65 / LEARN_R2=0.20 / LEARN_ACC=0.10` (:91-93). `_injection_learnable` (:364-391)이 패밀리 무관하게 동일 임계 적용 |

**각 패밀리가 실제로 구현하는 기하** (주석이 아니라 코드 기준, `:338-361`)

공통:
```
Xf    = nan_to_num(X)
tn    = (t - min t) / (max t - min t)          # [0,1]
order = argsort(-Xf.std(0))                    # 분산 내림차순
a, b  = z(Xf[:,f0]), z(Xf[:,f1])
ang   = 2.5 * tn
score = cos(ang)*a + sin(ang)*b + N(0, 0.3)
```
라벨: binclass = `score > median(score)` (전역 중앙값 → 클래스 균형 50/50 고정),
multiclass = 3-way argmax, regression = score 자체.

| 패밀리 | f0, f1 선택 | 회전 축 변형 |
|---|---|---|
| `topvar` | `order[0], order[1]` (분산 상위 2) | 없음 (기준) |
| `lowvar` | `nz = [j for j in order[::-1] if std>1e-9]` 의 첫 2개 | 없음. **선택 열만 다름** |
| `interaction` | **topvar와 동일** | `a := z(a*b)` 로 교체 (:349-350) |
| `subpop` | **topvar와 동일** | `f2 = order[2]`, `ang *= (z(Xf[:,f2])>0)` (:352-354). 부분모집단 밖은 `ang=0` → `score = a + noise` (정적 규칙) |

> ⚠ `interaction`의 두 회전 축은 `z(z(f0)·z(f1))` 와 `z(f1)` — 두 번째 축이 첫 번째 축의
> **구성 인자**다. 독립적 main effect가 아니다. (E.2/D절 판별 조건에서 재언급)

### A.2 PREREG §14 원문

`PREREG_DEPLOYMENT_V2.md:270-310` 전문:

> ## 14. 리버탈 실험 [C]·[D] 사전 규정 (2026-07-18, 서버 실행 *전* 커밋 — 리뷰어2 대응 옵션 B)
>
> > 규칙: 아래 판독 규정은 결과를 보기 전에 커밋된다. 캐스케이드·임계값·floor·게이트·envelope는
> > §3~§8과 동일하게 **불변**. 탐색 시드 0–9 → 판정이 기존 지도와 달라지는 셀은 확증 시드
> > 100–109 재실행을 통과해야만 논문에 들어간다(기존 §7 규율 그대로). 코드 = 커밋 ffe1e56의
> > `--tabred-span full` / `--inj-family` (로컬 스모크 통과, 사전등록 배터리 경로 불변).
>
> ### [C] 배포 held-out 구간 감사 (`--tabred-span full`, 8개 TabReD, 10시드)
>
> - **무엇**: train+val+test를 공유 정규화 timestamp로 연결·정렬 — 윈도우가 공식 분할이 hold-out
>   하는 배포 갭을 가로지른다. 행 태그 `_fullspan`.
> - **예측 (커밋 시점)**: 지도 판정 유지 (full-span에서도 산업 mean-rule drift 발화 없음).
> - **판독 규정 (양방향, 결과 확인 전 커밋)**:
>   - **판정 전 셀 유지** → 부록 B.3 "지도는 배포 갭을 포함해도 불변" — 리뷰어2 사유 1의 잔여
>     절반(§7 한계 2의 train-구간 한정) 실측 방어로 격상.
>   - **어떤 산업 셀이든 DEPLOYMENT-CONCEPT 발화** (denoised CI>0 ∧ mean>floor, envelope 내)
>     → ① 확증 100–109 재실행 통과 시에만 인정, ② 인정되면 **0/8 헤드라인은 "train 구간"으로
>     재스코프**되고 해당 셀은 신규 발견으로 보고된다 — 프레이밍: "drift는 정확히 벤치마크가
>     딥 방법을 평가하는 구간에 산다" (이는 실패가 아니라 이 계기가 처음 잡는 산업 양성).
>     초록·§5.2·Figure 2 갱신 필수.
>   - **NOISE-DRIFT-CONFOUNDED / RAW-ONLY 발화** → 기제 라벨대로 보고 (concept 아님, §3.3).
>   - **rule-sensitive / unstable** → 기존 규칙대로 헤드라인 금지.
>   - full-span은 지도를 **대체하지 않는다**: 본 지도 = train 구간(공식 분할의 감사 가능 구간);
>     full-span = 강건성 부록. 재스코프 트리거는 위 CONCEPT 케이스뿐.
>
> ### [D] 주입-패밀리 sweep (`--inj-family {lowvar, interaction, subpop}`, 인증서 셀 6 + insects, 10시드)
>
> - **무엇**: 기준 패밀리(topvar 회전)를 저분산 2피처 / 상호작용항 / 부분모집단(z(f2)>0)-국소
>   회전으로 교체. 학습가능성 게이트 불변 — 학습불능 패밀리는 *그 패밀리에 대해* VACUOUS.
> - **예측 (커밋 시점)**: cooking/delivery 회복이 학습가능한 패밀리들에서 유지; insects(양성
>   대조) 전 학습가능 패밀리에서 회복; vacuous 3셀(ecom/homecredit/weather)은 패밀리를 바꿔도
>   대부분 학습불능 또는 미회복.
> - **판독 규정 (양방향)**:
>   - **회복이 학습가능 패밀리 전반에서 유지** → §7 한계 (6)이 "실측 방어"로 격상, 부록 B.4에
>     패밀리×셀 표. "verified no-concept" 명명 유지(family-상대성 각주 유지).
>   - **기존 verified 셀(cooking/delivery)이 어떤 학습가능 패밀리에서 미회복** → 그 셀 인증서를
>     §5.2에서 **family-상대적으로 재표기**하고, "인증서는 패밀리-상대적"이 한계가 아니라 본문
>     발견으로 승격(§3.2 재작성). 확증 재실행 규칙 동일 적용.
>   - **insects가 어떤 학습가능 패밀리에서 미회복** → 그 패밀리에 대한 계기 감도 한계로 §6에
>     보고 (은폐 금지).
>   - 학습불능 패밀리는 어떤 셀에서도 인증·반증 어느 쪽으로도 쓰지 않는다 (vacuous 규율).

**[C] 판독 한 줄** — full-span에서 판정이 유지되면 부록 B.3 강건성; 산업 셀이 DEPLOYMENT-CONCEPT를
발화하면 확증 재실행 후 0/8을 "train 구간"으로 재스코프.

**[D] 판독 한 줄** — 학습가능 패밀리 전반에서 cooking/delivery 회복이 유지되면 한계(6) 해소;
어떤 학습가능 패밀리에서든 미회복이면 인증서를 family-상대로 재표기.

**집계 규칙 — 규정되어 있지 않음.** §14는 셀별 양방향 판독만 규정하고, 패밀리 3개의 결과를
하나의 셀 라벨로 합치는 규칙을 명시하지 않는다.

#### §14의 불완전·모호 지점 (지적만 — 수정하지 않음)

1. **"학습가능 패밀리 전반에서 유지"의 "전반"이 미정의.** 전부인지 과반인지 규정 없음.
2. **다중비교 회계 없음.** 패밀리 3개를 더하면 인증서 상실(불리)·획득(유리) 양방향 모두
   위양성 확률이 max-over-K만큼 오른다. 규칙 A(one-sided 실효 2.5%) 기준 K=3 → ≈7.3%.
3. **★ 최대 공백: "공허 셀이 새 패밀리에서 회복되는 경우"의 판독 규정이 없다.**
   §14는 그 경우를 *예측*에서만 "대부분 학습불능 또는 미회복"이라 적고, 판독 규정 4개 중
   대응 항목이 없다. 외부 검토가 지목한 "분모 복구" 분기가 정확히 이것이고 사전등록되어 있지 않다.
4. **강도(rad) 차원이 §14에 전혀 없다.** "최소 2단계 강도"는 사전등록 밖의 신규 차원이다.
5. **셀 목록 불일치.** §14 / `audit_repair_2026-07-18/SERVER_RUNBOOK.md:48`의 "인증서 셀 6" =
   cooking / delivery / elec2 / ecom / homecredit / weather + insects.
   `homesite_insurance`는 **포함되어 있지 않다.**

### A.3 기존 아티팩트 인벤토리

| 질문 | 답 |
|---|---|
| 시드별 원값이 남은 셀 | **지도(`prereg_results/phase234/`, git `5f3217d`)에는 전무.** `per_seed` 필드는 커밋 `384f272` 이후 산출물에만 존재 |
| 시드별 원값 보유 파일 | `prereg_results/optional/*` (MLP 8셀·elec2·insects·ember2018·ACS), `summary_20260718T{040332,052219,055856}_f6e65b6.json` (B.1 N-스윕 3건), `deployment_decay_synth_summary*.json` (배터리 14셀) |
| 윈도우별 원값 | **없음.** `_per_seed`가 윈도우 루프에서 `stales/dens/recs/decays`를 모으고 `mean()`만 반환 (`:549-551`). 집계 전 윈도우 값은 어떤 아티팩트에도 없다 |
| 셀별 분리도 D | **있음.** 모든 행에 `D_strip` / `D_full` / `D_shuffle` / `D_spread` (`:780`) |
| arm별 절대 성능 | **없음.** `sRec`·`sOld`·`sRecOld`·`sRecOldD`는 `_per_seed` 지역변수로만 존재, 차이값만 저장 (`:537-548`) |
| 주입 arm의 per-seed | **없음.** `_injection_recovers`(`:404-409`)가 `st` 리스트를 모으고 mean/CI만 반환 |
| `--tabred-span full` | 구현 완료 (`:834-843`). 로더가 train/val/test 전부에 공유 `t_min/t_max` 정규화 시각 부여(`src/data/tabred_loader.py:173`) → 연결 성립. **실행 이력 0건** (`_fullspan` 행 없음). 예상 3.5–5 h(비경합, 행 수 train 대비 ~1.4–1.6배) |

### A.4 재현 환경

- **freeze 파일**: `env_explaintab311_freeze.txt` (97줄, `pip freeze`). `prereg_results/phase234_raw/`
  사본과 **바이트 동일**. 단 Python/sklearn/numpy 버전 자체는 freeze에 없고 각 아티팩트
  `meta`에만 있다 (3.11.15 / 1.9.0 / 2.4.6).
- **marker 재개**: `scripts/run_prereg_phases.sh:22-27`의 `step()`이
  `results/phase1/deployment_decay/markers/*.done` 기반으로 정상 동작.
  **그러나 이 드라이버에 `--inj-family` / `--tabred-span` 스텝이 전혀 없다** — 스윕은 재개 가능
  드라이버 밖이고, SERVER_RUNBOOK은 marker 없는 맨 `nohup` 루프를 쓴다.
- **덮어쓰기 위험 경로 3곳**
  1. `results/phase1/deployment_decay/summary_latest.json` — 매 실행 무조건 덮어씀 (`:1189`)
  2. `results/phase1/deployment_decay/synth_summary.json` — 고정 이름 (`:1115`). 배터리 재실행 시
     기존 PASS 산출물 소실 (런북 §1의 `cp`가 대응)
  3. `summary_<UTC초>_<git>.json` — 스탬프가 **초 단위**. 패밀리 3개 동시 launch 시 같은 초·같은
     커밋에서 충돌 가능. 병렬 시 출력 분리 또는 stagger 필요
- **행 이름에 패밀리가 안 붙는다.** `_fullspan`과 달리 `--inj-family`는 `dataset` 이름을 바꾸지
  않고 `injection_family` 필드에만 기록한다 (`:786`). 집계 키는 `meta.argv` 또는 그 필드.

---

## B. 주장 검증

### 주장 1 — 헤드라인 분모 팽창 → **확인됨** (분류), 프레이밍은 해석 문제

아티팩트 `prereg_results/phase234/summary_20260704T212616_5f3217d.json` (확증 시드 100–109, HGB):

| 셀 | 판정 | 인증서 상태 | 정보량 |
|---|---|---|---|
| ecom_offers | UNIDENT-EXPLOITABLE | `injection_learnable=false` (lscore 0.527) | ✗ 공허 |
| homecredit_default | UNIDENT-EXPLOITABLE | `false` (0.637) | ✗ 공허 |
| weather | UNIDENT-EXPLOITABLE | `false` (−0.022) | ✗ 공허 |
| homesite_insurance | UNIDENT-INERT | `true`(0.694) ↔ 탐색 `false`(0.516) | ✗ 불안정 |
| sberbank_housing | NOISE-DRIFT-CONFOUNDED | 기제 진단 | ✓ |
| cooking_time | INJECTION-RECOVERED | inj +0.555 | ✓ (단일 패밀리) |
| delivery_eta | INJECTION-RECOVERED | inj +0.318 | ✓ (단일 패밀리) |
| maps_routing | NO-STRONG-CONCEPT | D 0.578 식별가능 | ✓ |

외부 검토의 분류는 아티팩트와 **완전히 일치**. 정보량 있는 셀 4개, 그중 2개가 단일 패밀리 인증서.

> **0/8 → 0/4 재계산에는 스윕이 필요 없다. 오늘 기존 아티팩트만으로 가능하다.**

단, "분모에 들어가서는 안 된다"는 규범적 주장이다. 현 헤드라인 문언(`PAPER_DRAFT_V4.md:542`)은
"감사한 8개 중 검출 0"이라는 사실 진술이고 그 자체로 거짓은 아니다. 쟁점은 정직성이지 정확성이 아니다.

### 주장 2 — δ 자기모순 → **문자 그대로 반증됨**, 실질은 확인됨

공허 3셀의 아티팩트에 `delta_staleness`가 **실제로 기록되어 있다**:

| 셀 | δ (탐색) | δ (확증) |
|---|---|---|
| ecom_offers | 0.0096 | 0.0040 |
| homecredit_default | 0.0038 | 0.0023 |
| weather | 0.0006 | 0.0008 |

계산 근거 `_delta` (`:568-577`): `(z₀.₉₅+z₀.₈)·SE(stale_s) ≈ 2.49·SE` — **실 staleness의 시드 간
표준오차**에서 나오며 주입과 무관하다.

→ "인증서가 거부된 셀은 δ를 산출할 수 없다"는 **틀렸다**. 그러나 실질은 유효: 이 δ는
*통계적 정밀도* 양이지 *검출 가능성* 양이 아니다. weather의 δ=0.0006은 "0.0006 넘는 staleness는
배제된다"로 읽히지만, 같은 셀에서 2.5 rad 심어진 규칙이 **학습조차 되지 않았다**(R²=−0.022).
정밀도와 "그 기하가 규칙 변화를 나를 수 있음"은 다른 명제이고, 초록·§4.1의
"모든 null은 자신의 δ를 지니고 다닌다"는 그 둘을 구분하지 않는다.

**결론: δ는 존재한다. 공허 셀에서 δ를 검출가능성 진술로 읽는 것이 근거 없다.**

### 주장 3 — homecredit 무증거 대역 초과 적용 → **확인됨**, 다만 경로가 다름

#### (a) B.1 수치 대조 — 일치

`summary_20260718T{040332,052219,055856}_f6e65b6.json` + 지도 실행:

| N | denoised | CI |
|---|---|---|
| 1,500 | +0.00650 | [+0.00464, +0.00837] |
| 6,000 (지도 탐색) | +0.00399 | [+0.00180, +0.00619] |
| ≈17,200 (cap 24k) | +0.00573 | [+0.00434, +0.00712] |
| ≈17,200 (cap 96k) | +0.00484 | [+0.00343, +0.00626] |
| 6,000 (지도 **확증**) | +0.00516 | [+0.00353, +0.00686] |

전 범위 CI가 0 위. floor 0.02 대비 1/5 ~ 1/3. **초고 부록 B.1과 일치.**

#### (b) 무증거 대역 캘리브레이션에 쓰인 no-drift 앵커 5개 전체

`prereg_results/phase234/summary_20260704T172128_5f3217d.json`:

| 앵커 | den | CI | 판정 |
|---|---|---|---|
| river_sea_nodrift | **+0.00100** | [+0.00020, +0.00180] | SUBFLOOR |
| river_sine_nodrift | **+0.00097** | [+0.00038, +0.00155] | SUBFLOOR |
| river_agrawal_nodrift | +0.00000 | [−0.0000, +0.0000] | NO-STRONG |
| river_stagger_nodrift | +0.00000 | [−0.0000, +0.0000] | NO-STRONG |
| river_hyperplane_static | −0.00603 | [−0.00903, −0.00304] | NO-STRONG |

양수 착지 2건이 **둘 다 ≈0.001 = floor의 1/20**. PREREG §9(`:174`)의 "+0.001~+0.002 (1/10~1/20)"는
상한이 관측되지 않은 서술이고, 실제 캘리브레이션 범위는 [0.00097, 0.00100]으로 더 좁다.
homecredit의 +0.0048~+0.0065는 그 대역의 **4.8×–6.7× 밖**. 외부 검토의 "2~5배"는 과소평가.

#### (c) 패널 전체에서 denoised가 모든 N에서 CI-양수인 셀

HGB 지도 10셀 중 **homecredit 하나뿐** (insects는 CONCEPT 발화이므로 별개). 나머지 전부 음수 또는
0 포함. **확인됨.**
부기: RF 프로브에서는 homesite +0.0055, homecredit +0.0145가 둘 다 CI-양수 — RF는 결정등급
클래스이므로 별도 검토 필요 (E.2-4).

#### (d) 다른 판정을 지지하는 근거 — 있으나 판정 경로가 다름

**외부 검토의 논리를 정정해야 한다.** 캐스케이드(`:708-727`) 순서상 homecredit은 `den_sub`
(=SUBFLOOR) 분기에 **도달하지 않는다**: `disjoint`(D_strip 0.9998 ≥ D\*=0.96)가 먼저 걸려
UNIDENTIFIABLE-EXPLOITABLE로 라우팅된다. 이 셀이 양성으로 카운트되지 않는 실제 이유는
"무증거 대역"이 아니라 **D-게이트 + floor**다. 무증거 대역은 초고 부록 B.1 산문
("sits at a quarter of the decision floor")에서만 원용된다.

그래도 결론은 유지된다. 이 셀은 (i) 유일한 지속적 양수 방향 신호이고, (ii) 인증서가 공허해
"안 보이는 것"인지 "없는 것"인지 구분 불가이며, (iii) 그것을 밀어내는 산문 근거가 캘리브레이션
범위 5–7배 밖의 대역 원용이다.

> 스윕이 homecredit을 **인증(학습가능+미회복)**하면 (ii)만 해소되고 +0.005는 여전히 UNIDENT.
> 스윕이 **회복**시키면 비로소 "실 null이 정보량 있음"이 되고 +0.005가 진짜로 무증거가 된다.

### 주장 4 — 인증서 회계 불일치 → **확인됨**

| 위치 | 문언 | 함의 |
|---|---|---|
| 초록 `V4:53-58` | "0/8 … every remaining cell is either an identifiable null or carries a certificate verdict — verified, blind, refused, or (one cell) unstable" | 10셀 대상, 개수 미명시 |
| §1 기여 `V4:158` / KO `:114` | "…실명 인증서를 얻으며, 셋은 인증에 실패한다 … 남은 두 셀은 식별가능 null과 시드-불안정 인증서" | 실명 1 + 실패 3 + null 1 + 불안정 1 |
| §3.2 `V4:233` / KO `:314` | "three of four 'unidentifiable' cells fail the learnability gate" / "**산업** 셀 4개 중 3개" | 산업 UNIDENT = 4, 통과 1 |
| §5.2 표 `V4:527-538` | ecom/homecredit/weather = *vacuous*, homesite = *unstable*, elec2 = blindness earned | 산업 UNIDENT = 4개, **전부 인증 실패 또는 불안정** |
| §5.2 산문 `V4:545` | "Three of four blindness claims … turn out to be vacuous" | 4번째 = ? |
| §7 `V4:675` | "The 0/8 aggregate … three cells are certificate-less and one is blind-but-certified" | 인증서 없음 3 + 실명인증 1 |

**특정되는 불일치 2건**

1. **§3.2 / §5.2 산문의 "4개 중 3개"** — 산업 unidentifiable 셀은 아티팩트상 4개
   (ecom·homecredit·weather·homesite)이고 **4개 모두** 실명 인증서를 얻지 못했다
   (3개 학습불능, 1개 시드 간 불안정). "4개 중 3개가 실패"는 homesite가 통과했다는 뜻이 되어
   §5.2 표의 *unstable* 및 PREREG §9(`:141`) "어느 라벨도 채택 금지"와 충돌한다.
   4번째가 elec2를 가리킨 것이라면 elec2는 산업 8셋이 아니므로 KO §3.2의 "**산업** 셀 4개"와 충돌.
2. **§7의 "one is blind-but-certified"** — 0/8 문맥에서 실명 인증을 받은 셀은 **8개 중 0개**다.
   유일한 earned blindness인 elec2는 TabReD가 아니다. 그리고 8개 중 인증서 없는 셀은 3이 아니라
   **4**(homesite 포함)다.

### 주장 5 — 분리도 D와 측정량의 구조적 관계 → **부분 확인됨**

산출물 (기존 아티팩트만 사용, 재실행 없음):
- `figures/claim5_D_vs_denoised.png` / `.txt`
- `figures/claim5_injection_vs_D.png` / `.txt`

#### (a) 요구된 산점도 — (D, denoised staleness)

| 패널 | Pearson | Spearman |
|---|---|---|
| 지도 HGB 탐색 (n=10) | r = −0.223 (p=0.54) | ρ = +0.075 (p=0.84) |
| 지도 HGB 확증 (n=10) | r = −0.225 (p=0.53) | ρ = +0.025 (p=0.95) |
| 배터리 14셀 | r = −0.410 (p=0.15) | ρ = −0.469 (p=0.09) |
| river 앵커 23셀 | r = −0.057 (p=0.79) | ρ = −0.122 (p=0.58) |

**어느 패널도 유의하지 않고, 이 산점도로는 주장을 판정할 수 없다 — 구조적 이유로.**
지도 패널은 10셀 중 8셀이 D∈[0.965,1.000]에 몰려 있고 저-D 점은 maps(0.578, null)와
insects(0.844, 유일한 CONCEPT) 둘뿐이라 상관이 insects 한 점으로 결정된다.
배터리 패널은 더 나쁘다 — 고-D 셀 4개가 **설계상 전부 covariate-shift 셀**(정답 = 규칙 불변)이고
concept 셀들은 D=0.515로 만들어졌다. **D와 정답 라벨이 설계 단계에서 교락되어 있다.**

#### (b) 실제로 답이 되는 것 — 주입 대조

주입은 *강도가 고정된(2.5 rad) 알려진 규칙 변화*를 각 셀의 실제 기하에 심는다.
이것이 주장 5의 직접 검정이다. 학습가능 주입만 (vacuous 제외):

| | Pearson | Spearman |
|---|---|---|
| HGB (결정등급, n=20) | r = −0.095 (p=0.69) | **ρ = −0.469 (p=0.037)** |
| 전 프로브 클래스 (n=38) | r = +0.094 (p=0.57) | **ρ = −0.338 (p=0.038)** |

순위 상관이 양쪽 다 유의하게 음수. 가장 깨끗한 대조:

- **같은 데이터셋(elec2), 다른 D**: linear 프로브 D=0.9048 → 회복 **+0.190** /
  HGB 프로브 D=1.0000 → 회복 **+0.018**. 학습가능성은 동급(0.981 vs 0.976). **10배 차이.**
- **D 사다리 (HGB)**: cooking D 0.9656 → +0.546 / delivery D 0.9990 → +0.332 /
  elec2 D 1.0000 → +0.018 / homesite D 1.0000 → **−0.052**
- **학습가능성은 D와 무관**: weather(D 0.994) lscore −0.022, elec2(D 1.000) lscore 0.976.
  붕괴하는 것은 학습가능성이 아니라 **회복**이다.

**판정**: 주장 5의 경험적 예측은 **주입 대조에서 유의하게 확인된다**. 다만 (i) 데이터셋 정체성과
교락되어 있고, (ii) 이것은 논문이 이미 D\*와 인증서로 명명한 현상이다 — UNIDENTIFIABLE 라벨
자체가 "이 기하에서는 못 본다"는 진술이다.

> 주장이 **새로 더하는 것**: D\*=0.96이 이분법 컷으로 쓰이지만 실제 감쇠는 D=0.96~1.00 구간
> **안에서** 연속적이고 급격하다. cooking(0.9656)에서 이미 최대 회복이고 delivery(0.9990)에서
> 1.7배 하락한다. 즉 **"인증됨" 셀 사이에서도 검정력이 크게 다르고, 이것은 §5.2 표에 없다.**

결정적 검정은 *동일 셀에서 D를 변화시키며 고정 강도 규칙을 심는 것*이고 재실행이 필요하다.
**패밀리 스윕으로는 안 된다 — 패밀리는 D를 바꾸지 않는다.**

### 주장 6 — arm 표본 크기 교란 → **이진 반증 / 회귀 확인**

#### (a) arm별 절대 성능은 아티팩트에 없다 (A.3). (a)항 직접 추정 불가.

#### (b) 그러나 배터리에 직접 측정치가 있다

`synth_stable`은 규칙 고정 + 공변량 이동 없음 = old 행이 recent와 i.i.d.다.
이 셀의 staleness는 **정의상 표본 크기 이득의 음수**다 (`deployment_decay_synth_summary.json`, n=12,000):

| 배터리 셀 | task | raw staleness | CI |
|---|---|---|---|
| synth_stable | binclass (AUC) | **−0.00074** | [−0.00156, **+0.00008**] — **0 포함** |
| synth_reg_stable | regression (z-RMSE) | **−0.01212** | [−0.01381, −0.01043] — **유의** |

- **이진분류**: (a)항 ≤0.0016 (floor의 8%)이고 0과 구분되지 않는다. 이진 산업 셀
  (ecom −0.0085, homecredit −0.0053, homesite −0.0041)의 음수는 크기 효과로 설명되지 않는다.
  → **주장 6은 이진 셀에서 반증.**
- **회귀**: (a)항이 −0.0121로 실재하고 floor의 60%다. 회귀 산업 셀의 관측 staleness는
  maps −0.0075 / delivery −0.0111 / cooking −0.0116 / weather −0.0124 — **전부 이 크기 안**이다.
  → **주장 6은 회귀 셀(산업 8셀 중 5개 + maps)에서 확인됨.**

#### (c) 코드 주석의 기존 방어와 그 증거 상태

주석(`:481-486`)은 크기 맞춘 변종을 "구현·GT테스트 후 기각"했고 "d=10..700 bias 스윕에서
N-vs-2N ≈ fixed-size"라 적는다. 이 기록의 유일한 소재는 `RESULTS_LEDGER.md:77-79`의 산문이다.
**해당 스윕의 아티팩트도, 재현 스크립트도, 크기 맞춘 변종의 코드도 저장소에 없다**
(`fixed-size`/`bias sweep` 전문 검색 결과 이 두 곳뿐). 기각 사유는 그럴듯하나 검증 불가.

#### (d) 크기 맞춘 arm 추가 구현 비용

- **변종 추가**: `_per_seed`에서 `recent2 = _sample(recent_pool, N, rng2)` 하나 더 뽑아
  `sRecRec` 계산 후 `stales_fixed.append(sRec − sRecRec)` — **약 6줄**. 기존 arm의 RNG 스트림을
  건드리지 않으려면 `rng2`에서 뽑아야 한다(v3 규약, `:488`). 런타임 +25~30%.
  단 주석의 기각 사유가 옳다면 이 변종은 covariate 셀에서 위양성을 낸다.
- **★ 더 싼 대안 (권장)**: `sRec`·`sOld`·`sRecOld`·`sRecOldD` **절대값을 출력에 추가** —
  3줄, 계산 비용 0, RNG 불변. (a)항이 셀별로 사후 계산 가능해진다.

---

## C. 스윕 실행 계획

### C.0 실행 전 필수 정정 — [D]가 할 수 없는 것

> `--inj-family`는 `_injection_recovers`(`:394`)에만 들어간다. 실 판정을 결정하는
> `staleness_harm` / `denoised_staleness`는 그 전에 실 라벨 `y`로 계산된다(`:653-665`).
> **패밀리를 바꿔도 실데이터 판정은 비트 단위로 불변이다.**

따라서 "셀이 발화 → 실데이터 양성 대조 획득 → §7 한계 3 해소"는 성립하지 않는다.
스윕이 만들 수 있는 "양성"은 심어진 규칙의 회복(= 실 기하 위 양성 대조)뿐이고,
이는 cooking/delivery에서 topvar로 이미 있다.
**자연 발생 산업 양성(한계 3)은 [D]로 접근 불가 — [C] full-span만이 그 후보다.**

실제 3(4)갈래:

| 결과 | 의미 | 헤드라인 영향 |
|---|---|---|
| **VACUOUS** (전 패밀리 학습불능) | 현상 유지 | 없음. 분모에 정보 없는 셀 존재가 재확인 |
| **EARNED-BLIND** (학습가능·미회복) | 인증서 획득, 그러나 여전히 "못 본다" | 분모 **복구 안 됨**. 정직성만 개선 |
| **RECOVERED** (학습가능·회복) | verified no-concept — **분모 복구** | 0/4 → 0/5 or 0/6 |
| **대조 셀 인증서 상실** | 인증서가 family-상대 | 검증 셀 수 감소 (0/4 → 0/2~0/3) |

### C.1 설계

**대상 셀 (8+1)**

```
[사전등록 §14/런북]  cooking_time  delivery_eta  ecom_offers  homecredit_default
                     weather  elec2  insects_incremental_balanced
[사전등록 밖·탐색]   homesite_insurance          ← "exploratory addition" 라벨 필수
[구현 대조·신규]     river_agrawal_abrupt        ← 판별 도구
```

- `homesite_insurance`는 §14 목록에 없다(A.2-5). 사전등록 집합에 사후 추가하는 것은 이 논문이
  파는 규율과 충돌하므로 **넣되 별도 라벨로 보고**한다. 근거: vacuous↔earned 불안정의 원인이
  lscore 0.516/0.694가 컷 0.65를 걸치기 때문이고(PREREG §9`:140-141`), 다른 패밀리가 안정시킬 수
  있는지는 답할 가치가 있다.
- `river_agrawal_abrupt`는 **구현 대조**. D≈0.498, topvar 회복 +0.201, lscore 0.956, n=8,000(수 분).
  어떤 패밀리에서 이 셀조차 회복하지 못하면 그 패밀리의 구현을 의심한다.

**패밀리 (3, 전부 구현됨)**: `lowvar`, `interaction`, `subpop`.
기준 `topvar`는 이미 실행된 지도가 대조군.

**강도: 이번 실행은 2.5 rad 고정.** 사유 3가지 —
1. `--inj-strength` CLI 플래그가 **존재하지 않는다**(A.1). 추가는 4줄이지만
   `run_deployment_decay.py` 수정이고, PREREG §4에 따라 `--synth` 재PASS가 선행되어야 한다.
2. 강도 차원은 **§14에 사전등록되어 있지 않다**(A.2-4). §6에 따르면 실행 전에 새 PREREG 섹션과
   예측 커밋이 필요하다.
3. 따라서 아래처럼 분리한다.

| | 내용 | 선행 조건 |
|---|---|---|
| **[D1]** | 패밀리 스윕 @ 2.5 rad — 사전등록 그대로 | 배터리 재PASS |
| **[D2]** | 강도 스윕 | `--inj-strength` 추가 + 배터리 재PASS + **새 PREREG 섹션과 예측 커밋** |

**이번 승인 요청은 [D1]까지.**

**시드**: 탐색 0–9 (n=10, 본 판독과 동일). 판정이 지도와 달라진 셀만 100–109 확증 (§14 규율).

### C.2 산출물 명세

무수정 코드가 이미 방출하는 것:
`injected_staleness`, `injected_staleness_ci`, `injection_learnable`, `injection_learn_score`,
`injection_recovered_strict`, `injection_features`, `injection_family`,
`per_seed{stale,den,decay,rec,noise_ratio}`, provenance(`meta.utc/git/argv/python/sklearn/numpy`).

**PART 5.1 요건 중 충족되지 않는 3건**

| 요건 | 상태 | 대응 |
|---|---|---|
| 시드별 원값 | 실 arm ✅ / **주입 arm ❌** | 3줄 추가로 `injected_per_seed` 방출. 없으면 rule-B 회복 CI의 사후 재구성이 또 불가해진다(§15b가 겪은 문제 반복) |
| 윈도우별 원값 | ❌ 구조적 부재 | `_per_seed`가 리스트를 반환하도록 5줄 변경 |
| arm별 절대 성능 | ❌ | `sRec/sOld/sRecOld/sRecOldD` 방출 3줄, 계산 비용 0 (주장 6 대응) |

세 항목 모두 **RNG 스트림을 건드리지 않는 순수 로깅 추가**다. 그래도 코드 변경이므로 PREREG §4
게이트(`--synth` 재PASS + 커밋본 14/14 일치)를 통과해야 한다 — §15b가 정확히 그 절차를 밟았고
raw 수치 비트 동일을 증명했으니 선례가 있다.

> **권고: 로깅 3건을 먼저 넣고 배터리 재PASS를 통과시킨 뒤 [D1]을 돌린다.**
> 로깅 없이 돌리면 이번 스윕의 아티팩트도 다음 라운드에서 "사후 계산 불가"가 된다.

**추가 요구 — 검정력 회계** (기존 프로토콜에 없던 항목)

규칙 A(one-sided α=0.05 → 실효 2.5%)와 학습가능성 게이트 통과를 가정:

| 패밀리 추가 | 위양성 위험 변화 | 검정력 변화 |
|---|---|---|
| 인증서 **상실** 방향 ("어떤 학습가능 패밀리에서든 미회복 ⇒ family-상대 재표기") | max-over-K. K=1 → 2.5% / K=3 → **≈7.3%** / K=4(topvar 포함) → **≈9.6%**. cooking·delivery가 순전히 우연으로 인증서를 잃을 확률이 4배 | 진짜 family-의존성 검출력 크게 상승 (목적) |
| 인증서 **획득** 방향 ("어떤 학습가능 패밀리에서든 회복 ⇒ 분모 복구") | 동일하게 K=3 → ≈7.3%. 공허 셀이 우연으로 "verified"가 될 위험 | 실제 검출 가능한 기하를 찾을 확률 상승 |
| 학습가능성 게이트 | 패밀리별 독립 판정 → "적어도 하나 학습가능" 확률 상승 → **공허 판정이 줄어드는 방향으로 편향** | — |

**기록 항목**: 패밀리별 (학습가능 여부, lscore, rule-A 회복, rule-B 회복,
`injected_staleness` ± CI). 셀 라벨 확정 시 **"K개 중 몇 개"를 숫자로 명시** — §14가 "전반"으로
남긴 공백을 결과 보고 시 메우되, 그것이 사후 명시임을 명기한다(규칙 변경 아님).

### C.3 실행 비용

**총 실행 수**: 8 대상 셀 × 3 패밀리 × 1 강도 × 10 시드 = 셀-런 24 (+river 대조 3) = **27 셀-런**,
프로세스 호출 **12회** (패밀리당 tabred 배치 1 + elec2 1 + insects 1 + river 1).

**wall-clock 근거** (기존 phase 로그 실측)

| 근거 | 실측 |
|---|---|
| `p2.log` HGB 8-tabred 확증 (경합 적음) | 18:50:51 → 21:26:16 = **2h35m** |
| `p2.log` HGB 8-tabred 탐색 (4-phase 경합) | 11h22m |
| `p3.log` linear 8-tabred / knn / rf | 1h49m / 1h43m / 5h52m |
| `p2.log` elec2 / insects | 14m / 68m |
| `p4.log` river 23스트림 (경합) | 11h14m ≈ 30m/스트림 |

이번 tabred 배치는 6셋(8셋의 3/4)이지만 **선택된 6셀이 전부 주입 단계로 라우팅되므로**
`INJ_SEEDS=10`의 추가 패스가 붙는다(지도 확증에서는 8셋 중 5셋만 주입). 배치당 **3–4 h**로 잡는다.

| 구성 | 시간 |
|---|---|
| 패밀리 1개 (tabred 6 + elec2 + insects + river 1) | 3–4h + 15m + 70m + 30m ≈ **5–6 h** |
| 3 패밀리 **순차** | 15–18 h |
| 3 패밀리 **병렬** (권장) | **5–7 h** |
| 확증 재실행 (판정 이동 셀만, 최악 4셀×3패밀리) | +3–5 h |

**병렬화**: 패밀리 3개를 각각 별도 **tmux 창**에서 동시 실행 (SERVER_RUNBOOK §2의 `nohup sh -c &`
루프는 tmux로 고쳐 쓸 것). `run_deployment_decay.py`는 출력 경로를 하드코딩하므로(`:1095`)
**각 패밀리 완료 직후 즉시 별도 폴더로 `cp`** — A.4의 스탬프 충돌·`summary_latest.json` 경합을
코드 변경 없이 회피한다.

**중간 체크포인트 3곳**

1. **배터리 재PASS 직후** — 로깅 추가 코드가 14/14 판정 일치 + raw 수치 비트 동일인지.
   불일치면 즉시 중단.
2. **river 구현 대조 3건 (패밀리당 수 분)** — `river_agrawal_abrupt`가 3패밀리 전부에서
   학습가능+회복하는지. **여기서 멈춰서 본다.** 실패 패밀리가 있으면 그 패밀리의 tabred 배치를
   돌리기 전에 구현을 의심한다.
3. **첫 패밀리(`lowvar`) tabred 배치 완료 시** — cooking/delivery 유지 여부, 공허 3셀 중 하나라도
   학습가능해졌는지. 예측 대조 1차 기록.

---

## D. 사전 예측 (실행 전 커밋)

세 결과 라벨은 C.0의 정의를 쓴다: **VACUOUS**(전 패밀리 학습불능) /
**EARNED-BLIND**(학습가능·미회복) / **RECOVERED**(학습가능·회복 = verified no-concept = 분모 복구).
확률은 "3 패밀리 중 적어도 하나에서" 기준, 각 행 합계 100%.

### 핵심 기제 가설 (예측의 근거)

공허 3셀의 실패는 감사 L4(`:366`)가 실증한 것과 같은 **top-variance 열의 heavy tail** 때문이다.
두꺼운 꼬리 열을 z-표준화하면 99%의 행이 ≈0에 몰리고 `N(0,0.3)` 잡음이 score를 지배 → 라벨이
사실상 동전던지기 → 윈도우 내 AUC 0.51–0.53. 이 가설의 함의:

- `interaction`은 **여전히 f0,f1(top-var)을 쓴다**(`:349-350`) → `z(a·b)`는 꼬리가 더 두꺼움
  → **거의 확실히 더 나쁨**
- `subpop`도 **f0,f1을 그대로 쓴다**(`:352-354`) → topvar와 유사, 절반이 정적 규칙이라 약간 나을 수 있음
- `lowvar`만 **다른 열**을 쓴다(`:342-345`) → 최저분산 비퇴화 열은 이진 지시자일 가능성이 높고
  꼬리가 없음 → **공허 셀을 뒤집을 유일한 후보**

### 예측 표

| 셀 | 예측 (최빈) | 확률 | 근거 |
|---|---|---|---|
| **ecom_offers** | VACUOUS | 45% (EARNED-BLIND 40% / RECOVERED 15%) | topvar lscore 0.527(hgb)·0.516(rf)·0.516(knn) — 전 프로브 무작위 수준 = heavy-tail 진단. interaction·subpop은 같은 열 재사용 → 학습불능 유지 예상. lowvar만 승산(≈55%). 학습가능해져도 **D=1.0000**이라 회복 낮음 (주장 5 증거: D≥0.999 학습가능 셀 회복 = elec2 ✗, homesite ✗, sberbank ✗, delivery ✓ → 1/4) |
| **homecredit_default** | EARNED-BLIND | 45% (VACUOUS 35% / RECOVERED 20%) | lscore 0.633/0.637/0.617 — **컷 0.65에 가장 가깝다**. 넘길 가능성 셋 중 최고. 단 `proxy-stripped:80` 이후 공간이고 D=0.9998 → 회복은 어려움. **EARNED-BLIND가 되면 주장 3의 (ii)만 해소되고 +0.005는 UNIDENT로 남는다** |
| **weather** | VACUOUS | 55% (EARNED-BLIND 35% / RECOVERED 10%) | 유일하게 lscore **음수** (R² −0.0221/−0.0283/−0.0297, 전 프로브 일관). 회귀 게이트가 R²≥0.20 — 이진의 AUC≥0.65보다 훨씬 먼 거리. −0.02 → +0.20 도약 필요 |
| **homesite_insurance** *(탐색 추가)* | EARNED-BLIND | 45% (VACUOUS 30% / 여전히 불안정 20% / RECOVERED 5%) | lscore 0.516↔0.694가 컷을 걸쳐 흔들림. D=1.0000이고 topvar 주입이 **음수 회복**(−0.052/−0.055) — 심어진 규칙조차 old 데이터가 도움 방향. 회복 가능성 최저 |
| **cooking_time** *(대조)* | **인증 유지** | **70%** | topvar 회복 +0.546/+0.555/+0.577 = floor의 27×, lscore 0.89, **D 0.9656으로 인증서 셀 중 최저 = 검정력 최고.** 30% 실패 위험은 거의 전부 `lowvar`에서 |
| **delivery_eta** *(대조)* | **인증 유지** | **60%** | 회복 +0.332/+0.318 = floor의 16× (cooking의 60%), **D 0.9990**으로 cooking보다 훨씬 높음. 주장 5의 감쇠 기울기상 마진이 얇다. `subpop`(유효 drift 절반)에서 floor 아래 하락 위험 최대 |
| *(참조)* insects | 전 학습가능 패밀리 회복 | 85% | D 0.8436, 회복 +0.162, 다클래스. 실패 시 §14대로 §6에 은폐 없이 보고 |
| *(참조)* elec2 | EARNED-BLIND 유지 | 75% | lscore 0.976(최고 학습가능) + 회복 +0.018(floor 미달) — 주장 5의 가장 깨끗한 사례 |
| *(참조)* river_agrawal_abrupt | 3/3 회복 | 95% | 구현 대조. 실패 = 구현 결함 |

**전체 예측 한 줄** — 공허 3셀 중 **적어도 1개가 어떤 패밀리에서 학습가능해질 확률 ≈ 70%**,
**적어도 1개가 회복(분모 복구)될 확률 ≈ 35%**, **cooking/delivery 중 적어도 하나가 어떤 학습가능
패밀리에서 인증서를 잃을 확률 ≈ 50%**.
가장 가능성 높은 단일 시나리오: *lowvar에서 homecredit이 학습가능해지나 회복하지 못해
EARNED-BLIND로 승격, cooking은 유지, delivery는 subpop에서 경계.*

### 3개 문항

**Q1. 어떤 결과가 나오면 헤드라인을 0/8에서 내려야 하는가**

[D] 단독으로는 **어떤 결과도 0/8을 내리지 못한다** — 패밀리는 실 판정에 비트 단위로 영향이
없다(C.0). 0/8이 내려가는 조건은 §14가 규정한 대로 오직 **[C] full-span에서 산업 셀이
DEPLOYMENT-CONCEPT를 발화하고 시드 100–109 확증을 통과하는 경우**다.

다만 **"0/8" 표기 자체는 스윕과 무관하게 지금 바꿔야 한다**: 8셀 중 4셀
(ecom·homecredit·weather·homesite)이 인증서 없이 분모에 들어가 있다는 것은 주장 1에서 이미
아티팩트로 확인되었다. 정직한 표기는 **"감사 8, 인증서로 판독 가능 4, 그중 mean-rule drift 0"**.
스윕의 역할은 그 4를 늘리거나(RECOVERED) 줄이는(대조 셀 인증서 상실) 것뿐이다.

**Q2. 어떤 결과가 나오면 실데이터 양성 대조를 획득했다고 선언하는가**

엄밀히는 **이미 갖고 있다** — cooking(+0.55)·delivery(+0.33)의 topvar 회복이 실데이터 기하 위의
양성 대조다. 스윕이 추가할 수 있는 것은 **그 대조의 신호-클래스 폭**이다.

선언 기준: *현재 공허한 셀(ecom / homecredit / weather / homesite) 중 하나에서, 학습가능한
패밀리의 주입이 rule-A와 rule-B 양쪽에서 회복되고 시드 100–109 확증을 통과할 때* — 그때 그 셀은
"measurable하지만 drift 없음"으로 승격되고 분모가 복구된다.

**§7 한계 3(자연 발생 산업 양성 부재)은 [D]로 해소되지 않는다.** 심어진 규칙은 자연 발생이 아니다.
그 한계는 [C] 또는 새 데이터셋만이 건드릴 수 있다.

**Q3. 대조 셀 2개가 스윕에서 인증에 실패하면 그것은 무엇을 의미하는가**

두 해석이 경합하고, 판별은 **river 구현 대조**가 한다.

- **(a) `river_agrawal_abrupt`가 같은 패밀리에서 회복하면** → 구현 정상. cooking/delivery의 실패는
  실질적 발견이다: "verified no-concept" 인증서는 신호 패밀리에 상대적이며, 이 논문의 가장 강한
  두 셀조차 top-variance 회전 밖에서는 검정력이 없다. §14 규정대로 §5.2를 family-상대로 재표기,
  §3.2를 본문 발견으로 승격. 헤드라인의 "검증된" 셀은 0/4에서 0/2~0/3으로 더 줄어든다.
- **(b) `river_agrawal_abrupt`가 같이 실패하면** → **스윕 구현 자체를 의심한다.** D≈0.50,
  lscore 0.956의 저-분리도 앵커에서 회복이 안 되는 것은 기하 문제가 아니라 코드 문제다.
  우선 의심 지점 2곳:
  1. `interaction`의 `a := z(a*b)`가 `b`를 그대로 두 번째 축으로 쓰는 구성(`:349-350`) — 두 축이 종속
  2. `lowvar`의 `nz` 선택이 `std>1e-9`만 걸러 사실상 상수인 열을 잡는 경우(`:343`)

  이 경우 결과는 어느 방향으로도 보고하지 않고 구현을 고친 뒤 재실행한다.

어느 쪽이든 **은폐 금지**. (b)는 스모크 테스트가 산출물을 남기지 않았다는 A.1의 공백이 실제로
대가를 치른 사례로 기록한다.

---

## E. 불일치·불확실 목록

### E.1 외부 검토 주장 중 저장소가 반증하는 것

1. **주장 2 (δ 미산출)** — 반증. 공허 3셀 모두 `delta_staleness` 기록됨
   (ecom 0.0040 / homecredit 0.0023 / weather 0.0008). 실질(δ를 검출가능성으로 읽으면 안 됨)은 유효.
2. **주장 3의 경로** — 부분 반증. homecredit이 양성으로 안 잡히는 실제 이유는 무증거 대역이 아니라
   캐스케이드에서 `disjoint`(D=0.9998)가 `den_sub`보다 먼저 걸리기 때문(`:721-724`). 결론은 유지.
3. **주장 3의 배수** — 검토가 "2~5배"라 했으나 실제 앵커 2건이 둘 다 ≈0.001이므로 **4.8–6.7배**.
   검토가 과소평가했다.
4. **주장 6** — **이진 셀에서 반증.** `synth_stable`이 (a)항을 −0.00074 [CI 0 포함]으로 직접
   측정한다. 회귀 셀에서는 확인됨(−0.0121). "모든 δ가 부풀려져 있다"는 절반만 옳다.
5. **지시 §1.3의 "셀이 발화" 갈래** — 성립하지 않음. `--inj-family`는 실 판정에 영향 없음(C.0).
   "§7 한계 3이 해소된다"는 서술은 틀렸다.
6. **지시 §1.2의 "30 CPU-시간"** — 저평가. 3패밀리 병렬 5–7h wall-clock이고 서버 코어 수를 곱하면
   CPU-시간은 훨씬 크다. 초고 §8의 "no single dataset cell exceeds a few CPU-hours"는 셀 단위
   진술이지 스윕 총량이 아니다.
7. **주장 4의 "elec2는 TabReD 8개에 포함되지 않는다"** — 맞다. 그것이 §7 문장 불일치의 정확한 원인.

### E.2 확인할 수 없었던 것

1. **`--inj-family` 스모크 테스트의 실제 수치** — 산출물 없음. 커밋 메시지 `ffe1e56`와
   `REVIEW_ROUND6_2026-07-18.md:13`의 산문("4패밀리 전부 학습가능 AUC 0.95±")만 존재. 어느
   데이터셋에서 측정했는지, 재현 가능한지 확인 불가. **이 값은 신뢰할 근거가 없다.**
2. **N-vs-2N bias 스윕 (d=10/400/700)** — `RESULTS_LEDGER.md:77-79`의 산문뿐. 아티팩트·스크립트·
   코드 경로 전무. 코드 주석(`:481-486`)이 "구현·GT테스트 후 기각"이라 주장하는 크기 맞춘 변종의
   코드도 없다.
3. **주장 5의 결정적 검정** — 동일 셀에서 D만 변화시키며 고정 강도 규칙을 심는 실험. 기존
   아티팩트로는 데이터셋 정체성과의 교락을 풀 수 없다. **패밀리 스윕도 이를 풀지 못한다(D 불변).**
4. **RF 프로브에서 homesite +0.0055, homecredit +0.0145가 둘 다 CI-양수** — RF는 결정등급
   클래스인데 초고 §5.4는 "HGB↔RF 10/10 작동적 일치"만 보고하고 이 denoised 부호 차이를 다루지
   않는다. 판정 라벨은 같으므로 형식상 모순은 아니나, 주장 3의 "homecredit이 유일한 양수"에 RF
   맥락을 붙이면 그림이 달라진다. 초고에 서술이 없어 의도 확인 불가.
5. **서버 머신에서의 배터리 재PASS** — PREREG §15a(`:327`)가 "잔여: 서버 머신 자체에서의
   재실행(§14 실행 시 선행)"이라 명시. 미실행. **[D1] 실행의 사전 조건이다.**

### E.3 지시 자체의 모순·오류

1. **§1.3의 첫 갈래가 기제상 불가능**(E.1-5). 이 지시의 핵심 동기 중 가장 매력적인 갈래가
   [D]가 아니라 [C]에 속한다. **우선순위 재검토 여지**: 한계 3(자연 발생 양성)과 한계 2(train 구간
   한정)를 동시에 건드리는 것은 [C] full-span이고, §14에서 유일하게 헤드라인 재스코프 트리거를
   갖는 것도 [C]다. 다만 [D]가 대조 셀 인증서를 무너뜨릴 수 있다는 점(≈50%)에서 방어적으로 더
   급한 것도 사실이다.
2. **§4.1의 "강도 최소 2단계"가 사전등록 밖 + 코드 미지원.** `--inj-strength` 부재(A.1),
   §14에 강도 차원 없음(A.2-4). PREREG 수정 금지와 충돌 → C.1에서 [D1]/[D2]로 분리.
3. **§4.1 대상 셀 목록이 §14와 불일치.** 지시는 homesite를 넣고 elec2를 뺀다. §14/런북은 반대.
   지시대로만 하면 사전등록 집합을 임의 변경하는 것이 된다 → C.1에서 합집합 + 라벨 분리로 처리.
4. **§5 예측 표의 라벨 "인증 / 여전히 공허 / 발화"가 캐스케이드 라벨과 대응하지 않는다.**
   "인증"(earned blindness)은 분모를 복구하지 **않는다** — 여전히 "못 본다"이다. 분모를 복구하는
   것은 "회복"(INJECTION-RECOVERED)이다. §1.3 표의 "셀이 인증됨 → 분모 복구"는 이 구분을
   혼동한다. D절에서 3라벨을 재정의해 답했다.
5. **§7 "PREREG 수정 금지"와 §4.2 "PART 5.1 로깅 요건 충족"의 잠재 충돌.** 요구된 로깅 3건은
   코드 변경을 요구하고, 코드 변경은 PREREG §4의 배터리 게이트 재통과를 요구한다. PREREG 문서
   자체는 수정하지 않아도 되지만 **절차상 배터리 재PASS가 [D1] 앞에 들어간다.**
   C.3 체크포인트 1로 반영.

---

## F. 승인 요청 항목

| # | 항목 | 비고 |
|---|---|---|
| 1 | 로깅 3건 추가 (`sRec/sOld/sRecOld/sRecOldD` 절대값, 윈도우별 원값, 주입 per-seed) + `--synth` 배터리 재PASS (서버 머신) | RNG 불변. PREREG §4 게이트 통과 필수 |
| 2 | `river_agrawal_abrupt` × 3 패밀리 구현 대조 (수 분) | 체크포인트 2 |
| 3 | **[D1]** 패밀리 스윕: 3 패밀리 × 8 셀 × 시드 0–9, 2.5 rad 고정 | 5–7 h (tmux 3-way 병렬) |
| 4 | 판정 이동 셀만 시드 100–109 확증 | §14 규율 |

**별도 승인 건으로 남김**: **[D2]** 강도 스윕 (플래그 추가 + 배터리 재PASS + 새 PREREG 섹션),
**[C]** `--tabred-span full` 감사.

### 부속 산출물

- `figures/claim5_D_vs_denoised.png` / `.txt` — (D, denoised staleness) 산점도 4패널 + 상관
- `figures/claim5_injection_vs_D.png` / `.txt` — 주입 회복 vs D (주장 5의 실질 검정)
