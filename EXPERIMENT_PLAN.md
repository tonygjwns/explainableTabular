# EXPERIMENT PLAN: Time-Indexed Distribution Memory for Drift-Aware Tabular Learning

> 이 문서는 아키텍처가 구체화된 후의 **정식 실험 계획**입니다.
> 기존 PLAN.md의 phase 구조는 유지하되, "voting head"라는 막연한 표현을
> "시간 인덱싱 분포 메모리 + 검색(retrieval)" 이라는 구체 구조로 대체합니다.
> HANDOFF.md(컨텍스트) → 이 문서(아키텍처+계획) → SETUP.md(구현) 순으로 읽으세요.

---

## 0. 한 줄 요약

표 데이터의 시간 분포 변화를, **시간에 따라 진화하는 프로토타입 메모리**에 저장하고,
입력이 **자기 시점의 어느 프로토타입에 가까운지 검색**하여 예측하는 구조.
Cai et al.(변조)과 TabR(무시간 검색)이 둘 다 안 한 "시간 인덱싱된 검색"의 빈 자리를 차지한다.

---

## 1. 핵심 통찰: 왜 "head"가 아니라 "메모리"인가

이 프로젝트의 결정적 전환은 다음 직관에서 나왔다:

> "시간에 따른 feature 분포 정보를 가진 *어딘가*가 있고,
> 들어온 입력이 그중 *어느 것에 가까운지*를 보는 구조여야 한다."

이 문장을 분해하면 세 요소가 나온다:

1. **"분포 정보를 가진 어딘가"** = 정보를 *저장*하는 구조 → **메모리**
2. **"어느 것에 가까운지"** = *검색(retrieval)*
3. **"시간에 따른"** = 그 메모리가 *시간을 명시적 좌표로* 가짐

여기서 왜 head로는 부족한지가 분명해진다. **head는 "표현 → 출력" 변환일 뿐 정보를 저장하지 않는다.**
우리가 원하는 건 시간별 분포를 저장하고 조회하는 것이므로, 이건 변환 구조가 아니라 **메모리 구조**다.

이 통찰은 두 가지 분석으로 뒷받침된다:

- **Cai et al. NeurIPS 2025 증거**: 시간 정보를 출력 단(head 근처)에만 주입하면 전체 효과의 12.6%에 불과.
  입력 단이 87.4%. → head-only 시간 주입은 성능에서 약하다.
- **TimeMCL 메커니즘 분석**: Winner-Takes-All은 *출력 분포의 모드*를 따라 head를 분화시키지,
  *시간축*을 따라 분화시키지 않는다. → 순수 WTA head로는 시간 구조를 발견조차 못 한다.

결론: 시간은 **메모리의 명시적 좌표**(프로토타입을 시간의 함수로)와 **입력 단 약한 주입**으로 다루고,
검색이 "입력이 어느 시점-프로토타입에 가까운지"를 판단한다.

---

## 2. 우리 위치 (관련 연구 교차점)

| 방법 | 시간 정보 | 메커니즘 |
|---|---|---|
| Cai et al. (NeurIPS 2025) | 있음 | 변조(modulation) — 검색 없음 |
| TabR / ModernNCA | 없음 | 검색(retrieval) — 시간 없음 |
| **우리** | **있음** | **검색(retrieval)** |

"시간 인덱싱된 검색"은 변조 진영과 무시간 검색 진영 사이의 **빈 교차점**이다.
그리고 시간 좌표가 명시적이므로 해석가능성이 구조에 내장된다:
"이 입력은 *2021년 3분기의 프로토타입 #7*에 가장 가까웠고, 그 프로토타입은 2021→2024 동안
이 방향으로 이동했다" 라고 말할 수 있다.

**메인 청구는 성능이 아니라 해석가능성 + 보완적 메커니즘이다.** (이전 결정 유지)

---

## 3. 아키텍처

### 3.1 학습되는 파라미터 (5개 그룹)

```
1. Backbone Encoder    z = Encoder(x)              [TabM 백본]
2. 프로토타입 베이스    P_k^base   (K개의 d차원 벡터)
3. 드리프트 함수        drift_k(·)  (각 프로토타입의 시간 변화, 작은 MLP)
4. 시간 임베딩          Fourier(t) 위의 학습 가중치  [Cai & Ye ICML 2025 방식]
5. 예측기              Predictor(·) → ŷ
```

전부 **하나의 손실로 end-to-end** 학습된다.

### 3.2 Forward (한 샘플 (x, y, t_x) 기준)

```
1. 인코딩
   z = Backbone(x)                              # x에 시간 약하게 주입 (3.4 참고)

2. 메모리 구성 (현재 파라미터로, t_x 시점)
   P_k(t_x) = P_k^base + drift_k(Fourier(t_x))   # k = 1..K
   → "t_x 시점에 데이터가 어떻게 생겼는지"를 K개 프로토타입으로 표현

3. 검색 (분포 유사도; 시간은 P_k(t_x)에 이미 반영됨)
   s_k = -‖z - P_k(t_x)‖² / τ
   w_k = softmax(s_k)                            # 가까운 프로토타입에 큰 가중

4. 집계
   V_k = W_y(label_dist_k) + value_k             # 라벨 분포 임베딩 + 학습 벡터
   aggregated = Σ_k w_k · V_k

5. 예측
   ŷ = Predictor(z, aggregated)
```

### 3.3 시간이 학습되는 메커니즘 (핵심 원리)

**열쇠: 미니배치에 서로 다른 시점의 샘플들이 섞여 있다.**

```
미니배치 예:
  (x_a, y_a, t=2021Q1)
  (x_b, y_b, t=2021Q1)
  (x_c, y_c, t=2023Q3)
  ...
```

- 2021Q1 샘플은 `P_k(2021Q1)`과 비교, 2023Q3 샘플은 `P_k(2023Q3)`과 비교
- 두 시점의 분포가 다르면, 같은 `P_k^base`로 양쪽을 다 잘 분류하려면
  **drift_k가 시점마다 다른 위치를 만들어야 한다**
- → 손실이 drift_k에게 "시간에 따라 프로토타입을 움직여라"고 압력을 가함

이것이 head로 안 됐던 것의 해결이다. head는 시점을 저장할 곳이 없었지만,
`P_k(t)`는 시점 t를 명시적 입력으로 받으므로 시간 구조를 담는다.

### 3.4 시간 주입 위치 (결정 4)

```
주(主) 통로:  프로토타입 P_k(t) = P_k^base + drift_k(Fourier(t))   ← 메모리에 시간
보조 통로:    z = Backbone( concat[x, Fourier(t_x)] )            ← 입력에도 약하게
```

이유: 메모리가 우리 직관의 핵심 통로지만, Cai 증거(입력단 87.4%)를 무시할 수 없으므로
입력에도 시간을 약하게 주입해 성능 안전망을 둔다. ablation에서 "메모리만 / 입력만 / 둘 다"를 비교한다.

---

## 4. 설계 결정과 근거 (승인됨)

각 결정의 원칙: **핵심 직관(시간 메모리 + 검색)만 먼저 세우고, 화려한 요소는 증거 기반으로 나중에 추가.**
외부 검토(GPT)가 가장 강하게 경고한 "ablation factory" 함정을 피하기 위함.

**결정 1 — 검색은 단순 softmax부터.**
TabR의 보정항 `T(W_K(x)-W_K(x_i))`은 검색 대상이 실제 학습 인스턴스일 때 의미가 있다.
우리 프로토타입은 학습된 추상 점이라 "차이 보정"의 의미가 약하다. 단순 softmax로 시작하고,
보정항은 Phase 2 ablation 항목으로 둔다.

**결정 2 — V_k는 라벨 분포 임베딩 + 학습 벡터.**
순수 학습 벡터만 두면 "이 프로토타입이 무슨 클래스를 대표하는지" 해석이 흐려진다.
메인 청구가 해석가능성이므로 라벨 정보가 V_k에 명시적으로 있어야 한다.
프로토타입별 학습 가능한 라벨 분포가 무엇으로 수렴하는지가 곧 해석 결과다.

**결정 3 — WTA는 처음엔 넣지 않는다. soft softmax 집계로 시작.**
(팀원 제안과 정면으로 다른 방향이므로 팀 토론 필요)
WTA는 출력 분포 모드로 분화하지 시간축을 안 잡는다. 우리 시간축은 이미 `P_k(t)`의 t 입력이
담당하므로 WTA를 넣으면 두 메커니즘이 충돌한다. WTA의 학습 불안정(승자만 backprop,
dead head, annealing 튜닝)은 토대가 흔들릴 때 디버깅을 어렵게 한다.
전문화가 부족하다는 증거가 나오면 Phase 2에서 annealed WTA(aMCL)를 추가한다.

**결정 4 — 시간은 메모리(주) + 입력(보조) 양쪽에.** (3.4 참고)

**결정 5 — 프로토타입은 시간 슬라이스별 KMeans로 초기화.**
랜덤 초기화하면 drift가 학습 초반에 헤맨다. 학습 데이터를 시간 구간으로 나눠 각 구간에서
KMeans로 `P_k^base`를 초기화하면, 프로토타입이 시간별 분포에서 출발해 drift는 미세 조정만
하면 된다. 수렴이 빠르고 안정적이다.

---

## 5. 손실 함수

```
L = L_main(ŷ, y)                  # 분류: cross-entropy, 회귀: MSE
  + λ₁ · L_smooth                 # 궤적 매끄러움 (외삽 가능성)
```

**L_smooth (궤적 매끄러움)**: 프로토타입이 시간에 따라 급변하지 않도록.
```
L_smooth = Σ_k ‖drift_k(t) - drift_k(t')‖² / |t - t'|²   (가까운 t, t' 쌍)
```
미래 외삽이 가능하려면 궤적이 매끄러워야 한다. (회귀에서 label-conditional smoothness를 쓸 때는
앞선 결정대로 `|y_i - y_j| < δ` 임계값 사용.)

**Phase 1에서 의도적으로 뺀 손실** (Phase 2 ablation으로 추가):
- WTA 손실 + annealing (결정 3)
- 분포 정렬 손실
- 다단계 시간 주입

---

## 6. Phase 1 최소 버전 (먼저 깨끗하게 검증할 것)

복잡도를 의도적으로 낮춘 첫 구현. **핵심 가설("시간 인덱싱 메모리 + 검색"이 작동하는가)만 검증.**

```
1. Backbone: TabM (입력에 Fourier(t_x) 약하게 concat)
2. 메모리: P_k(t) = P_k^base + drift_MLP_k(Fourier(t))
   - K = 1000, 시간 슬라이스 KMeans 초기화
3. 검색: w_k = softmax(-‖z - P_k(t_x)‖² / τ)        [단순, WTA 없음]
4. 집계: Σ_k w_k · V_k,  V_k = 라벨분포 임베딩 + 학습벡터
5. 예측: Predictor(z, aggregated)
6. 손실: L_main + λ₁ · L_smooth
```

이게 작동하면 → 요소를 하나씩 더해 향상을 측정.
안 되면 → 핵심 가설 자체가 틀린 것이므로 빨리 알 수 있다. (C→B Gate 철학)

---

## 7. 평가 및 베이스라인

### 7.1 데이터셋 / 프로토콜
- TabReD 8개 데이터셋
- Cai & Ye ICML 2025의 개선된 시간 분할(학습 lag 0, validation bias 최소화)
- 시드: Phase 0/1 = 5~10, Phase 2 메인 = 20
- 지표: 분류 ROC-AUC, 회귀 RMSE + MLP 대비 상대 향상율
- 통계: paired Wilcoxon signed-rank + Benjamini-Hochberg FDR + Hedges' g

### 7.2 베이스라인 (단계적)
- **Phase 0 필수**: TabM 재현
- **Phase 1**: Cai et al. NeurIPS 2025 재구현 (우리 구조가 그 위/옆에 어떻게 서는지)
- **Phase 2 (결과 나온 후)**: TabM-RW, Cai & Ye ICML 2025, GBDT 3종, TabR/ModernNCA 등
  - 이미 공개된 신뢰할 만한 수치는 인용, 비교가 결정적인 것만 직접 재현

---

## 8. Sanity Check / C→B Gate (Phase 1)

핵심 가설을 빠르게 검증하고, 결과에 따라 메인 청구(C) 또는 진단 paper(B)로 분기.
PRE_REGISTRATION.md에 기준을 사전 commit하고 동료 입회 하에 판정.

### 갱신된 테스트 (새 아키텍처 반영)

**Test 1 — 시간 인덱싱이 무시간보다 나은가**
- 비교: `P_k(t)` (시간 메모리) vs `P_k` (고정 메모리, drift 제거) vs Cai-only baseline
- PASS: 시간 메모리 > 고정 메모리, 4개 중 3개 데이터셋에서 paired Wilcoxon p<0.05, Hedges' g ≥ 0.3

**Test 2 — 외삽 검증**
- 학습 데이터 시간 70%까지만 학습, 나머지 30% 시점에 `P_k(t)` 외삽
- 외삽된 프로토타입 위치 vs 실제 그 시점 데이터 분포 비교
- PASS: 분산 설명력(R²) ≥ 30%가 4개 중 2개 데이터셋에서

**Test 3 — 검색이 의미 있는가 (해석가능성)**
- 학습된 프로토타입 궤적 `P_k(t)`를 PCA/UMAP 시각화
- 입력들이 자기 시점 프로토타입에 실제로 가깝게 검색되는지 확인
- PASS: 궤적이 매끄럽고 방향성 있음 + 검색 가중치가 의미 있게 집중 (동료+본인 동의)

**Test 4 — 시간 주입 위치 비교 (준이 질문의 실증)**
- "메모리만 / 입력만 / 둘 다" 비교
- head-only(또는 입력-only)가 메모리 방식보다 약함을 데이터로 확인
- 이 테스트는 PASS/FAIL 결정엔 안 쓰고, 설계 정당화 근거로 사용

### 판정
- Test 1·2·3 모두 PASS → **Phase 2a** (메인 청구로 전진)
- 하나라도 FAIL 또는 AMBIGUOUS → **Phase 2b** (진단 paper로 pivot)

---

## 9. Phase 2 Ablation 로드맵 (Phase 1 PASS 시)

핵심 구조가 작동하면, 다음 요소를 **하나씩** 더해 향상을 측정한다 (leave-one-out 병행):

1. **TabR식 보정항** — 검색에 `T(z - P_k(t))` 추가가 도움 되는가
2. **Annealed WTA (aMCL)** — 프로토타입 전문화가 부족할 때 (결정 3의 조건부 추가)
3. **다단계 시간 주입** — 중간 표현에도 시간 변조 (Cai식)
4. **K 스케일** — 프로토타입 수 (500/1000/5000)
5. **TabM 32 submodel과의 결합 방식** — 메모리를 submodel별 vs 공유

각 추가는 sequential + leave-one-out 둘 다로 검증해 음의 상호작용을 포착한다.

---

## 10. 관련 연구와의 차별화

**Cai et al. (NeurIPS 2025)** — representation을 시간 정렬(변조). 검색 없음.
우리는 decision 단계에서 **시간 인덱싱 메모리 검색**. 보완적이며, 그들 위에 build 가능.

**TabR / ModernNCA** — 검색 기반이지만 시간 좌표 없음. TabReD 시간 분할에서 실패.
우리는 검색에 **시간 인덱싱**을 추가. 그 실패의 빈자리를 메움.

**TimeMCL (ICML 2025)** — WTA로 프로토타입을 centroid로 학습. 단, 출력 모드로 분화(시간축 아님).
우리는 WTA를 처음엔 안 쓰고(결정 3), 필요 시 aMCL annealing만 차용.

**EvolveGCN (AAAI 2020) / Latent ODE (NeurIPS 2019)** — 모델 컴포넌트의 시간 진화 원리.
도메인(그래프/시계열)·진화 대상(가중치/잠재상태)·시간 표현(이산 RNN/ODE)이 모두 다름.
우리는 표 데이터 + 프로토타입 메모리 + 연속 Fourier. 인용+차별화.

---

## 11. 위험 및 완화

| 위험 | 완화 |
|---|---|
| 시간 메모리가 무시간 검색보다 안 나음 (Test 1 FAIL) | Phase 2b 진단 paper. "왜 시간 검색이 안 통하는가" 자체가 메시지 |
| 외삽 실패 (Test 2 FAIL) — drift가 보간만 함 | Fourier 차수·smoothness 강도 조정. 그래도 안 되면 핵심 가설 재검토 |
| Cai et al.를 못 이김 | 메인 청구는 성능 아닌 해석가능성. 동등 성능 + 해석 가치로 충분 |
| TabM 32 submodel과 메모리 충돌 | Phase 1은 단일 백본 표현 사용, 32 결합은 Phase 2 ablation |
| 복잡도 폭발 (ablation factory) | Phase 1 최소 버전 엄수. 요소는 하나씩만 추가 |
| 팀원 WTA 제안과 방향 충돌 | 결정 3을 데이터로 검증 (Test에서 WTA 유무 비교 가능) |

---

## 12. 일정 (기존 PLAN.md 유지)

```
Phase 0 (3주):  TabM + Cai 재현, 환경 셋업
Phase 1 (3주):  최소 버전 구현 + Sanity Check (Test 1~4)
Phase 2a/2b (6-7주):  PASS → 풀 평가 + 해석 case study / FAIL → 진단 paper
Phase 3 (3주):  글쓰기
```

H100 ×2, 독립 병렬 실행.

---

## 13. 팀 토론을 위한 열린 질문

1. **결정 3 (WTA를 처음엔 뺀다)에 동의하는가?** 팀원의 WTA 제안과 정면으로 다른 방향.
   Test에서 WTA 유무를 비교해 데이터로 결판내는 방식은 받아들일 만한가?

2. **V_k의 "라벨 분포"를 분류/회귀에서 각각 어떻게 정의할 것인가?**
   분류는 클래스 분포 벡터, 회귀는? (스칼라 라벨 임베딩? 구간 분포?)

3. **시간 슬라이스 KMeans 초기화의 슬라이스 개수**는 몇 개가 적절한가?
   데이터셋 기간/크기에 따라 다를 텐데, 자동 결정 기준이 필요한가?

4. **TabM 백본의 32 submodel을 Phase 1에서 어떻게 다룰 것인가?**
   단순히 평균? 아니면 처음부터 1개 표현만? (Phase 2에서 본격 결합 실험)

5. **"검색"과 "예측기"의 역할 분담**: aggregated를 z와 concat해서 Predictor에 넣는데,
   Predictor가 너무 강하면 메모리 검색이 무시될 수 있다. Predictor를 단순하게 제약할 것인가?

---

## 부록: 이 아키텍처에 도달하기까지

이 구조는 다음 사고 흐름의 결과다 (상세는 HANDOFF.md):

1. 처음엔 "TabM + 시간 변조 + trajectory voting head"였다.
2. 외부 검토에서 voting head가 retrieval의 실패 모드를 답습할 위험이 지적됐다.
3. Cai et al.(변조)와 TimeMCL(WTA) 분석으로 "head에 시간을 넣는 것"의 한계가 드러났다.
   - Cai: 출력단 시간 주입은 12.6% 효과
   - TimeMCL: WTA는 시간축이 아닌 출력 모드로 분화
4. 팀 토론에서 "head로는 아쉽다, 시간 분포 정보를 가진 메모리가 있어야 한다"는 직관이 나왔다.
5. 이 직관을 "시간 인덱싱 분포 메모리 + 검색"으로 구체화했다.
   head(변환)가 아니라 메모리(저장)+검색(조회)이 본질이었다.
6. 화려한 요소(WTA, 보정항, 외적 게이팅)는 토대 검증 후로 미루기로 했다.

핵심 교훈: **막연했던 이유는 아이디어가 부실해서가 아니라, 여러 논문의 조각(TabR 검색,
Cai 시간, TimeMCL centroid, 팀원 외적)이 한 구조로 안 맞춰져서였다.
"시간 인덱싱 메모리 + 검색"을 중심축으로 잡으니 조각들이 각자 자리를 찾았다.**
