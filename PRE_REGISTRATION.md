# PRE_REGISTRATION: Sanity Check 기준 사전 commit

> 이 문서는 **실험 시작 전**에 git에 commit하고, 동료 1명과 공유하여 입회 받습니다.
> 결과 본 후 기준 변경 금지. 모호한 결과는 안전한 길(B) 선택.

---

## 0. 메타 정보

- **작성일**: ______________
- **동료 검토자**: ______________
- **Git commit hash**: ______________
- **확정일**: ______________

---

## 1. 메인 가설 (재명시)

**H1**: TabM + Cai et al. NeurIPS 2025 modulation 베이스라인 위/옆에 **시간 인덱싱 프로토타입 메모리 + 검색**을 추가하면, 다음 둘 중 하나가 달성된다:

- **(a) 성능 청구**: Paired Wilcoxon signed-rank test에서 시간 인덱싱 메모리 > 고정 메모리가 통계 유의 (p < 0.05, BH 보정 후), **TabReD 8개 중 5개 이상 데이터셋**에서 성립

- **(b) 해석가능성 청구**: 성능은 동등 (no statistically significant degradation, 위 same test), **AND** 프로토타입 궤적이 실제 데이터 분포 변화와 통계적으로 유의한 매칭 (Pearson correlation r ≥ 0.4, p < 0.05)

(a) 또는 (b) 어느 하나만 만족해도 OK. 둘 다 만족 못 하면 **H1 기각, 진단 paper로 pivot (B)**.

> 구조: 시간 인덱싱 프로토타입 메모리 `P_k(t) = P_k^base + drift_k(Fourier(t))` + 단순 softmax 검색.
> **head가 아니라 백본-예측기 사이 메모리 레이어.** Phase 1은 최소 버전 (WTA 없음). 상세 EXPERIMENT_PLAN.md §3·§6.

---

## 2. Sanity Check 프로토콜 (Phase 1)

### 2.1 실험 설정

- **데이터셋**: 4개 (Sberbank Housing, Ecom Offers, Homesite Insurance, HomeCredit Default) — 회귀·이진분류 혼합, 크기 다양
- **시드**: 5
- **K (프로토타입 수)**: 1000 (메인 결과 5000보다 적음, 빠른 결정 목적)
- **백본**: TabM + Cai et al. NeurIPS 2025 modulation (고정)
- **검색**: 단순 softmax `w_k = softmax(-‖z - P_k(t_x)‖²/τ)` (WTA·보정항 없음 — 결정 3)
- **메모리 변형 (대조)**:
  - **Fixed memory**: P_k(t) = P_k^base (시간 무관 학습된 프로토타입)
  - **Time-indexed memory**: P_k(t) = P_k^base + drift_MLP_k(Fourier(t))
  - drift_MLP_k: 2-layer MLP, hidden dim 64, ReLU, input은 Fourier 시간 임베딩 출력

### 2.2 통계 분석

- **검정**: Paired Wilcoxon signed-rank test (같은 시드 페어)
- **다중 비교 보정**: Benjamini-Hochberg FDR (α=0.05)
- **효과 크기**: Hedges' g
- **단위**: 데이터셋 (시드 평균을 데이터셋 단위 점수로)

---

## 3. PASS/FAIL 기준 (사전 commit)

> Test 1·2·3이 **게이팅 테스트**(PASS/FAIL 결정). Test 4는 비-게이팅 진단(설계 정당화용).
> EXPERIMENT_PLAN.md §8과 동일 기준 — 두 문서 중 하나 수정 시 반드시 동기화.

### 3.1 Test 1: 시간 인덱싱 메모리 vs 고정 메모리 (성능)

**측정**: 각 데이터셋에서 5 시드 평균 점수
- 분류: ROC-AUC (↑ 좋음)
- 회귀: RMSE (↓ 좋음, 비교 시 부호 반전)

**PASS 조건**:
- Time-indexed > Fixed가 **4개 중 3개 데이터셋**에서 통계 유의 (paired Wilcoxon p < 0.05, BH 보정 후)
- AND Hedges' g ≥ 0.3 (small-to-medium effect)

**FAIL 조건**:
- 위 조건 미달
- 또는 Time-indexed < Fixed가 어느 데이터셋에서든 통계 유의 (음의 효과)

### 3.2 Test 2: 외삽 검증

**프로토콜**:
- 학습 데이터의 시간 70%까지만 학습
- 학습된 메모리 P_k(t)를 나머지 30% 시점에 외삽
- 동시에 그 30%의 실제 데이터 임베딩 평균 (또는 클러스터 중심)을 계산
- 두 값 비교

**측정**:
- Pearson correlation r 사이:
  - 외삽된 프로토타입 위치 (학습 데이터 안 본 시점)
  - 실제 그 시점 데이터의 임베딩 분포

**PASS 조건**:
- **분산 설명력 (R²) ≥ 30%** 이상이 4개 중 2개 데이터셋에서
- AND 추세 방향(예: 상승/하락)이 시각적으로 일치

**FAIL 조건**:
- R² < 30% 또는 부호가 무작위에 가까움
- → 메모리가 외삽이 아닌 학습 분포의 시간 보간만 하고 있다는 신호

### 3.3 Test 3: 검색 의미성 + 궤적 시각화 (해석가능성)

**측정 (두 부분)**:
- (검색) 입력들이 자기 시점 프로토타입에 실제로 가깝게 검색되는가 — 검색 가중치가
  소수 프로토타입에 의미 있게 집중되는지 (무작위 분산이 아닌지)
- (시각화) 학습된 P_k(t) curve를 PCA/UMAP 2D 투영, 4개 데이터셋 각각 그리기
- 동료와 본인이 독립 평가

**PASS 조건 (정성적, 둘 다 동의해야 함)**:
- 검색 가중치가 의미 있게 집중 (균일 분산 아님)
- 궤적이 **매끄럽다** (jagged하거나 random walk 같지 않음)
- 궤적이 **방향성을 가짐** (시작/끝이 일관된 방향)
- 가장 큰 이동을 보이는 프로토타입이 의미적으로 해석 가능

**FAIL 조건**:
- 검색 가중치가 균일 (검색이 무의미)
- 또는 궤적이 noise처럼 보임 / 한 점 근처에서 거의 안 움직임 (drift 학습 실패)

### 3.4 Test 4: 시간 주입 위치 비교 (비-게이팅 진단)

**목적**: "시간을 head/출력단에만 넣으면 부족한가"(준이 질문)를 데이터로 확인. PASS/FAIL 결정엔 미사용.

**측정**: 동일 데이터셋에서 세 변형 비교
- 메모리만 (시간이 P_k(t)에만)
- 입력만 (시간이 Backbone 입력에만, 메모리는 고정)
- 둘 다 (우리 본 제안)

**기록**: 세 변형의 성능 차이. "메모리만/둘 다"가 "입력만"보다 나으면 메모리 검색의 가치 입증.
(이 결과는 설계 정당화 및 논문 ablation에 사용.)

### 3.5 종합 판정

**PASS (→ Phase 2a, 메인 청구로 전진)**:
- Test 1 통과 AND Test 2 통과 AND Test 3 통과 (Test 4는 게이팅 아님)

**FAIL (→ Phase 2b, 진단 paper로 pivot)**:
- 게이팅 셋(1·2·3) 중 **하나라도** 미통과

**AMBIGUOUS 처리**:
- 모호한 경우 (예: p = 0.06, R² = 28%) → **FAIL 취급**
- 안전한 길 우선, 매몰비용 방지

---

## 4. 결정 과정 규칙

### 4.1 결과 검토 절차

1. 모든 sanity check 결과를 한 번에 본다 (한 테스트씩 보고 의사결정 X)
2. **동료 입회**: 본인 + 동료 1명이 함께 결과 표를 본다
3. 위 PASS/FAIL 기준에 기계적으로 매칭
4. 의견 다르면 본 문서의 기준 우선

### 4.2 변경 금지 사항

다음은 결과 본 후 절대 변경 금지:
- α 수준 (0.05)
- 데이터셋 수 (4) 또는 시드 수 (5)
- 통과 비율 (3/4 데이터셋)
- 효과 크기 임계 (Hedges' g ≥ 0.3)
- R² 임계 (≥ 30%)

### 4.3 변경 가능 사항 (사전에 명시)

다음은 결과 본 후 변경 가능, 단 사전 명시된 범위 내:
- 추가 ablation (위 셋 이외) — 결과 보강용, 메인 결정엔 미사용
- K 값 (1000 → 500 또는 2000) — 단, sanity 재실행 필요
- 데이터셋 추가 (4 → 6) — 단, 사전 명시된 4개의 기준은 유지

---

## 5. 시나리오별 대응

### 시나리오 A: 깔끔한 PASS (Test 1/2/3 모두 통과)
- Phase 2a 진행
- 8 데이터셋 × 20 시드로 풀 평가
- 해석가능성 case study 1-2개 데이터셋에서 작성

### 시나리오 B: Test 1만 통과, Test 2/3 실패
- **성능 청구 H1(a)는 가능, 해석가능성 H1(b)은 실패**
- → 성능 청구 단독 paper로 가능. 단 Cai et al.보다 향상이 작으면 가치 약함.
- Phase 2a로 가되, 해석가능성은 부수만.

### 시나리오 C: Test 2/3 통과, Test 1 실패
- **해석가능성 청구 가능, 성능 청구 실패**
- → 더 작은 페이퍼 (ICLR workshop, ICML position paper 등)
- "프로토타입 궤적 시각화는 의미 있지만 성능 향상 없음" 솔직 보고

### 시나리오 D: 모든 Test 실패
- Phase 2b로 pivot
- 진단 paper 또는 workshop short paper
- 메시지: "When does time-indexed retrieval help? Not always."

### 시나리오 E: Test 결과가 데이터셋마다 상반됨
- 매우 흥미로움 — "어느 데이터셋이 어느 메커니즘에 맞는지" 분석
- Phase 2b 진단 paper의 더 강한 버전

---

## 6. 동료 입회 서명

본인은 위 기준을 실험 시작 전에 검토했으며, 결과 본 후 기준을 자기 합리화로 변경하지 않을 것에 동의합니다.

- **실험자 서명**: ______________ 날짜: ______________
- **동료 입회 서명**: ______________ 날짜: ______________

---

## 7. 부록: 이 문서를 만든 이유

외부 검토에서 가장 큰 risk로 식별된 것이 **"결과를 본 후 PASS/FAIL 기준을 자기 합리화로 변경하는 함정"**. 이걸 막기 위해:

1. 기준을 실험 시작 전에 git commit
2. 동료 1명이 함께 결과 판정
3. 모호 = FAIL (안전한 길)
4. 변경 가능 사항을 사전 명시

이 절차가 있어야 어떤 결과가 나와도 논문에 "Our pre-registered criteria"라고 쓸 수 있고, reviewer가 신뢰함.
