# 리뷰 라운드 3 대응 기록 (2026-07-18)

> 입력 = 2차 외부 검증 보고서 (체크리스트 대조, 국문본 v4.2 대상; 판정 ✅44 ⚠️9 ❌1 ❓8).
> 반영 결과 = **v4.3** (EN md 정본 → KO → tex, tectonic 재컴파일 클린). 이 파일 = 항목별
> 판정·조치의 영구 기록. 라운드 1~2 = PAPER_DRAFT_V4 헤더·REVIEW_ROUND2_2026-07-18.md.

## 판정 요약

- **수용·즉시 반영 7건**: ❌ EMBER 수치 통일 / 초록 인증서 회계 / §1 패널 회계 완결 /
  §2 검출기 head-to-head 범위 문장 / §3.1 "해가 될 수 없다" 수위 완화 / §4.1 5-시드 사유 /
  Fig.2 색각 문구.
- **제출-시점 절차 3건**: 사전등록 제3자 증빙(OSF/서명 태그), concurrent 재스캔, 익명 사본
  커밋 메타데이터 점검(+선택적 Broader Impact Statement 여부 확인).
- **리버탈 큐 추가 2건**: 고전 검출기 회고 실행 1표, MLP 튜닝 재실행.
- **오해 해소 0건 / 기각 0건** — 이번 라운드는 전 지적이 유효했음.

## 항목별 처리

| # | 지적 | 판정 | 조치 |
|---|---|---|---|
| 1 | ❌ **EMBER §2↔§6 불일치** (−0.012/0.0014 vs −0.008/0.0013) | **수용 — 원인 규명** | §2의 수치는 대체된 전체-이력(126개월) 판독(RESULTS §29)이 잔존한 것. 아티팩트 재검증: 인증서급 2018-조밀 셀 = stale −0.0081 [−.0093,−.0069], den −0.0043, D 0.834, δ 0.0013, rec +0.0309 (optional 133244/002926 두 런 동일) → §6이 정본. **§2를 −0.008/0.0013으로 통일** (EN·KO·tex). 전체-이력 값은 §6 괄호("낮은 검정력에서 같은 null")가 무수치로 커버 |
| 2 | 초록 인증서 회계 (maps=식별가능 null, homesite=불안정이 "모든 null은 인증서" 문장 밖) | **수용** | 초록: "남은 셀은 모두 식별가능 null이거나 인증서 판정 — 검증됨·실명·거부·(한 셀) 불안정". §1 감사 문단에 "패널의 남은 두 셀 = 식별가능 null + 시드-불안정 인증서(주장 금지)" 추가 — 10-패널 회계 완결 |
| 3 | 고전 검출기(DDM/ADWIN) 비교 부재 | **부분 수용 + 큐** | §2에 명시적 범위 문장 추가(손실-스트림 검출기 = 유형-맹목이라 다른 질문; 동종 비교 = 카나리아 패널) + 리버탈 카드 #4 등록(회고 실행 1표 — 발화해도 유형-맹목 논지를 강화) |
| 4 | 사전등록 자기-타임스탬프 | **제출 절차** | NEXT_TAB 제출 체크리스트에 격상: 익명 미러에 커밋 이력 보존 + 서명 태그, 가능하면 OSF 등록. 본문 문구는 현행 유지("commit timestamps" — 허위 아님) |
| 5 | MLP 미튜닝 공정성 | **큐** | 리버탈 카드 #5: 표준 전처리+조기종료 MLP로 배터리+Electricity 재실행 → 카나리아 판정이 튜닝 아티팩트가 아님을 봉인 (§7 한계 (1)이 현행 방어) |
| 6 | 배터리 5시드 사유 미기재 | **수용** | §4.1에 한 줄: 심은 효과 = floor의 10–27×, 배터리는 추정이 아니라 pass/fail 관문 |
| 7 | §3.1 "해가 될 수 없다" 준-형식 서술 | **수용** | "해칠 기제가 남지 않는다(§4.2가 경계를 실측하는 기대이지 정리가 아님)"로 완화 — §2의 "정리 없음" 선언과 정합 |
| 8 | Fig.2 색각 접근성 | **수용** | 캡션에 "색은 중복 인코딩 — 모든 타일이 판정 텍스트를 인쇄" 명시 (설계상 이미 텍스트 이중화) |
| 9 | 드래프트 헤더 메모 노출 | **해당 없음(확인)** | md 헤더는 repo 내부 문서; 제출물(tex/PDF)에는 없음. 익명 사본 점검 항목에 포함 |
| 10 | LLM 공시 / Broader Impact | **확인 완료 / 절차** | TMLR author guide에 LLM-선언 요건 없음(2026-07-18 확인). Broader Impact Statement(선택)는 제출 시 포함 여부 결정 |

## 리버탈 실험 큐 (갱신; REVIEW_ROUND2의 1·2 ✅ 이후)

3. ⬜ FT-Transformer급 프로브 배터리 1차 (GPU, 2~3일) — 선택.
4. ⬜ 고전 검출기 회고 실행 1표 (ADWIN/DDM류를 10셀 손실-스트림에; river 패키지에 검출기
   있음 — 유형-맹목 실증 표). CPU 반나절.
5. ⬜ MLP 튜닝 재실행 (StandardScaler+early_stopping=True, 배터리 5컨트롤 + Electricity).
   CPU 수 시간. §4.3 각주 1줄로 반영 예정.
