# Data & results (large, machine-specific) — root-anchored so src/data/ is NOT ignored
/data/
/results/
/logs/
# stray result artifacts if a run wrote to repo root (no inline comments in .gitignore!)
/grid_report.json
diagnostics.jsonl
*.parquet
*.csv
*.npy
*.npz
*.pt
*.ckpt
checkpoints/
wandb/
catboost_info/

# External baseline repos (clone separately per SETUP.md)
external/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/
env/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Optuna / experiment artifacts
*.db
optuna_studies/
