# PREREG (확장) — ACSPublicCoverage 자연 실험: 문서화된 규칙 변경 위의 계기 검정

**작성 2026-07-31, 실행 전.** 승인 시 `PREREG_DEPLOYMENT_V2.md` §17로 편입한다.
분리 파일로 쓰는 이유: 이것은 §14/§16의 리버탈 실험 계열이 아니라 **신규 데이터셋 확장**이므로,
승인 전에 다른 실험의 사전등록 본문에 섞지 않는다. 규율(예측을 결과 확인 전 커밋)은 동일하게
적용되며, 이 파일의 git 커밋 타임스탬프가 그 역할을 한다.

> **캐스케이드·임계값·시드 프로토콜은 §2~§3과 동일하게 불변.** 이 문서는 대상·예측·판독만 고정한다.

---

## 1. 왜 이 데이터셋인가 — 논문의 가장 큰 구멍을 정면으로 겨냥한다

`PAPER_DRAFT_V4.md` §7 한계 3:

> *"유일한 견고 양성은 실험실에서 설계된 drift다. 크기를 가늠할 **자연 발생 산업 양성을 찾지
> 못했다** — 그것이 이 지도의 발견이자 동시에 약점이다."*

지금까지 실행된 어떤 실험도 이 한계를 건드리지 못한다. `[C]` full-span은 가능성이 있지만
사전 예측이 "판정 유지"이고, `[D]` 패밀리 스윕은 **기제상 불가능**하다(§16.2 — 심어진 규칙은
자연 발생이 아니다).

**ACSPublicCoverage는 다르다.** ACA 메디케이드 확대는 *동일한 x*(소득·연령·장애·부양·주)에 대해
*y*(공적보험 가입 자격/여부)를 바꾼, **외부 문서로 날짜가 확정된 조건부 분포 변경**이다.
그리고 결정적으로 **주(state)마다 채택 여부와 시점이 다르다** — 즉 처치군과 대조군이
같은 과제·같은 연도·같은 계기 안에 동시에 존재한다.

**이 논문이 지금까지 한 번도 갖지 못한 것: 정답을 아는 실데이터 양성.**

## 2. 설계 — 확대 시점이 창(2014–2018) 안 / 밖 / 없음

| 군 | 주 | 확대 시행일 | 관측창 내 규칙 변경 | 예상 n/년 (PUMS 1%, 필터 후) |
|---|---|---|---|---|
| **처치 (주)** | **PA** | 2015-01-01 | ✅ 2014만 pre, 이후 전부 post — **가장 깨끗** | ~50k |
| 처치 | IN | 2015-02-01 | ✅ 동일 구조 | ~27k |
| 처치 | LA | 2016-07-01 | ✅ 단 2016은 혼재 | ~19k |
| 처치 (보조) | MT | 2016-01-01 | ✅ | ~4k |
| **대조 (무변경)** | **TX** | 미채택 (2018까지) | ❌ | ~115k |
| 대조 | FL | 미채택 | ❌ | ~86k |
| 대조 | GA | 미채택 | ❌ | ~42k |
| **사전 정합성 (기실행)** | CA | 2014-01-01 (**창 시작 전**) | ❌ 창 안에서는 변경 없음 | ~150k |

**★ 기존 아티팩트가 이미 이 설계의 사전 정합성을 지지한다.** CA는 확대가 관측창 *이전*에
끝났으므로 창 안에서는 규칙 변경이 없어야 하고, 실제로
`prereg_results/optional/summary_20260715T135651_5b85cb0.json`이
**NO-STRONG-CONCEPT** (raw −0.0076, den −0.0074, D 0.515/shuffle 0.484, delta 0.00078)를 읽었다.
이건 사후 해석이 아니라 **이 설계가 예측했을 것을 이미 관측한 것**이다.

**식별가능성이 유리하다.** CA의 D=0.515는 지도 전체에서 가장 낮은 축에 속한다 — ACS는
D-게이트에 걸리지 않는 **식별가능 영역**이다. 따라서 여기서의 null은 "못 봤다"가 아니라
"봤는데 없었다"로 직접 읽힌다. 산업 패널의 4개 공허 셀과 정반대 조건이다.

**윈도우 구조** (`--by-value`, K=5):
`_per_seed` 기준 old 앵커 = 2014, 미래 윈도우 = {2016, 2017, 2018}, recent = {2015, 2016, 2017}.
PA/IN은 old(2014)만 pre이고 미래·recent가 전부 post → **old 라벨이 현행 규칙과 모순** = 정확히
계기가 재도록 설계된 상황.

## 3. 실행 전 **필수** 확인 (이것이 틀리면 설계 전체가 무너진다)

- [ ] **확대 시행일 원출처 대조** — 위 표의 날짜를 KFF Medicaid Expansion Tracker(또는 CMS 공식
      기록)로 전수 확인하고 인용을 문서에 남긴다. 본 문서의 날짜는 **검증 대상이지 근거가 아니다.**
- [ ] **WI 제외 확정** — 위스콘신은 ACA 확대가 아닌 별도 waiver 구조라 처치·대조 어느 쪽으로도
      분류하지 않는다.
- [ ] **2019+ 미포함 유지** — RELP→RELSHIPP 개명(2019)과 COVID 대체 가중치(2020)
      (`scripts/prep_folktables.py:6-9`). VA/ME(2019 확대)는 이번 범위 밖.
- [ ] **folktables `ACSPublicCoverage` 필터 확인** — 표준 정의(age<65 ∧ PINCP≤30000)가
      확대 대상 인구와 정합하는지, 그리고 `ST`가 피처에 포함되는지(단일 주 실행에서는 상수라
      `_nonconstant_mask`가 제거).

## 4. 구현 — 코드 델타 6줄

`scripts/prep_folktables.py`는 현재 `ACSIncome`만 하드코딩한다(usage에 `[task=income]`이 있으나
무시됨). 필요한 변경:

```python
TASKS = {"income": "ACSIncome", "pubcov": "ACSPublicCoverage",
         "employment": "ACSEmployment", "mobility": "ACSMobility"}
...
task = sys.argv[3] if len(sys.argv) > 3 else "income"
import folktables
problem = getattr(folktables, TASKS[task])
... problem.df_to_pandas(raw)
```

계기(`run_deployment_decay.py`)는 **일절 건드리지 않는다** — 데이터 재료화 스크립트만 바뀌므로
PREREG §4 배터리 게이트를 다시 통과할 필요가 없다.

```bash
for S in PA IN LA MT TX FL GA; do
  python scripts/prep_folktables.py $S acs_pubcov_$S.parquet pubcov
  python scripts/run_deployment_decay.py --csv acs_pubcov_$S.parquet \
    --target label --time YEAR --by-value --n-seeds 10 --name acs_pubcov_$S
done
```

CPU만 사용, 주당 수십 분 수준(기존 CA 실행 기준). Kaggle 인증 불필요(census 공개 데이터).

## 5. 사전 예측 (결과 확인 전 커밋)

**기제 예측**: 확대는 소득·부양아동 유무·장애 축에서 자격 경계를 이동시킨다. old(2014) 데이터는
옛 경계를 가르치므로 post 윈도우에서 해악 → `staleness_harm > 0`. 노이즈 변화가 아니므로
`denoised ≥ raw`여야 한다(insects 서명).

| 셀 | 최빈 예측 | 확률 | 크기 예측 |
|---|---|---|---|
| **acs_pubcov_PA** | **DEPLOYMENT-CONCEPT** | **40%** (SUBFLOOR 양성 30 / null 25 / 기타 5) | den +0.02 ~ +0.06 |
| acs_pubcov_IN | DEPLOYMENT-CONCEPT | 35% | den +0.02 ~ +0.05 |
| acs_pubcov_LA | SUBFLOOR 또는 CONCEPT | CONCEPT 30 / SUBFLOOR 35 | 2016 혼재로 희석 |
| acs_pubcov_MT | 판정 불가 또는 null | CONCEPT 20 | n 작음(~4k/윈도우) |
| **acs_pubcov_TX** *(대조)* | **NO-STRONG-CONCEPT / DECAY-COVARIATE** | **75%** | den ≤ 0 |
| acs_pubcov_FL *(대조)* | 동일 | 75% | den ≤ 0 |
| acs_pubcov_GA *(대조)* | 동일 | 70% | den ≤ 0 |

**집계 예측**: 처치 3주(PA/IN/LA) 중 **적어도 하나가 CONCEPT 발화할 확률 ≈ 65%**,
**대조 3주가 모두 null일 확률 ≈ 60%**, **둘 다 성립(깨끗한 자연 실험)할 확률 ≈ 40%**.

**왜 100%가 아닌가 (예측을 낮추는 세 가지, 미리 적는다)**
1. **AUC는 prior-invariant다.** 확대가 순위를 바꾸지 않고 base rate만 올리면 AUC가 안 움직인다.
   신규 자격자가 모델이 이미 "가입 가능성 높음"으로 순위 매기던 집단이면 규칙 변경이 지표에
   안 잡힌다. **이것이 가장 큰 단일 위험이다.**
2. **희석**: 공적보험(PUBCOV)에는 메디케어·VA·군보험이 섞이고 확대는 메디케이드만 건드린다.
3. **가입 지연**: 실제 등록은 수년에 걸쳐 점증한다 → 급격이 아니라 **단조 drift**. 다만 계기의
   감도 프로파일(§10)상 단조 앵커는 3/3 발화했으므로 이건 유리한 쪽이다.

## 6. 판독 규정 (양방향, 결과 확인 전 커밋)

**(a) 처치 발화 ∧ 대조 null** → **§7 한계 3 종결.** 이 프로젝트가 처음으로 *자연 발생·외부
문서로 확정된* 실데이터 규칙 변경 양성을 얻는다. 논문 성격이 전환된다:
"산업 데이터에 drift가 없다"에서 **"계기가 실제 정책 규칙 변경을 검출하고, 산업 벤치마크에서는
그런 것이 없다"**로. 확증 시드 100–109 통과 필수. 초록·§5·§6·§7 갱신.

**(b) 처치 null ∧ 대조 null → 이 실험은 계기를 반증할 수 있다. 반드시 그렇게 보고한다.**
문서화된·대규모·실재하는 규칙 변경을 식별가능 영역(D≈0.5)에서 놓쳤다면, 그것은
"ACS에 drift가 없다"가 아니라 **"이 계기는 이 크기의 실제 규칙 변경을 못 본다"**이다.
그 경우 지도의 모든 null이 약해지며, §7에 **신규 한계**로 명시하고 δ와 함께 검출 하한을 보고한다.
**이 분기를 사후에 "ACS 특성 탓"으로 재해석하지 않는다.**

**(c) 대조 발화** → 설계 또는 계기의 문제. 처치 결과를 어느 방향으로도 주장하지 않는다.
우선 진단: 2014년 전 주 공통 변화(개인의무가입·마켓플레이스 보조금)가 대조에도 규칙 변경을
만들었을 가능성 — 그렇다면 "대조"가 아니므로 설계를 재규정하고 재사전등록한다.

**(d) 처치가 SUBFLOOR(0 < den CI하한 ≤ floor)** → §9의 무증거 대역 규칙을 **그대로** 적용한다.
"약한 증거"로 격상하지 않는다. 단 δ와 함께 보고하고, floor 0.02가 이 크기의 실제 정책 변경보다
크다면 그것 자체가 **floor 캘리브레이션에 대한 실측 비판**이므로 §4.2/부록 C에 기록한다.

**(e) NOISE-DRIFT-CONFOUNDED 발화** → ACS 응답 품질의 시간 변화 가능성. 기제 라벨대로 보고하고
concept으로 읽지 않는다(§3.3).

## 7. 이 실험의 비대칭성 (사전 명시)

지금까지의 모든 실험은 **라벨을 조정**할 수 있을 뿐이었다. 이 실험은 다르다:

> **정답이 데이터 밖에 있고, 계기가 그것을 맞히거나 틀릴 수 있다.**

성공하면 한계 3이 닫히고 계기에 외부 타당성이 생긴다. 실패하면 계기의 검출 하한이 실측되고
지도의 null들이 약해진다. **양쪽 다 지금 상태보다 정보량이 크며, 그래서 실행할 가치가 있다.**
동시에 이것이 이 프로젝트에서 **가장 위험한 실험**이므로, 위 (b) 분기를 결과 확인 전에
못 박아 둔다.

## 8. 위험·한계 (사전 기재)

- **연 단위 5윈도우**는 거칠다. 계기의 K=10 기본과 다르며 미래 윈도우가 3개뿐 → CI가 넓다.
- **PUMS는 1% 표본**이고 가중치를 쓰지 않는다(기존 CA 실행과 동일). 모집단 추정이 아니라
  조건부 규칙 변화 검출이 목적이므로 정합하나, 대표성 주장은 하지 않는다.
- **주 단위 실행**이므로 `ST`가 상수 → 제거됨. 다주 pooled 실행은 별도 설계이며 이번 범위 밖.
- **`ACSPublicCoverage`는 WhyShift가 다루는 과제**이므로, 결과는 부록 B.2의 두-렌즈 대조에
  자연스럽게 붙는다(추가 이득).
- 처치·대조 배정은 **주의 정책 결정**이지 무작위가 아니다. 인과 추정을 주장하지 않는다 —
  주장하는 것은 "문서화된 규칙 변경이 있는 곳에서 계기가 발화하는가"라는 **계기 검정**이다.

## 9. 실행 순서

1. §3 체크리스트 4항 전수 확인 (확대 날짜 원출처 인용 포함)
2. `prep_folktables.py` 6줄 확장 + PA 1개 주로 스모크(파일 생성·연도·pos_rate 확인)
3. **PA + TX** 2셀 먼저 실행 (처치 최선 + 대조 최선). **여기서 멈춰서 본다.**
   - PA 발화 ∧ TX null → 나머지 5주 실행, 확증 시드 준비
   - 둘 다 null → §6(b) 발동. 나머지 주를 돌리기 전에 검출 하한 δ부터 보고
   - TX 발화 → §6(c) 발동. 중단하고 설계 재검토
4. 판정이 나온 셀 전부 확증 시드 100–109
5. 결과를 이 문서 아래에 **읽기 전용 절**로 추가 (기존 PREREG 관행)

---

**서명 조항**: 위 §5 예측과 §6 판독 규정은 데이터를 내려받기 전에 커밋된다. 실행 후 이 문서의
§1–§9는 수정하지 않으며, 결과는 §10 이하 신규 절로만 추가한다.

---

## 10. 결과 판독 (2026-08-01 실행, 커밋 f373aa2) — **§6(b) 반증 분기 발동**

아티팩트: `results/phase1/deployment_decay/summary_20260801T16{3629,3904}_f1d0630.json`,
`logs/acs_PA.log`, `logs/acs_TX.log`, `logs/acsprep_{PA,TX}.log`.
환경 py 3.11.15 / sklearn 1.9.0 / numpy 2.4.6. §3 체크리스트 이행: PA 시행일 **2015-01-01**을
KFF·MACPAC 원출처로 확인(Healthy PA 1115 웨이버, CMS 승인 2014-08-28), TX·FL·GA·TN 2018년까지
미확대 확인. WI 제외, 2019+ 미포함 유지.

### 10.1 설계가 데이터에 잡혔다 (서술)

`prep_folktables.py`가 방출한 연도별 양성률:

| | 2014 | 2015 | 2016 | 2017 | 2018 |
|---|---|---|---|---|---|
| **PA** (2015-01-01 확대) | 0.234 | **0.268** | 0.293 | 0.305 | 0.306 |
| **TX** (미확대) | 0.184 | 0.183 | 0.183 | 0.185 | 0.185 |

PA는 시행 연도에 +3.4pp 계단이 서고 가입 지연 램프가 붙어 2017–2018에 평탄해진다(총 +7.2pp).
TX는 전 구간 진폭 0.2pp. **처치와 대조가 의도한 대로 갈렸고, 사건이 데이터에 들어 있다.**
단 이는 서술이며 판정은 계기가 한다(§4 명시).

### 10.2 판정 — 처치·대조 모두 null

| | PA (처치) | TX (대조) |
|---|---|---|
| **판정** | **NO-STRONG-CONCEPT** | **NO-STRONG-CONCEPT** |
| staleness | −0.0107 [−.0115, −.0100] | −0.0155 [−.0173, −.0136] |
| denoised | −0.0088 [−.0096, −.0081] | −0.0108 [−.0126, −.0090] |
| recency_gain | **+0.0092 [+.0075, +.0108]** | −0.0015 [−.0040, +.0010] |
| decay | +0.0337 | +0.0214 |
| D_strip | **0.534** | 0.523 |
| δ (검출 하한) | **0.00083** | 0.00200 |
| n / 윈도우 | 76,717 / 5 | 62,753 / 5 |
| arm 절대 AUC | old 0.8028 → recent 0.8120 | old 0.8061 → recent 0.8046 |

**§6(b)가 발동한다.** 규정된 대로 보고한다:

> 문서화된·대규모·실재하는 규칙 변경을 **식별가능 영역(D≈0.53)**에서 놓쳤다. 이것은
> "ACS에 drift가 없다"가 아니라 **"이 계기는 이 크기의 실제 규칙 변경을 못 본다"**이다.

피할 구실이 없다는 점을 명시한다. D≈0.53은 산업 패널의 공허 셀들과 **정반대**로 완전 식별가능
영역이고, δ=0.00083은 지도 전체에서 가장 타이트한 null이다. **저전력이 아니라 자신 있게 0이다.**
그래서 이 미검출은 더 날카롭다.

### 10.3 계기가 본 것 (판정은 바뀌지 않는다)

사전등록된 네 arm 중 **recency에서 처치/대조가 갈렸다**: PA +0.0092(CI가 0 위) vs
TX −0.0015(0 포함). arm 절대값이 뒷받침한다(PA old 0.8028 → recent 0.8120; TX 0.8061 → 0.8046).
방향은 규칙 변경과 정합한다.

그러나 **+0.0092는 floor 0.02 미달이라 `rec_present`가 False**이고 캐스케이드는
NO-STRONG-CONCEPT로 간다. **판정은 바뀌지 않으며, 이 기록은 §6(b)의 결론을 완화하지 않는다.**
관측 보고이지 재해석이 아님을 명시한다.

### 10.4 기제 — §5에 사전등록한 위험 1번이 실현됐다

§5의 "예측을 낮추는 세 가지" 중 1번:

> **AUC는 prior-invariant다.** 확대가 순위를 바꾸지 않고 base rate만 올리면 AUC가 안 움직인다.
> **이것이 가장 큰 단일 위험이다.**

데이터가 확인한다: 양성률은 0.234 → 0.306(**+7.2pp**)로 크게 올랐으나 **모든 arm의 절대 AUC는
0.80~0.82에 머문다.** 메디케이드 확대는 자격 *문턱*을 옮겼지 *순위*를 바꾸지 않았고,
순위 기반 지표는 구조적으로 그것을 보지 못한다. **사후 설명이 아니라 실행 전에 적어둔 위험이
그대로 실현된 것이다.**

### 10.5 논문에 대한 귀결 (신규 한계)

논문의 estimand는 "**착취 가능한** mean-rule drift"이고 지표는 AUC(이진)다. 따라서:

- 배포 시스템이 **순위 기반**이면(우선순위·트리아지) 계기 판정은 **옳다** — 이 규칙 변경은
  실제로 착취 불가다.
- 배포 시스템이 **고정 문턱 또는 보정 확률**을 쓰면(자격 심사·보험 — ACSPublicCoverage가 정확히
  그 과제다) 이 변경은 결정적인데 **계기는 눈이 먼다.**

§3.1이 AUC를 고른 근거는 prior-invariance였다(prior shift에 오발하지 않기 위해). **이 실험은 그
선택의 대가를 실데이터·외부 정답으로 실측했다: 보정을 바꾸는 규칙 변경도 함께 못 본다.**

→ §7에 신규 한계로 추가하고, 지도의 모든 null에 **"순위 기반 지표에서"** 한정을 붙일 것.
§7 한계 3(자연 발생 산업 양성 부재)은 **해소되지 않았다.**

### 10.6 후속 실험 (예측을 먼저 커밋)

가설이 맞다면 **문턱 민감 지표로 바꾸면 PA가 발화하고 TX는 안 해야** 한다. 그것이 이 blindness가
지표에서 왔고 수리 가능함을 보이는 결정적 대조다.

| | PA | TX |
|---|---|---|
| Brier / log-loss 기반 staleness | **양수, floor 초과** | 0 근처 |
| **확률** | **55%** | **75%** |

45%로 틀릴 수 있다고 보는 이유: 문턱 이동이 옛 모델의 보정을 망가뜨리지만 2N 학습으로 보정이
부분 회복될 수 있다.

**범위 제한(사전 고정)**: 이는 지표 변경이므로 estimand와 floor 캘리브레이션이 함께 바뀐다.
**진단용 부록으로 한정하고 지도 판정에는 쓰지 않는다.** 실행 전 `_fit_score` 지표 분기 추가와
PREREG §4 배터리 재게이트가 선행되어야 한다.

### 10.7 미실행 항목

§2 표의 나머지 처치주(IN 2015-02-01, LA 2016-07-01, MT 2016-01-01, AK 2015-09-01)와 대조주
(FL, GA)는 **돌리지 않았다.** §9-3의 정지 지점 규정("PA+TX를 읽고 판단")에 따라, PA가 null인
상태에서 같은 지표로 다른 주를 추가하는 것은 같은 blindness를 반복 측정하는 것이므로 보류한다.
이들의 시행일은 §3 체크리스트대로 **사용 전 원출처 대조가 필요한 상태로 남아 있다**(본 문서의
날짜는 검증 대상이지 근거가 아니다).
