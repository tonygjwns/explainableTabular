> **V5 초안(한국어 미러), 2026-08-03 조립.** EN 정본 = `PAPER_DRAFT_V5.md`. §1·§3·§6·§7·§8·§9는
> V5용으로 새로 썼고, §2·§4·§5·부록은 V4 한국어 본문을 참조 번호만 기계 변환해 옮겼다(어느 것이
> 어디서 왔는지는 `PAPER_DRAFT_V5_SECTIONS.md`). 재측정한 수치는 없다. V4 3파일은 그대로 살아 있다.

# 라벨-노이즈 감쇠가 concept drift를 주조한다: 손실 기반 drift 귀속의 오탐 채널, 그리고 인증된 계기의 모습

## 초록 (Abstract)

drift 모니터는 무언가 움직였다가 아니라 무엇이 움직였는지를 말해야 한다. 규칙 P(y|x)가 변했다면
최근 데이터로 재학습하는 것이 답이고, 규칙이 고정된 채 공변량만 움직였다면 그것은 오답이기
때문이다. 이 귀속이 기대는 손실 기반 비교에는 우리가 문헌에서 찾지 못한 오탐 채널이 있다. 규칙을
증명적으로 고정하고 라벨 노이즈만 시간에 따라 감쇠시키면 그 비교는 concept-drift 판정을 낸다.
구성된 null에서 +0.021이고, 이는 판정 floor 0.02 위이며, 우리 자신의 과거 계기가 보고했고 우리가
믿었던 산업 양성 +0.024와 0.003 이내다. 우리 수리를 겨냥해 적대적으로 구성한 두 채널도 고정된
규칙 아래서 같은 방식으로 발화한다.

그다음 계기를 수리한다. old 라벨을 그 윈도우 내부의 교차적합 예측으로 대체하고, 윈도우별 게이트가
높은 라벨 노이즈를 표지하며, 유효성 envelope은 수리된 팔 자신이 null에서 floor를 넘는 노이즈
수준까지 실측한다. 14-셀 사전등록 배터리가 이 수리를 검증하는데, 여기에는 규칙 변화와 노이즈
감쇠가 공존하며 그래도 발화해야 하는 셀이 들어 있다. 발화하며, 청정 검정력의 58%를 유지한다.
판정은 가정이 아니라 분리도·주입·학습가능성 세 인증서로 획득한다.

그렇게 수리된 계기는 실데이터에서 자기 자신의 과거 오탐의 기제를 진단한다. 같은 윈도우에서 규칙
변화를 볼 수 있었다는 검정력이 인증된 채로다. 우리는 이 계기가 어디서 눈이 머는지도 네 축으로
실측하며, 마지막 축은 바깥에서 정해진다. 법으로 문서화된 규칙 변경인 ACA 메디케이드 확대에 대해
계기는 완전 식별가능 영역에서, 이 논문 최소의 검출 하한에서 null을 읽었다. 지표가 순위 기반이라
자격 임계값이 아무의 순서도 바꾸지 않은 채 많은 질량을 판정 경계 너머로 옮기기 때문이다. 산업
데이터셋 8개(TabReD)에 신선한 시드 재현을 붙여 적용하면 판정 10개 중 10개가 그대로이고, 배포된
트리-앙상블 클래스에 대해 데이터셋별 검출 하한 위의 착취 가능한 mean-rule drift는 없다. 감사
0/8이고, 정보를 주는 판독을 내는 셀 기준 0/5다. 유형을 귀속하는 프레임을 같은 배터리에
head-to-head로 돌리면 두 기제를 크기로는 갈라내되 부호로는 갈라내지 못하므로, 임계값 판독은
수리가 다른 두 사건을 하나로 파일링한다. 계기·배터리·감사 기록을 공개하며, 식별가능성 인증서
없는 drift-유형 귀속은, 그것이 현재의 기본값인데, 신뢰할 수 없다고 주장한다.

---

## 1. 서론

배포된 표 모델을 지켜보던 팀이 최근 손실이 오르는 걸 보고 선택해야 한다. 오래된 행을 낡은 것으로
버리고 최근 데이터로만 재학습할 것인가. 라벨링 규칙이 변했다면 이게 맞다. 아니면 규칙은 멀쩡하고
오래된 행도 여전히 옳은 라벨을 갖고 있으니 전부 유지할 것인가. 공변량만 움직였다면 이게 맞다.
잘못 고르는 비용은 양방향 모두 비싸고, 고르는 일이 바로 drift-유형 귀속이 존재하는 이유다.

그 귀속이 보통 기대는 비교는 확인 없이 믿어질 만큼 단순하다: 최근 데이터로 하나, 최근+old로 하나
적합해서 미래 윈도우에서 격차를 읽는다. old 행이 이제 오도한다면 그것을 더하는 게 해롭다. 우리는
이 양을 *staleness harm*이라 부른다. 이것은 중요도 가중 분해가 그렇지 못한 방식으로
배포-native하다. 최근 부분이 두 팔에서 동일하므로 공변량 커버리지가 구성상 상쇄되고, 밀도비도
support 중첩도 필요 없다. 산업 피처 공간에서 정확히 그것들이 무너지기 때문이다.

**이 비교에는 오탐 채널이 있다.** 옳은 라벨이 해가 될 리 없다는 그 비형식적 정당화는 유한 용량 경험위험최소화에서 거짓이다. 규칙을 증명적으로 고정한 채 라벨의 *노이즈*만 시간에 따라
감쇠시키면 비교는 발화한다: 비가중 ERM은 이분산 하에서 효율적이지 않으므로 노이즈가 큰 old 행은
그것을 적합하는 모든 모델의 손실을 부풀리고, 그 행을 제거하면 규칙과 아무 상관 없는 이유로 최근
윈도우 손실이 개선된다. 구성된 null에서 이 값은 +0.021이다. 이 문헌의 임계값들이 사는 0.02 floor 위이고, 우리 자신의
v2 계기가 보고했고 우리가 믿었던 산업 양성 +0.024와 0.003 이내다. 그
수치를 받아들인 모니터는 최근 데이터로 재학습하며 옳은 라벨을 버리고, 소유자에게는 규칙이 변했다고
말할 것이다. 우리 수리를 겨냥해 구성한 두 채널이 같은 방식으로 발화한다: 스케일이 x에 의존하며
감쇠하는 노이즈(+0.025), 그리고 규칙을 나르는 피처와 상관된 노이즈(+0.026, denoiser 최악 케이스).
노이즈가 *증가*하면 −0.023이므로, 이는 상수 편향이 아니라 노이즈 궤적의 방향성 인공물이다.

**순진한 계기가 거짓말하는 방식이 두 가지 더 있다.** 각각 §4.3에서 수리하고 §6.1에서 지도로
만든다. "여기서의 null은 정보가 없다"의 자연스러운 인증서는 윈도우 분리도 AUC인데, 분류기가 old
행과 미래 행을 구별할 수 있는지를 묻는 통계량이다. 행 단위로 계산하면 완전 중복·근접 중복·공변량
이동이 전혀 없는 엔티티 코호트에서 0.994–1.000으로 포화한다. 분류기가 분포가 아니라 행을 외우는 것이다. 실데이터
10개 중 8개가 순진한 게이트에서 정확히 1.000을 읽는다. 그리고 concept/covariate *분리*는 가설
클래스 상대적이다: kNN 프로브는 순수 공변량 이동을 concept drift로 읽고(+0.098), 2층 MLP는 고정
규칙 하의 prior shift를 concept drift로 읽는다(denoised +0.042). 트리 앙상블은 전 컨트롤을
통과한다. 이는 Shimodaira의 misspecification 결과를 경험적으로 만든 것이고, "old 데이터가
해친다"가 기하만으로 제조될 수 있다는 뜻이다.

**수리, 그리고 그것이 요구하는 규율.** 각 실패에는 실행으로 검증된 수리가 붙는다(§4–§5). denoised staleness 팔은 old 라벨을 그 윈도우
내부의 교차적합 예측으로 대체한다. 노이즈-단독 drift에서 pseudo-label은 근사적으로 옳으므로 해가
사라지고, 규칙이 변했다면 그것들이 여전히 old 규칙을 인코딩하므로 해가 남는다. 윈도우별 노이즈
게이트는 교락을 표지하고, 그 유효성 envelope을 가정하지 않고 실측한다. denoiser 자신이 null에서
판정 floor를 넘는 노이즈비까지 재고, 그 위에서 계기는 기권한다. group-aware 분리도는 정직한
drift를 남기면서 암기 채널만 깎는다. 그리고 학습가능성-게이트 주입 컨트롤은 "이 데이터셋은 측정
불가"를 가정에서 실증으로 바꾼다. 모든 판정은 트리-앙상블 클래스로 범위가 매겨지고,
선형·kNN 프로브는 그 오발을 숨기지 않고 보고하는 카나리아로 함께 실린다.

**계기를 자기 자신에게 겨눴다.** 과거 양성을 낸 데이터셋으로 되돌리면, 수리된 계기는 v2 raw
판독을 비트 단위로 재현하고, old 윈도우 라벨 노이즈를 최근 중앙값의 2.1–2.9×로 측정하며, 모든
윈도우 해상도에서 유의하게 음수인 denoised 팔을 낸다: 노이즈를 걷어내면 old 행이 *돕는다*.
같은 윈도우가 기준 강도로 심은 규칙을 회복하므로(+0.086, 윈도우 내 R² 0.93 학습가능), 이 음성은
논증이 아니라 인증된다. drift-귀속 계기가 자기 과거 오탐을 단순히 재현하지 못한 게 아니라 그
*기제*를 실데이터에서 진단한 선행 사례를 우리는 찾지 못했다.

**그리고 그 맹점을, 실측했다.** null은 자기 맹점만큼의 값어치이므로 우리 것을 네 축으로 지도화한다
(§6): 프로브 클래스, 분리도, 규칙 패밀리, 지표. 마지막은 바깥에서 정해진다. ACA 메디케이드 확대는 펜실베이니아가 2015-01-01에 시행했고 텍사스는
채택하지 않은, 법으로 문서화된 규칙 변경이며 원자료의 양성률에서 보인다(0.234 → 0.306 대 평평한
0.183–0.185). 계기는 두 주 모두 null로 읽었다. 완전 식별가능 영역에서, 이 논문 어디보다 타이트한
검출 하한에서다. 검정력 부족이 아니라 확신에 찬 0이다. 기제는 실행 전에 등록돼 있었다: 우리의 이진 지표는 AUC이고 순위 기반이라, 자격
임계값은 아무의 순서도 바꾸지 않은 채 많은 질량을 판정 경계 너머로 옮길 수 있다. proper score로
재면 같은 윈도우가 처치주에서 양으로 돌아서고(log-loss +0.021) 대조주는 +0.006이다. 맹점이
국소화되고 수리 방향이 생기지만, 그 판독이 허락하지 않는 세 가지를 §6.4가 함께 적는다.

**적용.** 계기가 인증되고 맹점이 지도화된 뒤, 그것을 만들게 만든 질문에 쓴다(§7): TabReD 8개
데이터셋·Electricity·INSECTS 스트림의 사전등록 감사, 확증 시드 재현 포함(10/10 안정). 결과는
검출 표가 아니라 식별가능성 지도다. 감사 0/8이고 정보를 주는 판독을 내는 셀 기준 0/5이며,
유일한 견고한 양성은 설계된 drift이고, 두 셀은 증거로 세어지는 대신 인증서를 거부당하며, 한 셀은
이미 §3.2에서 진단됐다. 앵커 스트림이 감도 프로파일을 고정한다: 단조·단발 규칙 전환은 9/9 발화하고,
재발 레짐은 올바르게 침묵하며 일방향 drift에서는 불가능한 음의 recency 지문으로 표지된다.

**기여.**

1. *손실 기반 drift 귀속의 오탐 채널*(§3), 논증이 아니라 실행: 증명적으로 고정된 규칙 하의
   라벨-노이즈 감쇠가 실제 경계선 양성과 맞먹는 크기로 concept 판정을 주조하며, 적대적 구성으로
   찾은 두 채널이 더 있고, 유형-귀속 프레임과의 head-to-head가 그 프레임이 두 기제를 크기로는
   갈라내되 부호로는 못 갈라냄을 보인다.
2. *수리된, 기권하는 계기*(§4–§5): 노이즈 게이트와 실측 유효성 envelope을 갖춘 denoised
   staleness, group-aware 분리도, 학습가능성-게이트 주입 인증서, 카나리아를 동반한 클래스-범위
   판정. 규칙 변화와 노이즈 감쇠가 공존하는 적대적 조합을 포함한 14-셀 사전등록 배터리로 검증했다
   (청정 검정력의 58%로 발화).
3. *계기 맹점의 실측 지도*(§6), 그중 하나는 외부 정답으로 정해진다: 클래스 상대성(선형 모니터가
   표준 Electricity 데이터셋을 같은 바이트에서 concept drift로 뒤집는다), 분리도 게이트 안쪽의
   연속적 검정력 붕괴(ρ = −0.47), 패밀리 상대성(R² 0.560으로 학습가능한 부분모집단-국소 규칙이
   −0.050으로 미회복), 지표 상대성(ACS 반증).
4. *산업 표 ML의 사전등록 식별가능성 지도*(§7): 감사 0/8, 정보 판독 0/5, 기제까지 밝힌 오탐 진단
   1건, 데이터셋별 인증서와 검출 하한, 확증 안정 10/10과 클래스 간 일치 10/10.

**주장하지 않는 것.** 산업 표 데이터에 concept drift가 없다고 주장하지 않는다: 여러 셀이 *맹목*
으로 인증되며, 맹목은 부재가 아니다. 식별가능성 이론이 새롭다고 주장하지 않는다. overlap 실패가
비모수 식별을 막는다는 것은 고전이고 클래스 상대성도 확립돼 있다. 우리의 기여는 실행된 채널,
수리된 계기, 인증된 지도다. 모델 불가지론을 주장하지 않는다: 모든 판정은 배포된 가설 클래스에
상대적이며, 우리는 그것이 drift-유형 귀속을 정직하게 진술하는 유일한 방법이라고 본다. 노이즈
채널이 이 비교가 실패하는 유일한 방식이라고 주장하지 않는다. 다만 아무도 보고하지 않았고, 우리를
속일 만큼 컸다는 것이다. 그리고 우리 계기가 어디서나 신뢰할 만하다고 주장하지 않는다. §6이 그렇지
않은 곳의 목록이고, 그 목록의 한 항목은 우리가 아니라 한 주의 정책 결정이 써 넣었다.

---

---

## 2. 관련 연구

**표 데이터의 shift-유형 지도.** WhyShift(Liu et al., 2023)가 가장 가까운 선행이다: 표
데이터셋들에서 성능 하락을 Y|X- vs X-shift로 귀속하고 Y|X-shift가 만연하다고 결론짓는다 —
주로 공간/부분모집단 축에서, overlap을 가정하는 분해로. 우리의 델타는 셋이다: 배포된 산업
표현 위의 *시간* 축(positivity가 실제로 실패하고 그들의 렌즈가 퇴화하는 곳), 데이터셋별
인증서를 동반한 *기권 arm*(WhyShift는 분해가 측정 가능함을 가정한다), 그리고 계기-실패
해부학. DISDE(Cai et al., 2023)는 우리가 용어를 물려받은 분해 형식론을 제공한다; 그
공유-분포 항은 공통 support에서만 식별된다 — 우리의 지도가 운영화하는 경계다. TabReD 자체
(Rubachev et al., 2025)는 모든 것을 "점진적 시간 변화"로 서술하며 분해도 recency 실험도
없다; TableShift(Gardner et al., 2023)와 Wild-Time(Yao et al., 2022)은 drift-유형 귀속이
없는 shift 벤치마크다.

**식별가능성과 클래스-상대성.** support 밖에서 P(y|x)가 가정 없이 식별되지 않는다는 것은
교과서적 positivity다(고차원 overlap 실패는 D'Amour et al., 2021; 학습이론적 불가능성은
Ben-David et al., 2010; 학습된 표현 내부의 support 실패는 Johansson et al., 2019). Hinder et al.(2023)은 손실 기반 drift 검출이 양방향으로 클래스-
상대적임을 구성적으로 증명하고 — 손실을 움직이는 virtual drift, 손실에 보이지 않는 real
drift — 그들의 서베이(Hinder et al., 2024)는 "사용된 모델 클래스가 결정적"이라고 명시한다.
Loog et al.(2019)은 i.i.d.에서조차 ERM 위험이 표본 크기에 비단조일 수 있음을 보여, 어떤
무조건적 "옳은 데이터 추가는 해가 될 수 없다" 보조정리의 문도 닫는다. Gower-Winter et al.
(2026)은 윈도우 선택을 통해 drift 검출이 ill-posed라고 논증한다. 우리는 이 모두를 응용
drift-모니터링 스택이 아직 흡수하지 못한 확정 이론으로 취급하고, 실행된 실증 + 수리된
프로토콜을 기여한다; 우리 자신의 정리는 하나도 진술하지 않는다 — 논문이 형식적으로 들리는
곳(envelope, 인증서)의 내용물은 증명된 경계가 아니라 실측된 경계다.

**old 데이터의 해악.** Shimodaira(2000)는 misspecified 케이스의 기제다; Data Addition
Dilemma(Shen et al., 2024)는 임상 ML에서 혼합 해악을 경험적으로 기록한다; Klinkenberg &
Joachims(2000)는 후보 학습 윈도우들의 held-out 오차로 윈도우 크기를 *적응*시킨다. 우리가
아는 한, add-old-data 대비를 판정 의미론을 갖춘 drift-*유형* 식별자로 쓰거나 라벨-노이즈-감쇠
오탐 채널을 보고한 선행은 없다 — real drift를 P(y|x)의 임의 변화로 정의하는 필드 표준
(Gama et al., 2014; Webb et al., 2016) 아래에서 노이즈 drift는 drift*다*. 이것이 바로
*착취 가능한 규칙 변화*를 검출한다고 주장하는 계기가 둘을 분리해야 하는 이유다; 우리는 그
분리를 구성으로 달성하고 적대적으로 검증한다.

**Pseudo-label·denoising·교차적합.** denoised arm의 역학은 의도적으로 차용된 것이다: 라벨을
모델 예측으로 대체하는 것은 self-training(Scudder, 1965; Lee, 2013)이자 distillation(Hinton
et al., 2015)이고; 시끄러운 라벨이 적합된 모델로 복구 가능하다는 관점은 noisy-label 학습
(Natarajan et al., 2013; Han et al., 2018)의 기저이며; 자기-적합 오염을 피하는 엄격한
out-of-fold 예측은 교차적합(cross-fitting; Chernozhukov et al., 2018)이다. 우리가 이
문헌들에서 가져오는 것은 추정기다; 그 *용법* — 적대적으로 지도화된 유효 envelope와 기권
규칙을 갖춘 drift-유형 판정의 판별 arm으로서 — 이 우리가 주장하는 기여이고, 우리는 그것을
근거의 범위로 한정한다: 우리가 읽은 한, 이들 인접 문헌 어디에도 이 arm이 무력화하려 존재하는
실패 채널은 보고되어 있지 않다.

**drift 검출기와 그 평가.** 고전 검출기들(서베이: Gama et al., 2014; Lu et al., 2019)은
손실 또는 분포 통계량을 모니터링하며 스트림에 기지 drift를 주입해 평가된다(Poenaru-Olaru
et al., 2022); Detectron(Ginsberg et al., 2023)은 harmful shift를 모델-상대적으로 정의한다.
우리의 주입 컨트롤은 역할이 다르다: *in-situ, 데이터셋별 검정력 인증서*(실제 공변량 기하에
심고 학습가능성으로 게이트)로서, earned blindness와 계기 공허(vacuity)를 구별하는 데 쓰인다
— 실데이터에서 중요함이 드러나는 구별이다: 산업 "unidentifiable" 셀 4개 중 둘은 시도한
모든 신호 패밀리에서 인증서를 얻지 못하고, 나머지 둘은 기준 패밀리 밖으로 넓혀야만 해소된다. 우리는 이 검출기들을 감사 셀에 head-to-head로 돌리지 않는다: 손실-스트림
검출기는 "무언가 변했는가?"에 답하며 구성상 유형-맹목이라, 그 발화는 drift-*유형* 귀속에
대해 아무것도 말해주지 않는다. 우리는 이것을 주장만 하지 않고 측정한다: 배터리의 prequential
오차 스트림 위에 표준 탐지기 4종을 돌리면 발화율이 진실을 전혀 따라가지 않는다 — 진짜 규칙 변화가
정상 셀과 정확히 같은 비율로 발화하고, 고정 규칙의 공변량 이동은 발화하며, 순수 정상 회귀 셀이
둘과 같은 수준으로 발화한다(부록 B.7). 유형을 *귀속하는* 프레임은 다른 문제이고, 우리는 그런 프레임
하나를 진실이 구성으로 고정된 배터리 셀에 head-to-head로 돌린다(§3.4): 그것은 규칙 변화와
노이즈 감쇠를 크기로는 갈라내되 부호로는 갈라내지 못한다. 같은 바이트 위에서 더 약한 *귀속*
프로브가 무엇을 보고하는지의 동종 비교는 우리 계기 내부의 카나리아 패널(§6.1)이고, 인증서
프로토콜의 스트리밍 모니터 이식은 향후 과제다(§8).

**말웨어.** TESSERACT(Pendlebury et al., 2019)는 말웨어에서 시간적으로 정직한 평가를 확립했고
성능 열화를 기록한다. 우리의 EMBER 판독(old 데이터가 *도움*; staleness −0.008; 검출 하한
0.0013)은 그것을 반박하는 게 아니라 정제한다: 열화는 커버리지-기인(새 패밀리 등장 = 공변량
확장)이지 라벨 부패가 아니며(2017년 말웨어 표본은 여전히 말웨어다), 이것이 정확히 배포 렌즈가
그어야 할 구별이다.

---

## 3. 실패 채널

### 3.1 라벨-노이즈 감쇠가 concept drift를 주조한다

배포에서 drift 모니터가 받는 질문은 "분포가 움직였는가"가 아니라 "*규칙*이 움직였는가"다. 두 답이
서로 다른 수리를 함의하기 때문이다. 최근-재학습은 규칙 변화의 처방이고, 규칙이 고정돼 있을 때는
여전히 유효한 데이터를 버리는 실수다. 손실 기반 귀속은 그 질문에 비교로 답한다: 최근 데이터로
적합한 모델 대 최근+old로 적합한 모델, 그 격차를 old 행이 이제 오도한다는 증거로 읽는다. 이
비교에는, 우리가 아는 한 보고된 적 없는 오탐 채널이 있다.

규칙을 증명적으로 고정하고 *라벨 노이즈*만 감쇠시키면 — 초기 라벨이 후기보다 노이즈가 크고, x에서
참 라벨로 가는 사상은 내내 불변 — 비교는 발화한다. 12,000행의 구성된 null에서 raw 팔은 +0.021
을 읽는다. 이 문헌의 임계값들이 사는 0.02 판정 floor 위다. 그 크기는 추상이 아니다: 우리 자신의
과거 계기가 보고했고 우리가 믿었던 산업 concept 양성 +0.024와 0.003 이내에 떨어진다(§3.2).
그 판독을 소비하는 모니터는 최근 데이터로 재학습하며 옳은 라벨을 버리고, 규칙이 결코 움직이지
않은 데이터셋에 규칙 변화를 귀속할 것이다.

이 채널은 단발 요령이 아니다. 수리 자체를 겨냥한 적대적 구성이 둘을 더 찾았다: *스케일이 x에
의존하며* 감쇠하는 노이즈(raw +0.025), 그리고 — 독립 red-team 패스에서 나온, pseudo-label
오차가 신호가 사는 바로 그곳에 집중되므로 denoiser의 최악 케이스인 — 규칙을 나르는 피처와 상관된
노이즈 스케일(raw +0.026). 셋 다 raw 팔에서 발화하고, 셋 다 규칙이 증명적으로 고정돼 있다.
기제는 일단 말하고 나면 초보적이다: 노이즈가 큰 old 라벨은 그것을 적합하는 모든 모델의 손실을
부풀리므로, old 행을 *제거*하면 규칙과 무관한 이유로 최근 윈도우 손실이 좋아진다. 위험한 것은 그
결과 수치가 부호로도 크기로도 실무자가 실제로 조치하는 것과 구별되지 않는다는 점이다.

대칭 검사: 라벨 노이즈가 시간에 따라 *증가*하면 −0.023 — old 행이 돕는 것처럼 보인다 — 이므로
이는 상수 편향이 아니라 노이즈 궤적의 방향성 인공물이다.

### 3.2 계기가 실데이터에서 자기 자신의 과거 양성을 진단한다

우리의 v2 계기(raw 팔만)는 패널의 유일한 회귀 데이터셋인 sberbank-housing을 유일한 산업 concept
양성으로 보고했다: +0.024 [+0.018, +0.030]. §3.1의 구성된 null은 그 크기에 맞춰 만들어졌고,
그다음 수리된 계기를 실데이터로 되돌렸다.

윈도우 해상도 K ∈ {5, 8, 10, 12, 20}에 걸쳐 raw 팔은 K ∈ {8, 10, 12}에서 발화하며, K = 10에서
v2 헤드라인을 비트 단위로 재현한다(+0.0239; raw 파이프라인의 RNG 스트림이 바뀌지 않았으므로
이는 우연히 달라진 재측정이 아니라 *같은* 신호의 재해석이다). old 윈도우의 실측 노이즈 프록시는
모든 K에서 최근 중앙값의 2.1–2.9×다. 그리고 denoised 팔은 — old 라벨을 교차적합 pseudo-label
로 대체한 — 모든 K에서 유의하게 음수다(−0.014 ~ −0.018, 전 CI가 0 아래): 노이즈를 걷어내면
old 행이 *돕는다*. 규칙은 변하지 않았고, 초기 라벨이 더 노이즈가 크다 — 2011–2012 위기기 러시아
주택 가격과 정합적이다.

이런 모양의 음성 결과는 반론 하나를 부른다: 윈도우에 그냥 검정력이 없는 것 아니냐. 아니며, 우리는
그것을 논증하지 않고 인증한다. 엄격 판정 규칙 하에서 이 셀은 주입 컨트롤에 도달하고, 거기서
기준 강도로 심은 규칙은 이 기하에서 학습가능하며(윈도우 내 R² 0.93) +0.086으로 회복된다.
K = 20에서는 심은 규칙이 같은 기하를 통해 +0.101로 회복된다. 규칙 변화를 보여주지 못하는 바로
그 윈도우가, 넣어둔 규칙 변화는 보여준다. 진단의 대상은 주조된 양성뿐이다: floor *아래*의 잔여
규칙 변화가 노이즈 감쇠와 공존할 가능성은 배제되지 않으며, 우리는 sub-floor에 대해 어느 방향으로도
주장하지 않는다.

drift-귀속 계기가 자기 과거 오탐을 재현하지 못한 게 아니라 그 *기제*를 실데이터에서 진단한 선행
사례를 우리는 찾지 못했다.

### 3.3 노이즈 게이트가 수리가 아닌 이유

자명한 대안 — 노이즈 궤적을 재고 old 윈도우 노이즈가 높으면 판정을 거부한다 — 는 필요하지만
불충분하며, 배터리가 그것을 구성으로 말한다. 회전하는 규칙과 감쇠하는 노이즈가 공존하는 셀에서
노이즈 게이트는 발화하지만(비 3.67) 규칙은 실제로 움직였다. 거부권을 가진 게이트는 진짜 양성을
억누를 것이다. 그래서 계기는 게이트와 denoise를 분리한다: 그 셀에서 denoised 팔은 여전히
+0.316을 읽으며 이는 청정 회전-규칙 신호(+0.541)의 58%이고, 작은 크기에서도 유지된다(0.8-rad
회전: 청정 +0.108, 노이즈 감쇠 동반 +0.062 — 감쇠되나 발화). 게이트는 교락을 표지하고, 판정은
denoised 팔만 한다.

denoise 자체가 편향돼 있다 — pseudo-label 오차가 old 윈도우 노이즈와 함께 커진다 — 므로 그
유효성 경계는 가정이 아니라 실측된다. 순수 고정-규칙 null에서 denoised 팔은 노이즈비 3.54에서
+0.0037, 3.76에서 +0.0048, 3.87에서 +0.0043, 4.72에서 +0.0140, 그리고 5.71에서 +0.0259 —
null에서 판정 floor를 넘는다. 비 4.7 위에서 계기는 기권한다. 그 너머에도 분리는 존재하지만
(비 ≈6에서 진짜 회전은 +0.086, null은 +0.026, CI 분리), 거기서의 임계값은 단일 노이즈 패밀리에
기대게 되므로 우리는 임계값을 보정하는 대신 판정을 거부한다. 기권은 면책 문구가 아니라 실측된
envelope이다.

### 3.4 이 채널은 실무가 실제로 쓰는 프레임에도 미치는가

위의 절들은 우리 자신의 프로브를 잰다. 자명한 반론은, 다른 원리로 만든 현장 도구 — 이동을 X-측과
Y|X-측 항으로 분해하는 재가중 기반 프레임 — 는 속지 않으리라는 것이다. 우리는 그런 프레임을,
진실이 구성으로 고정된 배터리 셀에, 그 프레임에 유리한 조건(공변량 중첩 유지, cov-AUC 0.500,
재가중 후 유효 표본 71.3%)에서 겨눴다.

**표 1.** 진실이 구성으로 고정된 배터리 셀 위의 재가중 기반 프레임. 두 기제를 크기로는 갈라내되 부호로는 갈라내지 못한다.
| 셀 | 진실 | Y\|X-측 격차 |
|---|---|---|
| 회전 규칙 (이진) | 규칙 이동 | **+0.4345** |
| 회전 규칙 (회귀) | 규칙 이동 | **+0.8207** |
| 라벨-노이즈 감쇠, 고정 규칙 | 규칙 고정 | +0.0576 |
| x-의존 노이즈 감쇠, 고정 규칙 | 규칙 고정 | +0.0615 |
| 정상 (회귀) | 규칙 고정, 노이즈 추세 없음 | −0.0208 |

우리 주장의 강한 버전은 이것으로 반증되며, 우리는 그렇게 말한다: 프레임은 두 기제를 크기로
갈라낸다, 7.5–14×. 라벨이 아니라 수치를 읽는 실무자는 오도되지 않는다. 남는 것은 더 좁고 여전히
비싸다. 부호는 갈라내지 못한다. 두 노이즈-감쇠 셀이 *양의* Y|X-측 격차를 내는데 진짜 정상 셀은
음수를 낸다. 그래서 현장 표준 정의(Webb 등; Gama 등) — 그 정의에서 노이즈 감쇠는 *곧* Y|X 변화다
— 아래에서 임계값 판독은 둘을 같은 사건으로 파일링한다. 두 사건의 수리는 다르다: 최근-재학습은
한쪽에서 옳고 다른 쪽에서는 유효한 라벨을 버린다. 실패하는 것은 부호나 임계값에 의한 귀속이고,
모니터에 필요한 것은 §3.1이 만든 종류의 null에 대해 보정된 크기에 의한 귀속이다.

---

## 4. 수리된 계기

**Figure 1.** 계기의 판정 캐스케이드 — 벡터 정본 = `paper/figures/fig1_cascade.tex`(TikZ,
컴파일 확인됨; `fig1_cascade.pdf`), 초안 스케치 = 영문본 §4의 mermaid. 캐스케이드는 §4.4과
PREREG §3에 원문 그대로 명세된다. 모든 실행에 주석으로 붙는 side channel: strict-rule 그림자
판정(rule-sensitive 플래그), 재발 레짐의 음의-recency 지문(§7.3), 카나리아-프로브 패널(§6.1),
그리고 provenance 스탬프.

*읽기 보조.* 실제 셀 하나가 아래 어휘의 대부분을 구동한다. sberbank-housing(§3.2)에서 raw
arm은 발화하지만(+0.024) 노이즈 게이트가 켜져 있고(2.1×) denoised arm은 음수다 — 그래서
캐스케이드는 concept이 아니라 NOISE-DRIFT-CONFOUNDED를 반환한다; 셋이 envelope 안에서
정렬했더라면 판정은 주입 양성대조를 조건으로 DEPLOYMENT-CONCEPT였을 것이다.

**용어 (각 한 줄).**

**표 2.** 용어, 한 줄씩.
| 용어 | 의미 |
|---|---|
| raw staleness | 미래-윈도우 score(recent) − score(recent ∪ old); >0 = old 데이터가 해침 |
| denoised staleness | 동일하되 old 라벨을 old-윈도우 내부 교차적합 예측으로 대체; >0 = *규칙*이 변함 |
| noise gate | old-윈도우 노이즈 프록시 / recent 중앙값; >1.5 = 라벨-노이즈 drift 존재 |
| envelope | denoiser 유효성의 실측 경계인 노이즈비 4.7; 그 위에서 계기는 기권 |
| D | 윈도우 분리도 AUC(group-aware·크기맞춤); 라우팅 통계량, support overlap이 *아님* |
| injection | 데이터셋 자신의 기하에 심은 기지 규칙 회전; 데이터셋별 검정력 인증서 |
| earned blindness | 주입이 윈도우 내 학습가능한데도 회복 불능 → 기하가 이 신호 클래스를 진짜로 숨김 |
| injection-vacuous | 주입 학습불능 → 그 null은 아무것도 인증하지 않음 |
| no-evidence band | sub-floor CI-양성 읽기; no-drift 앵커에서 노이즈로 캘리브레이션 |
| canary probe | 오발이 보고되는, 의도적으로 misspecified된 프로브 클래스(선형/kNN) |
| rule-sensitive | 일차/엄격 판정 규칙 간 판정 불일치; 헤드라인 금지 |

### 4.1 설정과 estimand

행 (x_i, y_i, t_i)가 시간에 따라 도착한다; 감사자는 라벨된 전체 이력을 본다(회고적 감사,
온라인 예측 아님). K개 윈도우를 timestamp-값 랭크로 고정한다(동률은 경계를 넘지 않음; 시드별
경계 지터). 각 미래 윈도우 W_j(후반부)에 대해, old 앵커 W_0(행 ≥200이고 학습 가능한 라벨을
가진 최초 윈도우)와 recent 윈도우 W_{j−1}로부터 크기 맞춘 표본(N = min(|recent|, |old|,
6000))을 뽑아 배포 클래스의 세 모델(recent-only, old-only, recent∪old)을 학습한다. 미래
윈도우들에 대한 시드별 평균으로 보고한다:

- **decay** = score(old 모델, W_0 held-out) − score(old 모델, W_j) — 노화, 기제-맹목
  (Vela et al., 2022가 대규모로 기록한 현상);
- recency gain = W_j에서 score(recent) − score(old) — 적응 가치, 커버리지와 규칙 변화가
  섞임; *그 부호 자체가 진단적이다(§7.3): 음의 recency는 단방향 drift에서 불가능하며 재발
  레짐을 지문한다*;
- raw staleness = W_j에서 score(recent) − score(recent∪old) — 귀속 프로브;
- denoised staleness = W_j에서 score(recent) − score(recent ∪ (X_old, ĝ_old(X_old))),
  여기서 ĝ_old는 W_0 *내부* 2-fold 교차적합 모델이 산출하는 엄격히 out-of-fold인 hard
  pseudo-label.

점수: AUC(이진), accuracy(다중클래스), z-표준화 목표의 −RMSE(회귀). CI는 10 시드에 대한
Student-t; 각 시드는 90% 행 부표본을 재추출하고 윈도우 경계를 재지터하며 모든 학습 집합을
재표집한다. 이것들은 심하게 겹치는 부표본 위의 시드-수준 구간이며 미세한 효과에서는
반보수적(anti-conservative)이다(§7.3이 그 결과인 무증거 대역을 no-drift 앵커로
캘리브레이션한다); 판정-수준 추론은 이것에만 의존하지 않는다 — 작동하는 안정성 점검은 확증
fresh-seed 재현(§7.1)이고, 모든 실행은 시드별 원값을 함께 출력하므로 대안적 구성(윈도우-블록
부트스트랩, split-half)을 재실행 없이 적용할 수 있다. 사전등록된 estimand는 *착취 가능한
mean-rule drift*: 배포된 가설 클래스에 대해 old 라벨이 현재 규칙과 모순되게 만드는, P(y|x)의
의사결정-관련 함수의 변화다. 이는 필드 표준 "P(y|x)의 임의 변화"(Moreno-Torres et al.,
2012; Webb et al., 2016)보다 의도적으로 좁다 — 그 좁힘이 요점이다. 넓은 정의는 라벨-노이즈 drift를 concept으로 분류하고,
그로써 도움이 될 수 없는 재학습을 정당화하기 때문이다.

### 4.2 denoised 팔과 노이즈 게이트

**denoised arm이 규칙 변화와 노이즈 drift를 분리하는 이유.** 노이즈-단독 drift에서 조건부
평균/Bayes 규칙은 불변이므로, ĝ_old는 시끄러운 라벨로부터 *같은* 규칙을 추정한다; 그
pseudo-label은 근사적으로 옳은 denoised 라벨이고, (X_old, ĝ_old(X_old))를 recent 집합에
추가하는 것에는 해칠 기제가 남지 않는다(§3.3가 경계를 실측하는 기대(expectation)이지
정리가 아니다) — 실행: +0.021의 노이즈-감쇠 오탐이 +0.004로
붕괴하고 게이트(아래)가 기제를 표지한다. 진짜 규칙 변화에서는 ĝ_old가 *옛* 규칙을 일관되게
추정한다; 그 pseudo-label은 여전히 현재 규칙과 모순되고 해악이 잔존한다 — 실행: 회전-규칙
스트림은 raw +0.546 중 +0.541을 유지하고, 규칙변화+노이즈감쇠 동시 발생도 +0.316으로
발화한다. denoiser는 불편(unbiased)이 아니다: pseudo-label 오차는 old 윈도우 노이즈와 함께
커지며, 우리는 *경계를 지도화했다* — old/recent 노이즈비 ≈5.7에서 denoised arm 자신이 순수
null 위에서 판정 floor를 넘는다(+0.026). 따라서 계기는 정리(theorem)의 가장(假裝) 대신
envelope 상수를 지니고 다닌다(비율 4.7 위 기권); Loog et al.(2019)에 따라, 얻어질 무조건적
soundness 보조정리는 존재하지 않는다.

**노이즈 게이트.** 윈도우마다 배포 클래스의 fresh 모델을 70%로 적합하고 held-out
비환원-오차 프록시를 기록한다(회귀: z-표준화 y의 MSE; 이진: 1−AUC; 다중: 1−accuracy). 게이트
통계량은 old 윈도우 프록시 / recent 윈도우 프록시 중앙값; 1.5 위에서 발화하고(안정 합성
컨트롤은 0.75–0.99로 캘리브레이션) 4.7 위에서 envelope를 정의한다. 게이트 단독은 충분한
수리가 *아니다* — 게이트-거부권 규칙은 노이즈 drift와 공존하는 진짜 규칙 변화를 오분류한다
(실행: 게이트 3.67에도 +0.316은 발화해야 한다) — 게이트는 raw arm이 발화하고 denoised arm이
발화하지 않을 때 *기제 라벨*(NOISE-DRIFT-CONFOUNDED)을 공급한다.

### 4.3 인증서: 분리도, 주입, 학습가능성

**윈도우 분리도 D ("overlap"이 아님).** D는 W_0와 각 후반부 윈도우를 분리하는 분류기의
held-out AUC 중앙값으로, proxy-strip된 피처 공간에서, 소박한 버전 대비 두 가지 수리와 함께
계산된다: *랜덤 부표본*에 의한 크기 맞춤(시간 정렬된 행에서의 head-slice는 윈도우-크기
불균형만으로 분리도를 제조한다 — 실행: 정적 피처가 전무한데 shuffle-D 0.94, 수리 후 0.50으로
붕괴)과 근접-중복 클러스터(0.1로 반올림한 z-표준화 행)를 키로 하는 *group-aware* train/test
분할: 다중도 5의 정확한 중복은 행-분할 D를 0.994로, entity cohort는 1.000으로 — 공변량 변화
전무 상태에서 — 부풀리고; group 분할은 정직한 drift의 높은 D는 유지한 채 둘 다 ≈0.50으로
되돌린다. 우리는 D를 *분리도*로만 보고하고 support overlap으로는 결코 보고하지 않는다:
진짜로 예측적인 drifting 피처 하나가 D를 포화시키므로, D ≥ D* = 0.96은 staleness null을
주입 컨트롤로 라우팅할 뿐 그 자체로는 아무것도 결론짓지 않는다.

**학습가능성-게이트 주입.** 모든 unidentifiable 후보 데이터셋(그리고 양성 대조로서, 모든
CONCEPT 판정)에 대해 기준 concept — 최고 분산 두 피처 위의 고정 강도 규칙 회전 — 을
데이터셋 자신의 (X, t) 기하에 심고 전체 staleness 파이프라인을 재실행한다(10 시드, 본판독과
동일 검정력). null이 어떤 의미든 갖도록 허용되기 전에, 주입된 규칙이 *윈도우 내에서 학습
가능*해야 한다(held-out AUC ≥ 0.65 / R² ≥ 0.20 / accuracy ≥ 다수클래스 + 0.10): 최고-분산
피처가 heavy-tail 잡동사니인 구성 기하에서 주입 규칙은 학습 불능이고(윈도우 내 AUC 0.482)
소박한 프로토콜은 "earned blindness"를 공허하게 부여한다; 게이트는 그것을 *injection-
vacuous* — 인증서 거부 — 로 전환한다. 결과: 회복(⇒ 실제 null은 유정보였음: 검증된
무-concept), earned blindness(학습 가능하나 회복 불가 ⇒ 기하가 이 신호 클래스를 진짜로
숨김), 또는 공허(인증서 없음). 실제 감사에서 이 구별은 하중을 진다: "unidentifiable" 산업
셀 4개 중 둘은 시도한 모든 신호 패밀리에서 학습가능성 게이트에 탈락해 인증서 없이 끝나고,
나머지 둘은 패밀리를 넓혀야 해소된다 — homesite_insurance는 기준 패밀리에서 게이트 ±0.05 안에
있어 시드 집합 간 뒤집히지만 저분산 캐리어에서는 여유 있게 학습가능하며 일관되게 미회복이고,
weather는 기준 패밀리에서 학습불능이지만 다른 두 패밀리에서 회복한다(§7.2).
인증서는 이 기준 패밀리에 명시적으로
상대적이다: 회복이 인증하는 것은 데이터셋의 고분산 방향이 실어 나르는 회전에 대한
검정력이지, 저분산 피처·상호작용·부분모집단에 사는 규칙 변화에 대한 것이 아니다(§8 한계 6).

**식별 경계 (선행연구의 재진술이며 신규 결과가 아님).** R을 미래 윈도우가 차지하고 old 앵커는
차지하지 않는 공변량 공간 영역이라 하자. R 위에서 P(y|x)는 관측된 윈도우들로부터 비모수적으로
식별되지 않는다: old 윈도우의 support에서 일치하고 R에서 다른 두 조건부는 동일한 관측
데이터를 유도하기 때문이다 — R에 old 행이 하나도 없으므로. 이는 DISDE가 명시하는 공유-support
요건(Cai et al., 2023, Prop. 1)의 시간축 사례이며, 우리는 우리 자신의 정리를 주장하지 않는다.
계기의 세 성질이 편의가 아니라 여기서 따라 나온다. 기권(abstention)은 소심함이 아니라
정확성이다. D는 패널이 그 경계에 얼마나 가까운지를 재는 라우팅 통계량이지 support
중첩의 측도가 아니며 — 그래서 D ≥ D\*는 그 자체로 아무것도 결론하지 않고 주입 컨트롤로 넘긴다.
그리고 식별가능성 인증서가 없는 셀은 *drift 없음*의 증거가 될 수 없고 오직 *측정 없음*의
증거일 뿐이며 — 그래서 우리는 사전등록된 0/8 옆에 인증된 0/5를 함께 들고 다닌다(§7.2).
반대 방향, 즉 *건전성*(staleness > 0이면 규칙 변화)에 대해서는 얻을 수 있는 정리가 없다:
Loog et al. (2019)이 같은 분포의 데이터를 더해도 ERM 위험이 비단조임을 보였으므로, drift가 0이고
이분산성이 0이어도 E[staleness] > 0이 가능하다. 이 비대칭이 §3.3가 건전성 보조정리가 있어야 할
자리에 *측정된* 유효 범위(envelope)를 놓는 이유다.

### 4.4 판정 캐스케이드와 범위

사전등록된 캐스케이드(PREREG §3; 아티팩트에 원문): DEPLOYMENT-CONCEPT는 노이즈 envelope
내에서 *denoised* arm의 발화(CI 하한 > 0 이고 평균 > 0.02)를 요구한다; 게이트 켜진 raw
발화는 NOISE-DRIFT-CONFOUNDED; 게이트 꺼진 raw 발화는 RAW-ONLY-POSITIVE(미해결, 결코
concept 아님); null은 D를 거쳐 주입 인증서로 라우팅되고; sub-floor denoised CI는 무증거
대역으로 보고된다(캘리브레이션: no-drift 앵커 스트림 5개 중 2개가 floor의 1/10–1/20에서
그곳에 착지 — CI-유의하나 의사결정-무관). 엄격한 이차 규칙(CI 하한 > floor)이 병행 계산되며;
두 읽기에서 판정이 갈리는 셀은 rule-sensitive로 표기되고 헤드라인에서 금지된다. 모든 판정은
배포된 가설 클래스로 범위 한정된다: ground-truth 스위트는 HGB와 랜덤 포레스트에서 통과하고
선형/kNN 프로브에서 실패하므로(§5), 트리-앙상블 판정이 의사결정 등급이고 선형/kNN은
카나리아로 달린다. 모든 실행은 provenance 메타데이터(커밋, argv, 라이브러리 버전, 시드)를
버전드 아티팩트에 기록하며; 판정 규칙·임계값·시드 프로토콜(탐색 0–9, 확증 100–109)·집계
읽기는 그것들이 지배하는 실행 이전에 커밋되었다. 부록 C가 모든 판정 상수를 캘리브레이션
출처·유효 범위와 함께 표로 제공한다.

---

## 5. 검증: 배터리와 관문 규율

### 5.1 14-셀 사전등록 배터리

계기는 어떤 실데이터 실행 전에도 합성 ground-truth 배터리를 통과해야 한다(PREREG §4).
배터리는 진입 관문이며, 논문의 논증을 지는 셀들 — 고정 규칙 하의 노이즈 감쇠, x-의존 노이즈,
규칙 변화와 노이즈 감쇠의 공존 — 은 §3에서 읽는다. 여기서 그것들은 계기가 자기 판정을
획득한다는 증거다. 전 셀: n = 12,000, d = 10, K = 10, 5 시드
(실데이터의 10과 대비 — 심은 효과가 판정 floor의 10~27×이고 배터리는 추정이 아니라
pass/fail 관문이다); 전체 캐스케이드 하의 판정(아래 표).

**표 3.** 14-셀 사전등록 배터리: 진실, 두 staleness 팔, 노이즈 게이트, 캐스케이드가 내는 판정.
| 셀 (진실) | raw | denoised | gate | 판정 |
|---|---|---|---|---|
| 회전 규칙 (이진) | +0.280 | +0.289 | 0.90 | **CONCEPT** ✓ |
| 회전 규칙 + drift하는 nuisance | +0.203 | +0.206 | 1.18 | **CONCEPT** ✓ (proxy-strip) |
| 회전 규칙 (회귀) | +0.546 | +0.541 | 0.91 | **CONCEPT** ✓ |
| **회전 규칙 + 노이즈 감쇠** | +0.357 | **+0.316** | 3.67 (발화) | **CONCEPT** ✓ — 게이트가 거부하면 안 됨 |
| 공변량 변화, 고정 규칙 | −0.001 | −0.000 | 0.74 | UNIDENT (실명 획득) ✓ |
| 정상(stationary) | −0.001 | −0.000 | 0.75 | NO-STRONG-CONCEPT ✓ |
| prior 변화, 고정 규칙 (다중클래스) | −0.003 | −0.004 | 0.84 | UNIDENT, injection-vacuous ✓ |
| 약한 공변량 변화 (D = 0.73, un-gated) | −0.003 | +0.001 | 0.99 | NO-STRONG-CONCEPT ✓ — null이 하중을 짐, 게이트로 치워지지 않음 |
| 정상 (회귀) | −0.012 | −0.002 | 0.99 | NO-STRONG-CONCEPT ✓ |
| 공변량 변화, 고정 *선형* 규칙 (회귀) | +0.002 | +0.004 | 0.91 | UNIDENT ✓ (결코 CONCEPT 아님) |
| 공변량 변화, 고정 *비선형* 규칙 (회귀) | −0.005 | −0.003 | 0.92 | UNIDENT ✓ |
| **라벨-노이즈 감쇠, 고정 규칙** (F1 킬러) | **+0.021 발화** | +0.004 | 3.54 | **NOISE-DRIFT-CONFOUNDED** ✓ |
| 라벨 노이즈 *증가*, 고정 규칙 | −0.023 | −0.019 | 0.14 | NO-STRONG-CONCEPT ✓ |
| **x-의존 노이즈, 감쇠 스케일** | **+0.025 발화** | +0.005 | 3.76 | **NOISE-DRIFT-CONFOUNDED** ✓ |

논문의 논증을 지는 세 셀 — 고정 규칙 하의 노이즈 감쇠, 그 x-의존 변종, 규칙 변화와 노이즈
감쇠의 공존 — 은 §3에서 읽으며 여기서 다시 논증하지 않는다. 배터리가 더하는 것은 그 주위의
규율이다: 나머지 모든 행은 진실이 구성으로 고정된 컨트롤이고, 계기는 실데이터에 손대기 전에
14개 전부에서 옳은 판정을 내야 한다. 표의 두 판독은 다른 어디에도 나오지 않는다. *세 번째*
노이즈 채널 — 규칙을 나르는 피처와 상관된 노이즈 스케일, denoiser 최악 케이스, 독립 red-team
패스가 구성 — 은 raw 팔을 +0.026으로 발화시키고 denoised 팔이 +0.006으로 무력화한다. 그리고
작은 old 윈도우에 대한 denoiser 강건성을 직접 확인했다(old 윈도우 600행, fold 300): 오탐
없음(+0.004), 미탐 없음(+0.429).

**지표 간 floor 비교가능성.** 0.02 판정 floor는 AUC·accuracy·z-표준화 −RMSE에 공유되는데,
이들은 의사결정-등가 단위가 아니다. 두 완화 장치가 구조적이다: 모든 null은 자신의 검출가능-
효과 하한 δ(집계의 실질 작업을 수행하는 데이터셋별·지표별 검정력 진술)를 지니고 다니고,
CONCEPT는 결코 floor 단독에 의존하지 않는다(denoised CI·게이트·envelope가 모두 정렬해야
한다). 민감도 점검으로, 배터리의 강도-맞춤 concept 크기(이진 +0.28 vs 회귀 +0.55 ⇒ 회귀
floor 0.04)로 재스케일한 지표별 floor 아래 모든 커밋된 실행을 재역치화해도 concept/무-concept
경계를 넘는 셀은 없다; 유일하게 영향받는 셀은 진단된 회귀 양성(§3.2)의 기제 라벨인데, 이는
이미 rule-sensitive로 표기되어 헤드라인에서 금지되어 있다.

### 5.2 관문에는 이빨이 있다: 계기가 바뀐 뒤의 재게이트

배터리는 한 번 돌리는 것이 아니다. 계기에 가해지는 모든 변경이 통과해야 하는 재게이트이며, 함께
커밋된 규칙은 불일치 시 변경을 설명하는 게 아니라 되돌린다는 것이다. 이 규칙은 사후에 실제로
행사됐다: 프로브에 opt-in 진단 플래그 두 개가 추가됐을 때 배터리를 재실행해 참조 환경에 대해
비트 동일한 결과를 요구했다 — 14셀 전부, 전 필드, 판정 포함. 그리고 동일했다. 사전등록된
계기는 코드가 이미 쓰인 뒤에도 그 관문이 실패할 수 있어야 의미가 있으며, 이것이 실패할 수 있다는
기록이다.

---

## 6. 계기가 눈머는 곳

§4와 §5는 수리된 계기가 말할 수 있는 것을 확립한다. 이 절은 말할 수 없는 것을, 주장이
아니라 실측으로, 네 축에 걸쳐 확립한다. 셋은 우리 자신의 도구로 재고, 넷째는 법으로 문서화된
규칙 변경에 대해 바깥에서 정해진다.

이것을 적용(§7)보다 앞에 두는 것은 의도적이다. "규칙 변화 없음"이라는 판정은 그 맹점만큼의
값어치이고, 맹점을 모르는 독자는 뒤따르는 지도를 값 매길 수 없다.

### 6.1 클래스 상대성: 같은 바이트, 다른 프로브, 다른 판정

계기의 모든 구성요소 — 두 staleness 팔, 노이즈 게이트, 분리도, 주입 컨트롤 — 는 프로브의 모델
클래스를 따라간다. 다섯 핵심 컨트롤에서 그 클래스를 바꾸면 보통 뭉뚱그려지는 두 성질이 갈라진다.
concept 검출은 클래스-강건하다: 다섯 클래스 전부 진짜 회전에서 발화한다(+0.280 ~ +0.310).
concept/covariate 분리 — 판정이 실제로 기대는 성질 — 은 그렇지 않다. kNN은 고정 규칙 하의
순수 공변량 이동을 concept으로 읽고(+0.098), 고정 규칙 prior shift에서는 kNN(+0.048)·
선형(+0.026)·2층 MLP(denoised +0.042)가 모두 오발화한다. 이는 Shimodaira(2000)를 경험적으로 만든
것이다: misspecification 하에서 혼합-ERM 최적해가 P(x)와 함께 움직이므로 공변량 이동만으로
"old 데이터가 해친다"가 제조된다. denoise는 이 채널을 고치지 못한다 — kNN은 pseudo-label에서도
+0.111로 오발화하는데, misspecification이 라벨이 아니라 프로브에 있기 때문이다.

신경망 결과는 따로 한 문장을 받을 만하다. 이 분야의 동기 질문이 딥 표 모델에 착취할 규칙 변화가
있느냐이기 때문이다: 우리가 시험한 첫 신경 프로브는 분리 배터리를 통과하지 못하며, 따라서
판정 등급이 아니라 카나리아로 실린다. 이것을 현대 딥 아키텍처의 생산 규모로 외삽하지 않는다.
우리가 주장하는 것은 입증 책임의 방향이다 — 프로브 클래스는 이 배터리를 통과함으로써 drift
판정 권한을 얻는다.

실데이터에서 그 결과는 가정이 아니다. Electricity — 문헌의 표준 concept-drift 데이터셋 — 는
독립된 카나리아 클래스 둘에서 DEPLOYMENT-CONCEPT로 읽힌다(선형: raw +0.023, denoised +0.033,
주입 회복 +0.190; MLP: raw +0.007, denoised +0.025). 같은 바이트에서 트리-앙상블 프로브는
unidentifiable-with-earned-blindness로 읽는다. 모니터가 그 데이터셋의 평판을 확인해 주는지 여부는
모니터의 성질이다. 경고로 보고할 만한 카나리아 거동이 하나 더 있다: ecom_offers에서 선형
프로브는 raw 팔이 null(−0.002)인 채 *denoised* 팔만 발화한다(+0.028) — 선형 ĝ_old의 계통 오차가
선형 하류 모델에 그 자체로 정보가 되기 때문에 생기는, misspecified 프로브 특유의 denoiser 인공물
이다. denoised 팔의 의미론 역시 클래스-범위다.

### 6.2 분리도 게이트 안쪽에서 검정력은 연속적으로 붕괴한다

분리도 임계값 D\* = 0.96은 셀을 주입 컨트롤로 라우팅하며 절벽처럼 읽힌다. 절벽이 아니다. 그 바로
안쪽에서 *고정* 강도로 심은 규칙이 전혀 다른 크기로 회복된다: cooking_time(D = 0.966) +0.546,
delivery_eta(D = 0.999) +0.332, Electricity(D = 1.000) +0.018, homesite_insurance
(D = 1.000) −0.052.

학습가능성은 이 순서를 설명하지 못한다 — Electricity의 심은 규칙은 패널에서 *가장* 학습가능한데
(윈도우 내 AUC 0.976) 거의 회복되지 않는다. D에 걸쳐 감쇠하는 것은 학습가능성이 아니라 회복이다.
판정 등급의 모든 학습가능 주입 실행에 걸쳐 D와 회복의 순위 상관은 ρ = −0.47 (p = 0.037,
n = 20)이고, 가장 깨끗한 대비는 한 데이터셋 안에 있다: Electricity의 동일한 심은 규칙이 선형
프로브에서 D = 0.905일 때 +0.190으로, D = 1.000에서의 회복의 열 배로 회복된다. 따라서 인증서는
이 분리도에서 기하가 검정력을 가졌음을 인증하는 것이지 인증된 셀들에 걸쳐 검정력이 균일하다고
인증하지 않는다.

이 상관을 통제된 사다리로 바꾸려 했고 실패했는데, 그 실패가 정보를 담고 있어 보고한다. 한 셀의
표현을 좁히면(상호정보 상위 k, k ∈ {5, 10, 20, 50}) 데이터셋과 프로브를 고정한 채 D가 단조로
움직인다 — delivery_eta 0.521 → 0.540 → 0.555 → 0.801, homecredit_default 0.721 → 0.796 → 0.833
→ 0.912, 전 표현에서의 0.999·1.000과 대비된다. 그러나 좁혀진 셀은 라우팅 임계값 *아래로* 떨어져
주입 컨트롤이 아예 실행되지 않고, 따라서 읽을 회복이 없다: D를 움직이는 손잡이가 검정력을 재는
영역 밖으로 셀을 밀어낸다. 셀 내부 회복 사다리는 그러므로 미측정으로 남으며, 우리는 상관을
상관으로 진술한다. (기록을 위해: 사전 예측은 D가 k와 함께 하락한다는 것이었고 실제로는 상승했다.
회복 예측은 방금 말한 이유로 측정 불가였다.)

사다리가 보여준 것이 둘 있다. 고정된 셀에서 표현만 바꿔도 denoised 판독 자체가 움직인다
(delivery_eta −0.002 → −0.013; homecredit_default는 D = 0.721로 식별가능 영역에 들어오는 k = 5에서
+0.013 [+0.010, +0.016], k = 50에서 −0.010) — 즉 표현 선택이 셀의 식별가능성 *과* 거기서 읽히는
부호를 함께 정한다. 그리고 인접한 저-D 영역의 그림은 고-D 패널과 반대다: river 앵커의 D ≈ 0.49
에서는 네 신호 패밀리가 모두 회복하며(+0.076 ~ +0.238, 전부 0.916–0.968로 학습가능), 여기에는
§6.3에서 고-D일 때 실패하는 부분모집단-국소 패밀리도 포함된다. 표현 의존성은 이 문헌의 estimand에
알려진 위험이고, 여기서는 실측된 위험이다.

### 6.3 패밀리 상대성: 인증서는 규칙 변화의 클래스에 상대적이다

주입 인증서는 기하가 *어떤* 심은 규칙을 나를 수 있었다고 말한다. *임의의* 규칙을 나를 수 있다고는
말하지 못하며, 그 차이는 측정 가능하다. 같은 윈도우에서 주입을 신호 패밀리에 걸쳐 스윕하면:
weather의 전 배포 구간에서 저분산 회전은 +0.128로, 상호작용-매개 규칙은 +0.046으로
회복하며 둘 다 엄격 규칙을 통과한다 — 반면 부분모집단-국소 규칙은 윈도우 내에서 R² 0.560
으로 여유 있게 학습가능한데도(게이트는 0.20) −0.050으로 회복에 실패하고, 확증 시드에서
재현된다(R² 0.523, −0.042).

그 조합이 요점이다. 학습가능성과 회복가능성이 갈라진다: 계기는 피처 *방향*의 변화에는 검정력을
갖고 부분모집단 *소속*의 변화에는 갖지 못한다. 같은 기하에서, 같은 강도로.

이 맹점은 패밀리 단독이 아니라 패밀리 *와* 기하의 성질이며, 앵커 스윕이 그것을 못박는다: 분리도가
낮은(D ≈ 0.49) 단발 전환 river 앵커에서 부분모집단-국소 패밀리는 세 앵커 모두에서 회복한다
(+0.119, +0.079, +0.076, 학습가능 0.937–0.963). 나머지 셋도 마찬가지다(+0.135 ~ +0.238). 그리고
그것은 네 패밀리 중 일관되게 가장 작은 회복이다 — 분리도가 오를 때 가장 먼저 떨어지는 패밀리
이고, 고-D 산업 기하에서는 이미 떨어진 패밀리다. 따라서 인증서의 범위는 어떤 규칙 패밀리를 어떤
분리도에서 시험했는가에 대한 결합 진술이며, §7은 둘 다 보고한다.

그러므로 이 논문의 모든 "verified no-concept"은 명시적 패밀리 분모를 달고 다니며, 산업 패널에서
성립하는 두 인증서(weather, homesite_insurance)는 실제로 시험된 패밀리에 대해 성립한다 — weather는
저분산 캐리어에서 신선한 시드에 +0.183 / +0.193, 상호작용 패밀리에서 +0.083 / +0.078로 회복한다.

### 6.4 지표 상대성: 외부에서 문서화된 규칙 변경으로 반증됨

위의 세 축은 계기로 잰 계기다. 이 축은 바깥에서 정해지며, 우리 도구에 대해 우리가 할 수 있는 가장
날카로운 말이다.

ACA 메디케이드 확대는 법으로 문서화된 규칙 변경이다: 펜실베이니아는 2015-01-01에 시행했고 텍사스는
채택하지 않았다. 2014–2018 ACS 공적보험 과제에서 그 효과는 원자료에 보인다 — 펜실베이니아 양성률이
0.234 → 0.268 → 0.293 → 0.305 → 0.306으로 시행 연도에 계단을 만들고, 텍사스는 내내 0.183–0.185에
머문다.

**계기는 두 주 모두 null로 읽었다.** 펜실베이니아: D = 0.534 — 게이트에 의한 기권이 아니라 완전
식별가능 — gate 0.91, raw −0.011 [−0.011, −0.010], denoised −0.009 [−0.010, −0.008]. 텍사스:
D = 0.523, raw −0.015, denoised −0.011. 그리고 펜실베이니아 null은 이 논문 어디보다 타이트한 검출
하한(δ = 0.00083)을 달고 있어, 검정력 부족이 아니라 확신에 찬 0이다 — 이는 실패를 더 무르게가
아니라 더 날카롭게 만든다.

기제는 실행 전에 등록됐고 실행이 확인해 준다. 우리의 이진 지표는 AUC이고 순위 기반이라, 자격
임계값은 아무의 순서도 바꾸지 않은 채 많은 질량을 판정 경계 너머로 옮길 수 있다. 같은 행을
proper score로 재면 처치-대조 분리가 AUC에서의 3.1×에서 Brier 6.3×, log-loss 6.6×, 규칙-이동
발산에서 15×로 넓어진다. 그러나 커지지는 않는다: 모든 판독이 두 계기의 판정 floor 아래에 머물고
대조주도 placebo-유의 격차를 내므로, 해석 가능한 것은 처치/대조 비뿐이다. 정직한 진술은 따라서
"지표가 실패를 설명한다"가 아니라 "지표가 신호를 6~7배 압축하며, 압축을 풀어도 이 정책 변화는
우리 floor 아래에 있다"이다.

**맹점은 국소화되며 수리 방향은 측정 가능하다.** 계기 자신을 proper score로 재실행하면 — 진단
전용이다. 지표를 바꾸면 estimand가 바뀌고 AUC로 보정된 floor가 무효가 되므로, 실행 전에 고정한
판독 규칙은 판정 라벨을 버리고 팔 크기와 PA-대-TX 대비만 읽는다 — 펜실베이니아 판독의 부호가
뒤집힌다:

**표 4.** 세 지표로 잰 ACS 공적보험 셀. 진단 전용이며, 지표를 바꾸면 estimand가 바뀌고 AUC로 보정된 floor가 무효가 된다.
| 주 | AUC (raw / denoised) | Brier | log-loss |
|---|---|---|---|
| 펜실베이니아 (확대) | −0.011 / −0.009 | −0.005 / **+0.002** | −0.018 / **+0.021** |
| 텍사스 (미채택) | −0.015 / −0.011 | −0.005 / −0.002 | −0.023 / **+0.006** |

denoised 팔은 두 proper score 모두에서 처치주에 대해 양으로 돌아서며, log-loss에서 +0.021 —
판정 floor의 크기 — 에 이른다. 대조주는 +0.006으로, 방향이 맞는 3.5× 대비다. raw 팔은 모든 지표에서
음수를 유지한다. 즉 실행 전에 등록한 기제가 외부 렌즈에서만이 아니라 계기 자신에서도 확인된다:
프로브가 볼 수 없었던 것은 *지표 때문에* 볼 수 없었다.

이것이 허락하지 않는 세 가지를, 흡수하지 않고 진술한다. proper-score 팔에는 인증서가 없다 —
그 실행에 심은 주입이 학습불능이다(학습가능성 −0.185). 대조주도 log-loss에서 양으로 움직이므로
(+0.006) 해석 가능한 것은 비뿐이고 절대값은 결코 아니다. §6.4의 정직한 요약은 여전히 "지표가
6~7배 압축하고 잔여는 그래도 floor 아래"다. 그리고 +0.021이 "0.02를 넘는다"는 것은 AUC 단위로
보정된 상수와의 비교라 판정이 아니다. 기록하되 상수는 그대로 둔다.

따라서 범위 문장은 절을 하나 얻되 힘은 잃지 않는다: "ACS에 drift가 없다"가 아니라 "이 계기는
순위 기반 지표로는 이 크기의 실제 규칙 변경을 보지 못하며, log-loss로는 floor 크기의, 인증되지
않은, 대조군과 공유되는 무언가를 본다"이다.

두 가지 귀결을 따르며 둘 다 받아들인다. §7의 모든 null은 순위 기반 지표로 범위가 매겨진다 —
랭킹이나 트리아지 시스템에는 맞는 범위이고, 바로 이 과제가 대표하는 고정 임계값 자격 시스템에는
틀린 범위다. 그리고 심은 효과와 no-drift 앵커로 보정된 0.02 판정 floor는 실제 인구 규모 정책
규칙 변화보다 크다. 이를 그 상수에 대한 실측된 비판으로 기록하되(부록 C) 조치하지 않는다.
결과를 본 뒤 사전등록된 임계값을 바꾸는 것이야말로 이 논문이 반대하는 일이기 때문이다.

범위 진술은 고정돼 있고 우리는 그것을 지킨다(`PREREG_ACS_EXTENSION` §6(b)): 이 발견은 "ACS에
drift가 없다"가 아니라 "이 계기는 이 크기의 실제 규칙 변경을 보지 못한다"이다. 사후에 데이터
탓으로 재해석하지 않는다.

---

## 7. 적용: 산업 패널 감사

여기까지는 전부 계기에 관한 것이다. 이 절은 그것을 애초에 만들게 만든 질문에 쓴다: 산업 표
데이터에 배포된 모델이 착취할 수 있는 규칙 변화가 있는가? 답은 지도이고, 그 지도는 정확히 §6이
말하는 만큼의 값어치다 — 아래의 모든 수치는 트리-앙상블 프로브 클래스로(§6.1), 인증서를 얻은
분리도에서(§6.2), 그 인증서가 시험된 규칙 패밀리에 대해(§6.3), 그리고 순위 기반 지표로(§6.4)
범위가 매겨진다. 한정어를 결과 뒤가 아니라 결과와 함께 진술한다.

### 7.1 프로토콜과 결과

TabReD 8개 데이터셋(Rubachev et al., 2025; 공식 시간 분할의 train 구간), Electricity, INSECTS
(incremental-balanced) — HGB 프로브, K = 10, 탐색 시드 10개(0–9), 이어서 신선한 시드로 확증
재실행(100–109); 둘 사이에서 움직이는 판정은 불안정으로 보고하고 주장에서 배제한다. 판정
캐스케이드·임계값·시드 프로토콜·집계 판독은 실행 전에 커밋됐다. 과거 v2 산업 양성은 예측을 동반한
철회 후보로 사전등록됐고(NOISE-DRIFT-CONFOUNDED 또는 denoised-null), 예측이 빗나갈 경우를 위한
생존 배터리도 사전 명세됐다 — 무슨 일이 있었는지는 §3.2다. 일차 규칙(CI > 0 ∧ 평균 > floor)과 엄격
규칙(CI > floor)을 모두 계산하고 규칙-민감 셀을 표지한다. 10셀 전부 확증-안정이다.

**표 5.** 감사 패널. 판정은 탐색·확증 시드에서 동일하고, 인증서는 각 null이 무엇만큼의 값어치인지 밝힌다.
| 데이터셋 | 판정 (= 확증) | raw | denoised | gate | D | 회복 | 인증서 |
|---|---|---|---|---|---|---|---|---|
| insects | **DEPLOYMENT-CONCEPT** | +0.135 / +0.129 | **+0.152 / +0.145** | 1.24 | 0.844 | +0.162 | 주입 회복 |
| sberbank_housing | **NOISE-DRIFT-CONFOUNDED** | +0.024 / +0.033 (발화) | **−0.015 / −0.011** | **2.11 / 2.21 (발화)** | 1.000 | — | 진단됨 (§3.2) |
| cooking_time | INJECTION-RECOVERED | −0.011 | −0.018 | 0.92 | 0.966 | **+0.546** | **verified no-concept** |
| delivery_eta | INJECTION-RECOVERED | −0.012 | −0.014 | 0.89 | 0.999 | **+0.332** | **verified no-concept** |
| maps_routing | NO-STRONG-CONCEPT | −0.008 | −0.010 | 1.01 | 0.578 | n/a | 식별가능 영역 |
| elec2 | UNIDENTIFIABLE | +0.001 | +0.001 | 0.66 | 1.000 | +0.018 | **blindness earned** |
| ecom_offers | UNIDENTIFIABLE | −0.001 | −0.007 | 1.25 | 1.000 | — | *공허* — 주입 학습불능 (0/4 패밀리) |
| homecredit_default | UNIDENTIFIABLE | −0.007 | +0.004 | 1.29 | 1.000 | — | *공허* (0/4 패밀리; 프록시 80개 제거) |
| weather | INJECTION-RECOVERED † | −0.012 | −0.011 | 0.93 | 0.994 | **+0.183** | **verified no-concept** (학습가능 2/3 패밀리) |
| homesite_insurance | UNIDENTIFIABLE-INERT | −0.004 | −0.002 | 1.14 | 1.000 | −0.001 | **blindness earned** (2/2 패밀리, 안정) |

*D* = group-aware 분리도 중앙값(D\* = 0.96이 주입 컨트롤로 라우팅), *회복* = 기준 강도로 심은 규칙의
회복된 staleness, "—" = 시도한 모든 패밀리에서 주입이 학습불능이라 회복 수치가 허용되지 않음,
*n/a* = 그 셀은 컨트롤로 라우팅되지 않음. 회복값은 시드 세트 간 어디서나 0.01 미만으로 움직인다.
† weather의 기준-패밀리 규칙은 그 기하에서 학습불능이며, 회복은 저분산 캐리어에서 측정돼 신선한
시드에 재현되고(+0.183 / +0.193) 상호작용 패밀리에서도 성립한다(+0.083 / +0.078). "(m/n 패밀리)"
카운트는 §6.3의 패밀리 분모이며, 인증서가 실제로 시험된 규칙 변화 클래스보다 강하게 읽히지 않도록
표에 인쇄한다.

**집계, 사전등록된 대로 그리고 그 값어치대로.** 트리-앙상블 클래스에 대해, 데이터셋별 검출 하한 위의
착취 가능한 mean-rule drift를 가진 산업 데이터셋 수는 감사 0/8이다. 정보를 주는 판독을 내는 셀
기준으로는 0/5이고, 분모를 밝혀 두는 편이 낫다. 세 셀이 서로 다른 두 이유로 빠지기 때문이다.
ecom_offers와 homecredit_default는 인증서를 얻지 못한다 — 심은 프로브 규칙이 시도한 모든 패밀리에서
학습불능이다 — 따라서 어느 방향으로도 증거가 되지 않는다. sberbank-housing은 애초에 null이 아니다:
§3.2의 진단된 노이즈 셀이며, 진단된 기제를 null로 세면 이중 계산이다. 남는 것은 verified no-concept
인증서 셋(cooking_time, delivery_eta, weather), earned-blindness 인증서 하나(homesite_insurance),
그리고 인증서가 필요 없는 식별가능 null 하나(maps_routing)다. 집계가 나오는 모든 곳에서 둘 다
보고한다: 앞의 것은 사전등록된 것이고, 뒤의 것은 그것의 값어치다.

세 판독은 각자의 문장을 받을 만하다. 유일한 concept 양성은 설계된-drift 스트림이며, 거기서는
denoised 팔이 raw 팔을 *넘어선다* — 라벨 노이즈가 제거되면 pseudo-label이 old 규칙을 깨끗하게
인코딩한다는, 진짜 규칙 변화의 지문이자 §3.1 채널의 거울상이다. 순진한 프로토콜이 내줬을 네 개의
맹목 주장 중 둘은 *공허*이며, 이는 학습가능성 게이트 없이는 보이지 않는 구별이다. 그리고 지도에서
가장 강한 셀은 직관과 반대로 verified no-concept 인증서 두 개다: 기하에 검정력이 실증적으로
있는데(+0.546 / +0.332로 심은 규칙 회복) 실제 staleness가 여전히 null이다. 그 검정력이 균일하지
*않다*는 것이 §6.2의 주제다.

### 7.2 배포 갭을 건너서

지도는 TabReD 공식 시간 분할의 train 구간을 덮는데, 그곳은 실무자가 적합하는 곳이지 모델이
평가받는 곳이 아니다. 여덟 셀 전부를 train·validation·test를 공유 정규화 타임스탬프로 이어 붙여
재실행하면 — 그래서 윈도우가 공식 분할이 떼어 둔 held-out 갭을 건너면 — 탐색 시드와 확증 시드
사이에서 8/8 판정이 동일하고 CONCEPT를 발화하는 산업 셀은 없다. 따라서 집계는 train 구간으로
재범위화되지 않는다.

세 셀이 파묻기보다 기록할 만하게 움직인다. sberbank의 raw 팔은 더 긴 구간에서 floor를 넘지
못하므로, §3.2의 노이즈 진단은 train 구간으로 명시 범위화된다. cooking_time의 분리도는 더 긴
구간이 그것을 올릴 것이라는 우리 기대와 반대로 D\* 아래로 *떨어져*(0.927) 인증서가 필요 없는
식별가능 null을 남긴다. 그리고 homecredit_default의 denoised 팔은 +0.018을 읽는다 — 판정
floor의 89%이자 이 논문 전체에서 양의 방향으로 가장 큰 수치 — 이고 여기서 인증서는 거부가 아니라
*획득*되므로, "측정되지 않음"이 아니라 "측정됐고 floor 아래"로 읽힌다. 그 셀은 이 패널이 가진
양성에 가장 가까운 것이며, 더 큰 N이나 더 촘촘한 윈도우의 후속이 가장 먼저 볼 곳으로 표지한다.

### 7.3 앵커와 감도 프로파일

null의 지도는 그것을 그린 계기의 감도만큼만 정보를 준다. 그래서 계기를 합성 river 스트림 23개
(SEA / Agrawal / STAGGER / Sine / Hyperplane; no-drift·단발 급변·점진·재발 변종)와 INSECTS 7개 변종
전부(실제 센서 데이터, 실험실 통제 온도 drift)에 돌렸다.

**단조·단발 규칙 전환은 발화한다: 9/9.** river: agrawal_abrupt +0.045, agrawal_gradual +0.047,
stagger_abrupt, sine_abrupt +0.031, hyperplane_incremental +0.112(게이트가 그 노이즈 성분을 올바르게
공동 표지), sine_reoccur2 +0.047. INSECTS: gradual-balanced +0.092, gradual-imbalanced +0.069,
incremental +0.135 — 발화하는 모든 셀에서 denoised ≥ raw이고 주입 양성 대조가 회복한다. 약한 전환
(SEA의 임계값 미세 조정)은 부호가 일관된 채 no-evidence 밴드에 떨어지며 각자의 검출 하한과 함께
보고된다.

**재발 레짐은 올바르게 침묵하며, 지문이 찍힌다.** INSECTS 급변 변종(진동하는 온도)은 *음의*
staleness(−0.070, −0.027)를 음의 recency gain(−0.058, −0.023)과 함께 읽는다: 테스트에 인접한
윈도우가 가장 오래된 윈도우보다 테스트를 *더 못* 예측한다. 음의 recency는 일방향 drift에서는 불가능
하며, 되돌아온 레짐의 서명이다. 이는 river의 재발 셀들과 INSECTS incremental-reoccurring에서도
재현된다. 이것은 결함이 아니라 범위 진술이다: 이 렌즈는 배포 질문 — 오늘 학습한 모델에 old
데이터가 해로운가? — 에 답하며, old 레짐이 재발할 때 old 데이터는 실제로 해롭지 않다. 이 렌즈로 만든
모니터는 재발 drift를 *검출하지* 못할 것이다. 대신 old 데이터를 보관해도 안전하다고 올바르게 말해
주고, 음의-recency 표지가 그 이유를 말해 준다. 감사된 패널에서 그 표지는 침묵한다: 모든 산업 셀이
recency gain ≥ 0을 읽는다(maps_routing +0.000에서 sberbank-housing +0.325까지). 이 표지는 평가
지평을 지배하는 규모의 재발만 인증한다 — 우리 자신의 sine_reoccur2 앵커는 최종 ~22%에서 초기 레짐
으로 돌아가며 recency +0.30을 읽는다 — 따라서 지평-지배적 재발은 배제하되 늦거나 짧거나 윈도우
이하로 주기적인 복귀에 대해서는 아무 말도 하지 않는다.

**no-evidence 밴드의 캘리브레이션.** no-drift 앵커 스트림 5개 중 2개가 판정 floor의 1/10–1/20에서
"CI-유의" sub-floor denoised 양성을 낸다: 겹치는 부분표본에서의 시드 수준 CI는 아주 작은 크기에서
반보수적이다. 따라서 이 논문 전체에서 sub-floor 밴드는 *증거 없음*으로 읽는다 — 앵커 스위트가
강제했고 사전등록이 기록한 캘리브레이션이다.

**외부 셀 둘.** 말웨어(EMBER, 2018-밀집 월별 윈도우)에서 계기는 인증서 등급 DEPLOYMENT-DECAY-
COVARIATE를 낸다: D = 0.834(식별가능), raw −0.008 [−0.009, −0.007], denoised −0.004, 게이트 조용,
recency gain +0.031로 floor 위, 검출 하한 0.0013. 말웨어의 시간적 열화는 실재하고 recency로 회복
가능하지만 커버리지 기인이며 — 새 패밀리가 등장하지 old 라벨이 썩지 않는다 — 렌즈는 정확히 그
구별을 그린다: old 데이터를 보관하고, 열화는 예상하고, 커버리지를 위해 재학습하라. *연도* 축의 ACS
소득(캘리포니아, 2014–2018)에서 계기는 지도에서 가장 검정력 높은 null을 낸다: NO-STRONG-CONCEPT,
raw −0.008, denoised −0.007(두 CI 모두 음수), 게이트 조용, D = 0.515, recency ≈ 0, 검출 하한
0.0008 — *공간* 축에서 Y|X-이동이 만연하다고 보고된 바로 그 과제에서다. 이동 유형을 정하는 것은
데이터셋이 아니라 축이다. 그 셀은 실데이터판 prior-shift 컨트롤도 통과한다: 인플레이션 하의
고정 $50k 임계값이 단조 양성률 램프(0.36 → 0.42)를 만들지만 계기는 그 prior drift를 concept으로
오독하지 않는다. 상보적인 ACS 판독 — 문서화된 규칙 변경이 보이지 않았던 쪽 — 은 §6.4이며, 둘은
함께 읽어야 한다: 계기는 이 과제에서 검정력이 충분한데도 그 위의 특정 종류 변화에 지표-맹목이다.

---

## 8. 토론

**이 논문이 무엇인가.** 계기 하나, 그 실패 해부학, 그 맹점의 실측 지도, 그리고 적용 하나. 우리가
방어하는 주장은 좁고, 우리 생각에 무게를 진다: 식별가능성 인증서 없는 drift-유형 귀속은 신뢰할 수
없다. 원리로부터의 논증이 아니라, 우리가 인증되지 않은 버전을 만들었고 그 출력 하나를 믿었으며
그것을 만든 기제를 나중에 지목했기 때문이다.

**한계.** 계기가 눈머는 네 축은 여기 나열하지 않는다 — 그것이 §6이며, 잴 수 있는 한계는 결과이므로
이 절에서 승격됐다. 여기 남는 것은 *연구*의 한계다.

(1) **규모.** 프로브는 팔당 N ≤ 6,000행으로 학습하는데 산업 모델은 그보다 몇 자릿수 큰 데이터로
학습한다. 훨씬 큰 N에서만 착취 가능한 규칙 변화는 여기서 보이지 않으므로, 모든 δ 하한과 두 집계는
프로브 규모에서의 진술이다. 1차 δ(N) 스윕은 가장 큰 null 셀 셋에서 팔 상한을 윈도우 기하의
천장(N ≈ 14k–24k, 헤드라인 규모의 최대 4×)까지 올린다: 모든 판정이 불변이고 floor로 향하는 추세도
없다. 그 너머의 생산 규모 N은 향후 과제이며, 거기서 지도가 바뀔 가능성을 일축하지 않고 표지한다.
규모는 두 번째 경로로도 들어온다: 두 팔의 크기가 다르므로(N 대 2N) 표본 크기 이득이 모든 staleness
판독에 접혀 들어간다 — 이진 셀에서는 무시할 만하지만 회귀에서는 판정 floor의 56%에 달해 지도의 회귀
5셀에서 검출 floor를 실질적으로 올린다. 그 null들은 이 변위를 흡수하지 않고 명시한 채 보고한다.

(2) **윈도우 기하.** 고정 초기 앵커와 함께 K = 10 롤링 윈도우: 윈도우 폭보다 훨씬 짧은 시간 규모의
drift는 평균으로 씻겨 나가고, §7.3의 재발 지문은 지평-지배적 복귀만 인증한다. 주입 컨트롤이 이를
부분적으로 재는데, 그 회복이 K에 따라 변하기 때문이다.

(3) **자연 발생 산업 양성의 부재.** 패널의 유일한 견고한 양성은 실험실 설계 drift다. 크기를 보정할
산업 셀을 찾지 못했다 — 그것은 이 지도의 발견이자 동시에 약점이며, 바깥에서 그것을 닫으려는 시도
(§6.4)는 대신 계기의 반증을 낳았다. 우리는 그것을 있었던 그대로 보고한다.

(4) **envelope 상수는 단일 노이즈 패밀리로 보정됐다.** 노이즈비 4.7의 기권 경계는 Gaussian
라벨-노이즈 null에서 실측된다(§3.3). heavy-tailed 라벨 노이즈는 *규율* — denoiser 자신의 편향이
유계가 아닌 곳에서 기권하라 — 은 물려받지만 그 숫자는 물려받지 못하며, 그 패밀리에 대해서는 다시
측정해야 한다. 우리는 이 상수를 추정량의 성질이 아니라 캘리브레이션 출처와 함께 진술한다(부록 C).

(5) **벤치마크 큐레이션.** TabReD는 시간 분할이 가능하도록 큐레이션돼 있다. 산업 데이터 일반으로의
외적 타당성은 우리가 밟기보다 표지하는 귀납적 도약이다.

(6) **딥 아키텍처.** 패널의 신경 프로브는 분리 배터리를 통과하지 못하고 카나리아로 실린다(§6.1).
생산 규모의 현대 딥 표 아키텍처는 미검증이다. 우리는 결론이 아니라 입증 책임의 방향을 주장한다:
프로브 클래스는 §5의 배터리를 통과함으로써 drift 판정 권한을 얻으며, 우리가 시험한 첫 신경 프로브는
통과하지 못한다.

**과정에 대하여.** 이 프로젝트의 장부에는 현재 결과 이전에 정밀 검토에서 녹아 없어진 양성 발견이
아홉 건 기록돼 있다. 아홉 번째 소멸이 §3.2이고, 측정 계기 자신이 진단한 유일한 건이다. 우리가
지지하는 방법론적 주장은, 마침내 안정된 결과를 낸 조합 — 실데이터 주장 전에 실행되는 적대적 null,
커밋 타임스탬프가 찍힌 예측을 동반한 사전등록 캐스케이드, 신선한 시드의 확증 재현, 가정 대신
인증서, 그리고 카나리아 프로브 — 이 그것이 막아 주는 소멸의 비용에 비해 싸다는 것이다. 이 논문
자신의 결과 둘도 그 규율이 있었기에만 존재한다: 실행 전에 등록된 예측이 실행으로 반증됐고(§6.4,
그리고 proper-score 후속에서 대조주의 거동), 재게이트가 잘못된 인터프리터로 실행된 배터리를
잡아냈다(§9). 우리는 소멸 기록을 포함해 감사 기록을 아티팩트의 일부로 공개한다.

**실무자가 가져갈 것.** 셋이다. 손실 기반 drift 판정은 old 윈도우의 노이즈 판독 없이 조치되어서는
안 된다. 사람들이 실제로 조치하는 크기에서 둘은 혼동 가능하기 때문이다. null은 검정력 인증서 없이
보고되어서는 안 된다. "아무것도 보지 못했다"와 "아무것도 볼 수 없었다"는 다른 문장이고 둘 중 하나만
증거이기 때문이다. 그리고 판정은 자신의 프로브 클래스·분리도·규칙 패밀리·지표를 달고 다녀야 한다.
넷 각각이 같은 바이트에서 판정을 뒤집을 수 있기 때문이다.

**향후 과제.** ACS 분석의 다중 주·2019년 이후 확장; denoised 팔의 클래스 불변성 조건(어떤 프로브
패밀리가 *애초에* 건전한 staleness 판독을 허용하는가?); 생산 규모의 δ(N) 스케일링 연구; 자체
보정된 floor를 갖는 proper-score 팔 — §6.4가 지표 맹점의 수리 방향임을 보이지만 자체 사전등록이
필요하다 —; 그리고 음의-recency 지문이 직접 조치 가능한 스트리밍 모니터로의 인증서 프로토콜 이식.

---

## 9. 재현성

전부 하나의 저장소에 있으며 리뷰용으로 익명화 링크된다. 계기는 sklearn만 쓰는 단일 스크립트이고,
모든 확률적 단계는 시드가 박혀 있으며, 모든 출력 아티팩트는 커밋 해시·argv·라이브러리 버전·UTC
타임스탬프를 담는다.

그 스탬핑은 의례가 아니고, 우리는 그것을 원리가 아니라 사고로부터 말할 수 있다. 배터리 실행 하나가
잘못된 인터프리터로 실행됐다. 셸 프롬프트는 의도한 conda 환경을 알리는데 `PATH`는 무관한 다른
프로젝트의 virtualenv를 가리키고 있었다. 판정은 통과했으므로 화면상 이상해 보이는 것은 없었다.
아티팩트에 기록된 라이브러리 버전이 실데이터 실행이 쓰는 환경과 맞지 않았고, 그 불일치만이 이것을
잡아냈다. 그 실행을 믿었다면 한 환경에서 인증된 관문이 다른 환경의 측정을 허가했을 것이다. 이후
모든 실행은 인터프리터를 절대경로로 호출하고 무엇을 하기 전에 버전을 단언한다.

**연산.** 이 논문의 모든 실행은 CPU 전용이며 — 계기와 모든 프로브 클래스가 scikit-learn 모델이고
GPU는 어디에도 쓰이지 않는다 — 공유 멀티코어 리눅스 서버 한 대에서 단일 노드로 실행됐다
(Python 3.11.15, scikit-learn 1.9.0, NumPy 2.4.6; 환경 freeze 커밋됨). 벽시계 시간은 커밋된 단계
로그로 재구성 가능하다: 주요 사전등록 단계들이 하루에 단계-병렬로 ≈17시간, 모델-클래스 패널이
≈2시간 추가, 선택 셀들이 이틀에 걸쳐 ≈13시간이며, 어떤 데이터셋 셀도 몇 CPU-시간을 넘지 않는다.

**관문의 재현성.** 합성 배터리는 바이트 재현 가능하다: 재실행하면 같은 환경에서 커밋된 아티팩트를
SHA 동일하게 재생성하고, 계기에 opt-in 진단 플래그 두 개가 추가됐을 때는 새 실행을 읽기 전에
배터리가 참조 환경에 대해 비트 동일하게 돌아올 것을 요구했다(§5). raw 팔의 RNG 스트림은 계기
버전 간 안정적이다 — v2 헤드라인 수치가 v3에서 비트 단위로 재현되며, 그것이 §3.2를 재굴림이 아니라
재해석으로 만든다. scikit-learn 릴리스 간 HistGradientBoosting의 정확한 비트 재현성은 주장하지
*않는다*. 시드 세트 간·HGB↔RF 간 판정 수준 안정성은 주장한다(각각 10/10, 10/10).

**사전등록.** 사전등록은 임계값·캐스케이드·시드 프로토콜·집계 판독을 동결하며, 결과는 git 이력이
순서를 강제하는 읽기 전용 절로만 덧붙는다. 기존 절은 결코 수정하지 않는다. 큐에 넣은 각 실험의
예측은 드라이버 스크립트 헤더에 실행 전 커밋되고, 이후 결과와 대조해 읽는다 — 빗나간 경우를 포함
해서다. 모든 서버 실행은 재개 가능한 단일 드라이버로 재현된다.

**공시 둘.** 첫째, 등록된 절차로부터의 이탈 하나: 패밀리 스윕은 구현 컨트롤 이후 사람의 체크포인트를
명세했는데, 그것을 실행한 배치가 체크포인트를 읽기 전에 네 신호 패밀리를 모두 계산해 관문이
실행-시점 필터가 아니라 판독-시점 필터가 되었다. 실제로 배제된 패밀리는 없었지만(컨트롤이 네 개
모두에서 회복했다) 순서가 커밋된 것과 달랐고, 기록은 그렇게 말한다. 둘째, 판정 상수 하나가 우리가
조치하지 않은 방향으로 틀렸음이 알려져 있다: §6.4는 0.02 판정 floor가 실제 인구 규모 정책 규칙
변화보다 크다는 것을 보인다. 이를 상수 부록에 기록하고 임계값은 그대로 둔다. 결과를 본 뒤
사전등록된 임계값을 바꾸는 것이 이 논문이 반대하는 바로 그것이기 때문이다.

**데이터 접근.** 출처별 접근과 라이선스 조건은 데이터 부록에 표로 정리돼 있다. TabReD는 Kaggle
인증과 대회별 규칙 동의를 요구하고, Electricity는 OpenML에서 가져오며, INSECTS와 river 패널은
`river`로 설치되고, EMBER-2018은 공개 아카이브에서 내려받아 의존성 없는 어댑터로 파싱한다.

---

## 부록 A. 데이터 접근과 라이선스

각 배포처 기준 2026-07-18 검증:

**표 A1.** 데이터 출처, 접근 경로, 라이선스 조건.
| 소스 | 파이프라인 내 접근 경로 | 라이선스 / 조건 |
|---|---|---|
| TabReD (산업 데이터셋 8개) | Kaggle, TabReD 전처리 도구 경유 | 대회별 Kaggle 규칙(동의 필요); TabReD 도구 Apache-2.0 |
| Electricity (elec2) | OpenML dataset 151 | OpenML 표기 "Public" |
| INSECTS (7 변종) | `river` 패키지로 fetch | `river` BSD-3-Clause; 데이터셋 출처 Souza et al. (2020) |
| river 합성 스트림 (SEA/Agrawal/STAGGER/Sine/Hyperplane) | `river`로 생성 | BSD-3-Clause |
| EMBER-2018 | 공개 아카이브 다운로드; 자체 무의존 파서 | 데이터 파일 MIT (ember *코드*는 AGPL-v3, 미사용) |
| ACS (folktables 브리지) | `folktables` | folktables MIT; ACS PUMS = 미국 센서스국 공개 데이터 |

## 부록 B. 강건성·경화, 그리고 외부 시험 하나

B.1–B.3은 사전등록 감사가 동결된 *이후* 실행되었고, 계기·캐스케이드·임계값은 불변이다
(커밋-스탬프 아티팩트); 사전등록 셀이 아니라 강건성 점검이다. B.4–B.6은 실행 전에
사전등록되었으며 — 판독 규정과 셀별 예측이 타임스탬프와 함께 실행 앞에 커밋되었다 —
어느 쪽으로 나왔든 그대로 보고한다. 어느 것도 임계값·캐스케이드·시드 프로토콜을 바꾸지 않는다.

**B.1 δ(N): N이 커져도 지도는 floor 쪽으로 움직이지 않는다.** 프로브의 헤드라인 스케일은
N ≤ 6,000이다(§8 한계 1b). 가장 큰 null 셀 3개를 arm 캡 {1,500, 24,000, 96,000}으로
재실행했다(각 10시드). 윈도우 기하가 실현 가능한 arm 크기를 제한하므로(N = min(|recent|,
|old|, cap), |window| ≈ n/K) 실현 상한은 homecredit_default ≈17,200(96,000 캡도 동일 N),
weather 24,000(캡-제한), maps_routing ≈14,300이다. 판정은 모든 N에서 불변이고, 어떤 읽기도
0.02 floor로 향하지 않는다:

**표 B1.** 가장 큰 null 셀 셋에서 팔 크기를 윈도우 기하의 천장까지 올린 결과.
| 셀 | 실현 N | raw staleness | denoised | 판정 |
|---|---|---|---|---|
| homecredit_default | 1,500 | −0.015 [−.018, −.012] | +0.007 [+.005, +.008] | UNIDENT (공허) — 불변 |
| homecredit_default | 6,000 (§7.2 표) | −0.007 | +0.004 | 불변 |
| homecredit_default | ≈17,200 (캡 24k) | −0.013 [−.016, −.011] | +0.006 [+.004, +.007] | 불변 |
| homecredit_default | ≈17,200 (캡 96k) | −0.010 [−.012, −.009] | +0.005 [+.003, +.006] | 불변 |
| weather | 1,500 | −0.017 [−.019, −.015] | −0.022 [−.024, −.020] | 불변 |
| weather | 6,000 (§7.2 표) | −0.012 | −0.011 | 불변 |
| weather | 24,000 | −0.005 [−.006, −.005] | −0.002 [−.002, −.002] | 불변 |
| maps_routing | 1,500 | −0.009 [−.010, −.009] | −0.017 [−.017, −.016] | NO-STRONG-CONCEPT |
| maps_routing | 6,000 (§7.2 표) | −0.008 | −0.010 | 불변 |
| maps_routing | ≈14,300 (캡 24k) | −0.008 [−.009, −.008] | −0.008 [−.009, −.008] | 불변 |

raw staleness는 세 셀 모두 모든 N에서 유의하게 *음수*다(old 데이터가 돕는다); 유일한
sub-floor 양수(homecredit의 denoised arm)는 판정 floor의 1/4 수준에서 11×의 N 범위에 걸쳐
평탄하고 범위 상단에서 축소된다; weather의 denoised arm은 아래에서 ≈0으로 수렴한다. 주입
인증서도 재현된다(homecredit/weather의 심은 규칙은 여전히 학습불능 — 공허; maps는 식별가능).
범위는 불변: K = 10 윈도우 기하가 실현 N을 여기서 ≈14k–24k로 제한하므로, 그 너머의 프로덕션
스케일 N은 미검으로 남는다.

**B.2 overlap이 성립하는 곳에서의 렌즈 간 일치.** 분리도가 overlap-기반 분해를 적용 가능하게
남겨두는 지도의 두 셀(maps_routing D = 0.58; ACS 브리지 D = 0.515)에서, WhyShift-식
within-overlap concept 분해(ess-게이트)를 배포 렌즈 옆에 나란히 돌렸다. 두 계기는 일치한다:

**표 B2.** overlap이 성립하는 두 셀에서 배포 렌즈와 overlap 기반 분해를 나란히 둔 결과.
| 셀 | 배포 렌즈 | within-overlap gap [CI] | 비고 |
|---|---|---|---|
| maps_routing (full, 986피처) | NO-STRONG-CONCEPT (den −0.010) | −0.003 [−.004, −.002] | 5/5 시드 measurable, ESS 93% |
| maps_routing (sparse MI@5/10/20/50) | 불변 | −0.005 … −0.005, 전부 ≈0 | 모든 표현에서 measurable |
| ACS CA 2014→2018 | NO-STRONG-CONCEPT (den −0.007) | −0.009 (placebo −0.008) | cov-AUC 0.68; gap ≈ placebo |

overlap이 성립하는 곳에서 overlap 렌즈와 배포 렌즈는 같은 판정을 돌려준다; 이 논문이 기록한
계기 불일치(§4–4)는 정확히 overlap이 무너지는 곳에서 발생한다 — 판정이 인증서를 지녀야 하는
이유다.

**B.3 표본크기 항, 논증이 아니라 측정.** staleness는 N개 최근 행으로 학습한 모델과 2N개
(최근 ∪ 옛)로 학습한 모델을 비교하므로, 관측되는 차이는 두 효과의 합이다: 학습셋이 두 배가
되는 *이득*(drift와 무관하게 staleness를 음으로 밀고)과 모순되는 옛 라벨의 *해악*(양으로 민다).
관측되는 것은 합뿐이다. red-team의 지적은 보고된 모든 staleness — 따라서 모든 δ — 가 첫 항만큼
어긋나 있다는 것이다. 이전 판본은 산문으로만 기록된 편향 스윕으로 답했다. 이제 계기가 arm별
*절대* 점수를 방출하므로, 규칙이 고정이고 동시에 옛 행이 최근 행과 동분포인 배터리 셀에서
이 항을 직접 잴 수 있다 — 그 셀에서는 관측된 차이가 곧 표본크기 항이다:

**표 B3.** 고정-규칙 배터리 셀에서 직접 측정한 표본크기 항.
| 배터리 셀 | task | recent → recent ∪ old | 크기 항 | floor 대비 | 윈도우별 (n = 25) |
|---|---|---|---|---|---|---|
| synth_stable | binclass (AUC) | 0.9895 → 0.9902 | **+0.00074** | 4% | SD 0.0016, 부호 혼재 |
| synth_covariate | binclass (AUC) | 0.9577 → 0.9583 | +0.00064 | 3% | SD 0.0019, 부호 혼재 |
| synth_covariate_mild | binclass (AUC) | 0.9631 → 0.9665 | +0.00340 | 17% | SD 0.0033 |
| **synth_reg_stable** | **regression (z-RMSE)** | −0.1332 → −0.1221 | **+0.01114** | **56%** | SD 0.0040, **25개 전부 양수** |

두 가지 읽기가 있고, 불리한 쪽을 먼저 적는다. 회귀에서 이 항은 실재하고 크다: 판정 floor의
56%이며 25개 윈도우 전부에서 양수이므로 단일-윈도우 아티팩트가 아니다. 진짜 규칙-변화 해악이
+0.012인 회귀 셀은 ≈0으로 관측되어 null로 읽힌다. 따라서 지도의 회귀 셀들(sberbank_housing,
cooking_time, delivery_eta, maps_routing, weather)의 검출가능 하한은 실질적으로 이만큼 올라가
있으며, 그 null들은 이 변위를 감안해 읽어야 한다(§8 한계 1b). 이진 분류에서는 무시할
수준이다 — floor의 3~4% — 그러나 얼버무리고 싶지 않은 단서가 있다: 배터리의 이진 셀은
AUC ≈ 0.99로 천장에 충분히 가까워 크기 이득이 압축되는데, 산업 이진 셀은 거기 있지 않다.
따라서 이진 하한은 측정된 그대로 이전되지 않는다. arm별 절대값은 이제 모든 실행에서
방출되므로, 같은 양을 다음 실행 때 산업 패널에서 배터리로부터 논증하는 대신 직접 측정한다.

**B.4 배포 갭: 지도는 그것을 건너도 움직이지 않는다.** 본 지도는 TabReD 공식 시간 분할의
train 구간을 다룬다(§8 한계 2). 여덟 셀 전부를 train+val+test를 공유 정규화 timestamp로 연결해
재실행했고 — 그러면 윈도우가 공식 분할이 hold-out 하는 갭을 가로지른다 — 탐색 후 확증 시드로
재확인했다(8/8 판정 동일):

**표 B4.** 배포 갭을 건너 재실행한 패널.
| 셀 | train-span 판정 | full-span 판정 | full-span raw / denoised | D |
|---|---|---|---|---|
| sberbank_housing | NOISE-DRIFT-CONFOUNDED | UNIDENTIFIABLE (raw 미발화) | +0.006 / −0.022 | 1.000 |
| cooking_time | INJECTION-RECOVERED | NO-STRONG-CONCEPT (D가 게이트 아래로) | −0.009 / −0.015 | 0.927 |
| delivery_eta | INJECTION-RECOVERED | INJECTION-RECOVERED | −0.014 / −0.016 | 0.999 |
| maps_routing | NO-STRONG-CONCEPT | NO-STRONG-CONCEPT | −0.007 / −0.011 | 0.630 |
| ecom_offers | UNIDENTIFIABLE *(공허)* | UNIDENTIFIABLE *(공허)* | −0.009 / −0.009 | 1.000 |
| homecredit_default | UNIDENTIFIABLE *(공허)* | UNIDENTIFIABLE *(**실명**)* | −0.003 / **+0.018** | 1.000 |
| homesite_insurance | UNIDENTIFIABLE-INERT *(불안정)* | UNIDENTIFIABLE-INERT *(**실명**)* | −0.004 / −0.001 | 1.000 |
| weather | UNIDENTIFIABLE *(공허)* | UNIDENTIFIABLE-INERT | −0.014 / −0.013 | 1.000 |

배포 갭을 가로질러도 산업 셀은 하나도 CONCEPT를 발화하지 않으므로 집계는 train 구간으로
재스코프되지 않는다. 셋은 묻지 말고 기록할 만하게 움직인다. sberbank는 더 긴 구간에서 raw arm이
floor를 못 넘어 §3.2의 노이즈 진단이 train 구간에 범위 한정된다. cooking_time의 분리도는 더 긴
구간이면 오를 것이라는 예상과 반대로 게이트 아래로 떨어져, 인증서가 필요 없는 식별가능 null이
된다. 그리고 homecredit_default의 denoised는 +0.018 — floor의 89%이자 이 논문 전체에서 가장 큰
양수 방향 수치 — 인데 인증서가 거부가 아니라 *실명*이라, "안 봤다"가 아니라 "재봤고 floor 아래"로
읽힌다.

**B.5 패밀리 × 캐리어: 인증서가 무엇에 대해 인증되었는가.** 기준 주입은 최고 분산 두 피처에
회전을 심는다. 시험하려던 네 규칙 기하 중 셋이 그대로면 같은 캐리어를 물려받았을 것이고 —
ecom/homecredit/weather의 주입 공허 원인으로 진단된 것이 바로 그 열들의 두꺼운 꼬리이므로 —
스윕은 규칙 기하와 캐리어를 교차한다: `topvar@hi`(기준), `lowvar@lo`, `interaction@lo`,
`subpop@lo`. 설계된-drift 스트림 위의 구현 대조가 네 조합 모두에서 회복하므로
(+0.201 / +0.211 / +0.156 / +0.077), 아래의 미회복은 코드가 아니라 셀의 성질이다.
회복된 staleness, "vac" = 주입 학습불능(사용 가능한 수치 없음):

**표 B5.** 신호 패밀리와 캐리어별 주입 회복.
| 셀 | topvar@hi | lowvar@lo | interaction@lo | subpop@lo | 판독 |
|---|---|---|---|---|---|---|
| cooking_time | +0.546 | +0.467 | +0.357 | +0.131 | 검증됨, 4/4 |
| delivery_eta | +0.332 | vac | +0.274 | vac | 검증됨, 학습가능 2/2 |
| elec2 | +0.018 | −0.003 | −0.001 | −0.002 | 실명 획득, 4/4 |
| homesite_insurance | vac | −0.001 | vac | −0.003 | 실명 획득, 학습가능 2/2 |
| weather | vac | **+0.183** | **+0.083** | 학습가능, **−0.009** | 검증됨, 학습가능 3개 중 2개 |
| ecom_offers | vac | vac | vac | vac | 인증서 없음 |
| homecredit_default | vac | vac | vac | vac | 인증서 없음 |
| insects (양성대조) | +0.162 | +0.265 | +0.229 | +0.047 | 회복, 4/4 |

기록을 넘는 결과가 둘이다. 첫째, weather의 기준-패밀리 규칙은 그 기하에서 학습불능인데
(윈도우 내 R² −0.022) 저분산 규칙은 R² 0.674로 학습가능하며 회복한다 — 단일 패밀리 프로토콜이면
미인증으로 남겼을 셀이 캐리어를 넓히자 인증되고, 그래서 인증 셀 수가 4가 아니라 5가 된다.
둘째, 반대 방향으로: 같은 셀의 full-span 변종에서 부분모집단-국소 규칙은 여유 있게 학습가능한데
(R² 0.560, 게이트 0.20에서 멀다) 여전히 회복에 실패하고(−0.050) 확증 시드에서 재현되는 반면,
저분산(+0.128)과 상호작용(+0.046) 규칙은 동일한 윈도우를 통해 회복한다. 따라서 인증서의
상대성은 우리가 주장하는 단서가 아니라 우리가 측정한 경계다: 이 계기는 피처 *방향*이 실어
나르는 규칙에는 검정력이 있고 *부분모집단 소속*이 실어 나르는 규칙에는 없다.

**B.6 계기가 보지 못하는, 외부 문서로 확정된 규칙 변화.** ACA 메디케이드 확대는 날짜와 범위가
데이터 밖에서 고정되고 채택이 주마다 다른 규칙 변화를 준다 — 즉 처치와 대조가 같은 과제·같은
연도·같은 계기 안에 존재한다. 펜실베이니아는 2015-01-01 시행, 텍사스는 2018년까지 미채택이다.
ACS 공적보험 과제(folktables, 2014–2018, 연 단위 윈도우)에서 설계는 데이터에 잡힌다 —
펜실베이니아 양성률이 0.234 → 0.268 → 0.293 → 0.305 → 0.306으로 시행 연도에 계단이 서고,
텍사스는 전 구간 0.183–0.185에 머문다:

**표 B6.** 배포 렌즈로 읽은 ACS 두 주.
| | 판정 | raw | denoised | recency | D | δ |
|---|---|---|---|---|---|---|
| 펜실베이니아 (처치) | NO-STRONG-CONCEPT | −0.011 | −0.009 | **+0.009** | 0.534 | **0.00083** |
| 텍사스 (대조) | NO-STRONG-CONCEPT | −0.015 | −0.011 | −0.001 | 0.523 | 0.00200 |

**B.7 배터리 위의 고전 스트림 탐지기: 발화율은 진실을 따라가지 않는다.** §2는 손실-스트림
탐지기가 구성상 유형-맹목이라고 논증한다. 여기서 그것을 측정한다. 표준 탐지기 4종(ADWIN·KSWIN·
DDM·PageHinkley)을 진실이 구성으로 고정된 배터리 셀의 prequential 오차 스트림(river 학습기) 위에
돌렸다. 값은 1,000 샘플당 감지 수, 3시드. DDM은 0/1 오차 스트림을 요구하므로 이진 셀에만 적용된다.

**표 B7.** 배터리 위의 고전 스트림 탐지기: 1,000 샘플당 감지 수, 3시드.
| 셀 | 진실 | ADWIN | DDM | KSWIN | PageHinkley |
|---|---|---|---|---|---|---|
| concept | 규칙 이동 | 0.167 | 0.278 | 0.000 | 0.194 |
| nuisance_proxy | 규칙 이동 | 0.111 | 0.139 | 0.028 | 0.195 |
| **concept + 노이즈 감쇠** | **규칙 이동** | 0.083 | **0.000** | 0.000 | 0.083 |
| reg_concept | 규칙 이동 | 0.083 | — | 1.556 | 0.584 |
| **reg_early_noisy** | **고정 규칙, 노이즈 감쇠** | **0.472** | — | 1.361 | 0.695 |
| **reg_xdep_noise** | **고정 규칙, 노이즈 감쇠** | **0.417** | — | 1.528 | 0.750 |
| stable | 고정 규칙 | 0.083 | 0.000 | 0.000 | 0.083 |
| **covariate** | **고정 규칙** | 0.111 | **0.139** | 0.000 | **0.278** |
| **reg_stable** | **고정 규칙(순수 null)** | 0.083 | — | **1.333** | **0.583** |
| reg_late_noisy | 고정 규칙 | 0.528 | — | 1.278 | **3.444** |

태스크가 다르면 학습기가 다르므로 태스크 안에서 읽어야 한다. 회귀에서 노이즈-감쇠 셀은 진짜
규칙-변화 셀보다 ADWIN을 *더 자주* 발화시키고, KSWIN·PageHinkley로는 3× 이내다 — 그런데 순수
정상 셀도 마찬가지이고(KSWIN 1.333, PageHinkley 0.583), 고정 규칙 셀 하나가 패널 최대의
PageHinkley 발화율을 낸다. 학습기가 고정된 이진 팔에서는 두 방향의 실패가 함께 보인다: 진짜 규칙
변화가 정상 셀과 정확히 같은 비율로 발화하고(미탐), 고정 규칙의 공변량 이동이 DDM·PageHinkley를
발화시킨다(오탐).

한계 둘을 흡수하지 않고 적는다. 회귀 오차 스트림에는 증분 학습기 자신의 학습 곡선이 섞여 있으므로
그 팔은 "발화가 진실을 따라가지 않는다"는 지지하되 "이 탐지기들이 노이즈 감쇠에 *특이적으로*
발화한다"는 단독으로 지지하지 못한다. 깨끗한 증거는 이진 팔의 미탐/오탐 쌍이다. 그리고 DDM의
단측성 — 오차 *증가*에 경고하는데 노이즈 감쇠는 오차를 *내린다* — 은 여기서 시험할 수 없었다.
배터리의 노이즈-감쇠 셀 둘이 모두 회귀이고 DDM은 이진 오차 스트림을 요구하기 때문이다. 이것을 본
뒤에 이진 노이즈-감쇠 셀을 추가하지 않았다. 이 탐지기들은 어느 것도 drift-유형 판정을 내지 않으므로
아무것도 지도에 들어가지 않는다. 요점은 발화가 유형 정보를 담지 않는다는 것뿐이며, 그것이 §2의
주장이다.

둘 다 null이다. 둘을 가르는 유일한 arm은 recency gain으로, 처치 주에서 양수이고 CI가 0을
제외하며 대조 주에서는 0과 구분되지 않는다 — 방향은 맞고, 여전히 floor 아래여서 판정은 움직이지
않는다. 이 null은 기본 행 솎기를 제거해도 유지되고, model-class 패널은 여섯 라벨 중 셋을
sub-floor 대역으로 옮기지만 어떤 판독도 바꾸지 않는다(대조 주의 선형-프로브 sub-floor 값이 실은
처치 주보다 *크므로* 그 패널에서는 아무것도 주장할 수 없다).

같은 행을 proper score로 다시 재면 미검출의 일부가 국소화된다. B.2의 within-overlap 렌즈를
깨끗한 pre/post 쌍(2014 vs 2018)에 순열 placebo와 15시드로 적용하면, placebo-보정 gap의
처치-대조 분리가 AUC에서 3.1×, Brier 6.3×, log-loss 6.6×, 규칙-이동 발산 15×다; 펜실베이니아는
지표 불변(네 지표 전부 양수)이고 텍사스는 아니다. 같은 렌즈 계열의 독립 실행이 시간 대비를
재현한다(+0.021 vs +0.003). 그러나 효과가 커지지는 않는다: 모든 값이 두 계기의 결정 floor 아래에
남고 대조군도 자체의 placebo-유의 gap을 내므로, 해석 가능한 것은 비율뿐이다. 따라서 결론도 그만큼
경계된다 — 순위 기반 점수가 이 신호를 6~7배 압축하며, 압축을 풀어도 이 변화는 우리 floor 아래에
남는다. 이에 따르는 범위 진술과 floor 상수에 대한 함의는 §8(한계 7)과 부록 C에 있다.

## 부록 C. 판정 상수: 캘리브레이션 출처와 유효 범위

캐스케이드의 모든 판정 상수를, 어디서 캘리브레이션되었고 어디서 유효성이 끝나는지와 함께.
계기는 숫자를 재사용하는 게 아니라 이 캘리브레이션 레시피(§5.1 배터리 + §7.3 no-drift 앵커)를
재실행함으로써 새 도메인에 이식된다.

**표 C1.** 모든 판정 상수와 그 캘리브레이션 출처·유효 범위.
| 상수 | 값 | 역할 | 캘리브레이션 | 유효 범위 / 밖에서의 거동 |
|---|---|---|---|---|
| 판정 floor | 0.02 | CONCEPT 크기 문턱 (AUC/accuracy/z-RMSE 공유) | 배터리 concept 크기(심은 효과 = floor의 10–27×); 지표별 재스케일에 셀 이동 0 (§5.1) | sub-floor CI-양성 = 무증거 대역 (no-drift 앵커로 캘리브레이션; 2/5가 floor의 1/10–1/20에 착지). **실측 비판:** 실제 인구 규모 정책 규칙 변화보다 크다 (§8 한계 7) — 아래 주석 참조 |
| 노이즈 게이트 | 1.5 | 라벨-노이즈-drift 플래그 (old 프록시/recent 중앙값) | 안정 합성 컨트롤 0.75–0.99; 노이즈-drift 셀 3.5–3.9 | 실데이터 값 전부 1.5 미만 또는 2.0–2.9 — envelope 근처 없음 |
| envelope | 4.7 | denoiser 유효 경계; 위에서 기권 | 고정-규칙 null 실측: 4.72에서 +0.014, 5.71에서 +0.026(floor 돌파) | Gaussian 패밀리 한정; heavy-tail은 기권 규율만 상속 (§8 한계 4) |
| D* | 0.96 | 주입 컨트롤로의 분리도 라우팅 | group-aware·크기맞춤 수리 (중복 0.994→≈0.50; shuffle-D 0.94→0.50) | 라우팅 통계량일 뿐, support overlap 아님; D ≥ D*는 단독으로 아무 결론 없음 |
| 학습가능성 게이트 | AUC 0.65 / R² 0.20 / acc 다수+0.10 | 주입-인증서 유효성 | junk-기하 스윕 실행: 꼬리가 두꺼워질수록 학습가능성이 게이트를 건너고(0.944 → 0.839 → **0.482** → 0.477) 인증서는 정확히 그 지점에서 공허가 된다(회복 +0.186 → +0.150 → **+0.012** → −0.002). 학습가능 컨트롤은 0.951 → 회복 +0.201 | 학습불능 ⇒ 그 패밀리에 대한 인증서 거부 — 실명이 아니라 공허 |
| 주입 강도 | 2.5 rad | 기준 회전 크기 | 학습가능 기하에서 floor 통과 (회복 +0.16 ~ +0.55) | 패밀리 스윕 실행됨(부록 B.5): 저분산·상호작용 규칙에 대해 인증, **부분모집단-국소에 대해 반증** (§8 한계 6) |
| 시드 프로토콜 | 10 (0–9) + 확증 (100–109); 배터리 5 | 검정력·안정성 | 배터리 효과가 floor의 10–27×라 5로 충분한 pass/fail 관문 | 시드 집합 간 판정 이동 시 배제 (unstable) |

**판정 floor에 관한 주석 (기록하되 조치하지 않음).** 사전등록은 floor가 실제 정책 규모의 규칙
변화보다 크다고 판명되면 그 사실을 이곳에 상수에 대한 실측 비판으로 남기도록 규정했다.
그렇게 되었다. ACA 메디케이드 확대는 감사 대상 인구에서 펜실베이니아의 공적보험률을 7.2pp
움직였고 계단은 시행 연도에 섰는데, 우리가 돌린 어떤 지표·어떤 프로브 클래스에서도 0.02를
넘는 읽기를 내지 못했다(§8 한계 7; 부록 B.6). floor는 그 값의 10–27× 크기의 심은 효과와
1/10–1/20 크기의 no-drift 앵커로 캘리브레이션되었고, 이 사건은 두 캘리브레이션 영역 사이의
빈 구간에 떨어진다 — 결정 상수가 가장 방어하기 어려운 지점이다. 우리는 그것을 바꾸지 않는다:
뒤집었을 결과를 본 뒤에 사전등록 임계값을 고치는 것이 이 논문이 반대하는 관행이다. 대신 따라오는
것은 범위 진술이다 — 이 논문의 모든 CONCEPT 판정은 "대규모 실제 정책 변화가 도달하지 못하는
크기 이상"을 뜻한다 — 그리고 계기를 이식하려는 사람을 위한 구체적 재캘리브레이션 표적이다:
floor를 심은 회전과 null 스트림만이 아니라 대상 도메인의 문서화된 규칙 변화에 앵커하라.

---

## 참고문헌

영문본(PAPER_DRAFT_V4.md §References) 참조 — 서지 정본은 영문본에서 관리 (검증 완료
2026-07-15; BibTeX = `paper/main.bib`).
