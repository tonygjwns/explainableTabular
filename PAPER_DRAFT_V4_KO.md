# 산업 표 데이터에 착취 가능한 concept drift는 존재하는가? 사전등록된 식별가능성 감사

> DRAFT v4.6 국문본 (2026-07-18; 리뷰어-2의 장부 채점 대응 — §6 지문 주장을 지평-지배 재발로
> 재스코프(sine_reoccur2 = 논문 내 반례; sub-window 주기성 미커버 명시), 부록 C 판정-상수 표,
> PREREG §14에 [C]/[D] 판독 규정 사전 커밋. v4.5 — 리뷰어-2 적대 리뷰 대응: 초록 0/8 볼드에
> 트리-앙상블 스코프,
> "주장하지 않는 것"에 딥-아키텍처 비판정 문장, §6에 재발-지문 산업 셀 전부 침묵 문장(전 셀
> recency ≥ 0, +0.000…+0.325), §3.2+§7(6) 주입 인증서의 reference-family 상대성, §5.3 sub-floor
> 헤지, §6 EMBER "인증서-등급 = 식별가능성" 명확화. 서버 실험 2종(--tabred-span full /
> --inj-family sweep) 구현·스모크 완료. 라운드 4(조판본 검증, "사실상 제출 가능") — 고아
> 참고문헌 3건 본문 인용 복원(Johansson→§2 / Moreno-Torres→§3.1 estimand / Vela→§3.1 decay),
> Fig.2 밴드 2행화. 라운드 3 — §2 EMBER 수치를 인증서급 셀로 통일
> (−0.008/0.0013; −0.012/0.0014는 대체된 전체-이력 판독), 초록 인증서 회계 정밀화, §1 패널
> 회계 완결, §2 검출기 head-to-head 범위 문장, §3.1 수위 완화, §4.1 5-시드 사유, Fig.2 색각
> 문구. 라운드 2 = 초록 압축·명제 제거·컴퓨트/라이선스·각주들·Figure 2·부록 B).
> **영문 정본 = PAPER_DRAFT_V4.md** (인용·수치의 정본은 영문본과 커밋된 아티팩트:
> `prereg_results/`, `audit_artifacts_2026-07-04/`, `PREREG_DEPLOYMENT_V2.md` §0–10).
> **LaTeX 정본 = `paper/main.tex`** (TMLR 템플릿, tectonic 컴파일 검증 완료, 표 4개+Figure 1 포함);
> Figure 1 벡터 소스 = `paper/figures/fig1_cascade.tex` (컴파일 확인됨). 서지 검증 완료(2026-07-15).

---

## 초록 (Abstract)

표 데이터셋이 겪는 시간적 변화가 어떤 *종류*인지 — 규칙 P(y|x)가 변하는가, 공변량 P(x)가
움직이는가 — 는 거의 측정되지 않으며, 우리는 그 판정이 측정 도구에 의존함을 보인다. 우리는
배포-네이티브 프로브인 *staleness harm*을 연구한다: 고정된 최근 학습 집합에 과거 예제를
추가하면 미래 성능이 나빠지는가? 실행된 세 가지 실패 모드가 소박한 프로브를 신뢰 불가로
만든다. 규칙이 고정임이 증명된 상황에서 라벨-노이즈 감쇠만으로 실제 산업 양성과 통계적으로
일치하는 "concept drift" 판정이 발급되고(+0.021 vs +0.024); null을 인증해야 할 분리도
게이트는 중복 행과 entity cohort만으로 포화하며; concept/covariate 분리는 가설 클래스
상대적이다 — 트리 앙상블이 올바르게 읽는 곳에서 kNN·선형 프로브는 오발한다. 우리는 계기를
수리하고 — 노이즈 게이트와 실측 기권 envelope를 동반한 교차적합 *denoised staleness* arm,
group-aware 분리도, 학습가능성-게이트 주입 인증서 — 규칙 변화·노이즈 drift 공존 케이스를
포함한 14-셀 사전등록 배터리로 검증한다. 8개 산업 데이터셋(TabReD)의 사전등록 감사 + 확증
fresh-seed 재현(10/10 안정)은 검출 표가 아니라 식별가능성 *지도*를 낳는다: **어떤 산업
데이터셋도, 배포된 트리-앙상블 클래스에 대해, 검출가능 하한을 넘는 착취 가능 mean-rule
drift를 보이지 않는다(감사 8셀 중 0/8 — 그리고 식별가능성이 인증된 셀만 세면 0/5이며, 나머지
둘은 음성이 아니라 무정보다)**; 유일한 견고 양성은 설계된 drift이고; 유일했던 과거 산업 양성은 계기 스스로에 의해 라벨-노이즈
감쇠로 *진단*되며; 남은 셀은 모두 식별가능 null이거나 인증서 판정 — 검증됨, 실명, 거부,
또는 (한 셀) 불안정 — 을 지닌다. 앵커
스트림들이 감도 프로파일을 고정한다: 단조·단발 규칙 전환은 9/9 발화하고, 재발 레짐은 음의
recency 지문과 함께 올바르게 침묵한다. 우리는 계기·배터리·감사 추적을 공개하며, 식별가능성
인증서 없는 drift 유형 귀속 — 현재 drift 모니터링의 기본값 — 은 신뢰할 수 없음(unreliable)을
논증한다.

---

## 1. 서론

산업 표 데이터의 시간 기반 분할(TabReD; Rubachev et al., 2025)은 정교한 시간-인지 딥러닝
방법들을 무너뜨리는 반면 그래디언트 부스팅 트리는 살아남는다. 시간-인지 아키텍처의 표준 동기는
*concept drift*다: 라벨링 규칙 P(y|x)가 시간에 따라 변한다면, 시간으로 인덱싱·검색·변조하는
모델이 시간을 단순 피처로 받는 모델을 이겨야 한다. 그러나 이들 벤치마크에서 규칙이 실제로
변하는지 — 고정된 규칙 아래 공변량 P(x)만 움직이는 것과 구별하여 — 는 측정된 적이 없고, 우리의
중심 주장은 그 측정이 필드가 가정하는 것보다 어렵다는 것이다: **concept/covariate 판정은
데이터의 성질인 만큼이나 측정 도구(그 가설 클래스, 노이즈 가정, 윈도우 기하)의 성질이다.**
우리는 측정 자체를 연구 대상으로 삼되, 배포된 시스템이 실제로 직면하는 설정 — 과거로 학습하고
미래를 예측하며 old 데이터를 어떻게 할지 결정하는 — 에서 그렇게 한다.

**프로브.** 우리는 *staleness harm*이라 부르는 배포-네이티브 양에서 출발한다:

> staleness = score(recent N으로 학습) − score(recent N ∪ old N으로 학습), 미래 윈도우에서 평가

를 K개 timestamp-값 윈도우 위 rolling-origin으로 계산한다. 두 arm의 recent 부분이 동일하므로
공변량 *커버리지*는 구성적으로 상쇄된다: 유일한 차이는 N개의 추가 old 예제다. 규칙이 고정이면
old 예제는 옳은 라벨을 담고 있어 추가가 해가 될 수 없어야 하고; 규칙이 변했으면 old 라벨이 현재
규칙과 모순되어 적합을 오염시킨다 — staleness > 0. 이 구성은 (동일한 역학을 *적응*에 쓰는 학습
윈도우 선택과 달리; Klinkenberg & Joachims, 2000) old 데이터가 돕는지/해치는지의 *이유*를
귀속하며, importance weight도 밀도비도 윈도우 support 간 겹침도 필요로 하지 않는다 — 이것이
overlap 기반 분해가 퇴화하는 산업 피처 공간(D'Amour et al., 2021; Cai et al., 2023)에서 이
프로브가 매력적인 정확한 이유다.

**실행으로 확인된 세 가지 실패 모드.** 이 논증의 소박한 형태 — "옳은 라벨은 도움만 될 수 있다"
— 는 유한 용량 ERM에서 거짓이며, 우리는 그것이 조용히 실패하는 세 가지 서로 다른 방식을, 각각
프로브가 오독하는 구성된 null로 보인다:

1. **라벨-노이즈 drift (이 논문을 촉발한 진단).** 조건부 평균이 *고정*이고 라벨 노이즈 표준편차가
   시간에 따라 1.5→0.3으로 줄어드는 회귀 스트림이 staleness +0.021 [+0.020, +0.023]로 "concept"
   판정을 발급한다 — 우리가 이전에 얻었던 실제 산업 양성(sberbank-housing, +0.024
   [+0.018, +0.030])과 통계적으로 구별 불가능한 크기다. old 라벨은 *틀릴* 필요가 없다;
   *더 시끄럽기만* 해도 충분하다 — 비가중 ERM은 이분산 하에서 효율적이지 않기 때문이다. 두 번째
   채널 — 스케일이 감쇠하는 x-의존 노이즈 — 는 똑같이 고정된 규칙에서 raw 프로브를 +0.025로
   발화시킨다.
2. **overlap-게이트 포화.** "여기서의 null은 무정보"의 자연스러운 인증서는 윈도우 분리도 AUC다
   (분류기가 old 행과 future 행을 구별할 수 있는가?). 행 단위 train/test 분할로 계산하면 이
   게이트는 정확한 중복, 근접 중복, entity cohort 구조만으로 — 공변량 변화가 *전무*해도 —
   0.994~1.000까지 포화한다: 분류기가 분포가 아니라 행을 암기하는 것이다. 소박한 게이트 아래
   실데이터 10개 중 8개가 정확히 1.000으로 읽혔다.
3. **가설 클래스 상대성.** 프로브의 모델 클래스를 바꿔 자체 ground-truth 스위트를 재실행하면
   concept/covariate *분리*가 프로브 설계의 성질이 아님이 드러난다: kNN 프로브는 순수 공변량
   변화를 concept drift로(+0.098), 선형 프로브는 prior 변화를 concept drift로(+0.026) 읽는
   반면 HGB와 랜덤 포레스트는 모든 컨트롤을 통과한다. 이는 기지 이론(Shimodaira, 2000; Hinder
   et al., 2023)의 경험적 얼굴이다: misspecification 하에서 ERM 최적해는 학습 공변량 분포에
   의존하므로, "old 데이터가 해친다"는 기하만으로도 제조될 수 있다.

**수리된 계기.** 각 실패 모드는 그 자체로 실행-검증된 수리를 받는다(§3–4): old 라벨을 old 윈도우
내부 교차적합 모델 예측으로 대체하는 *denoised staleness* arm — 노이즈-단독 drift에서는
pseudo-label이 근사적으로 옳아 해악이 소멸하고, 규칙이 변했으면 여전히 옛 규칙을 인코딩해
해악이 잔존한다 — 과, 그 너머에서 계기가 기권하는 유효 범위가 명시적으로 지도화된 윈도우별
*노이즈 게이트*; 메모리제이션 채널은 죽이되 정직한 drift는 남기는 group-aware·크기맞춤 분리도
추정; 그리고 "이 데이터셋은 측정 불가"를 가정에서 입증으로 바꾸는 *학습가능성-게이트 주입
컨트롤* — 기지의 규칙 회전을 데이터셋 자신의 공변량 기하에 심고, 윈도우 내에서 학습 가능함을
확인한 뒤에야, 지속되는 null이 실명을 인증하도록 허용한다. 모든 판정은 트리-앙상블 가설
클래스로 명시적으로 범위 한정되며, 선형·kNN 프로브는 오발이 보고되는 — 숨겨지지 않는 —
*카나리아*로 동행한다.

**감사.** 우리는 판정 캐스케이드·임계값·시드 프로토콜·집계 읽기를 실행 전에 사전등록했고
(규칙 → 예측 → 실행 → 판독, 커밋 타임스탬프로 증빙), 8개 TabReD 데이터셋과 Electricity,
INSECTS 스트림을 확증 fresh-seed 재현 및 model-class 패널과 함께 감사했다. 결과는 검출 표가
아니라 식별가능성 지도다(§5): 데이터셋별 검출가능 하한을 넘는 산업 mean-rule drift는
**감사 기준 0/8 — 인증된 셀 기준 0/5**이고, 나머지 둘은 음성이 아니라 무정보다;
설계된-drift 스트림이 유일한 양성(그리고 그 denoised staleness가 raw를 *초과* — 진짜 규칙
변화의 서명); 유일했던 과거 산업 양성은 그것을 발급했던 바로 그 계기에 의해 라벨-노이즈 감쇠로
— 철회가 아니라 — *진단*되고; 두 산업 데이터셋은 주입 회복으로 검증된 무-concept 인증서를
얻는 반면, **산업 8셀 중 둘은 어떤 신호 패밀리에서도 인증서를 얻지 못한다**(주입이 학습 불능
— 소박한 프로토콜이라면 "earned blindness"로 세탁했을 사실);
패널에서 유일한 실명 인증은 Electricity이며 이는 산업 8셋에 속하지 않는다. 남은 산업 셀은
식별가능 null 하나와 진단된 노이즈 기제 하나다. 앵커 스트림들(§6)은 계기에 일관된 감도 프로파일을 부여한다 —
단조·단발 규칙 전환은 9/9 발화; 재발 레짐은 올바르게 침묵하며 단방향 drift에서는 불가능한 음의
recency gain으로 표지된다; 말웨어(EMBER)는 라벨 부패가 아닌 커버리지-기인 열화로 읽힌다 —
그곳에서 시간적 열화가 실제로 발생하는 방식(Pendlebury et al., 2019)과 정합한다.

**기여.**
1. *drift-유형 귀속 실패의 실행된 해부학* (§3–4): concept-drift 판정을 조용히 발급하는 세 가지
   구성된 null(노이즈 감쇠, 게이트 메모리제이션, 클래스 상대성) — 각각 실제 경계선 양성과
   일치하는 크기로.
2. *수리된, 기권하는 계기* (§3): 유효 범위가 지도화된 denoised staleness + 노이즈 게이트;
   group-aware 분리도; 학습가능성-게이트 주입 인증서; 카나리아 동반 클래스-범위 판정. 적대
   조합(규칙 변화 *와* 노이즈 drift의 공존은 여전히 발화해야 — 실제로 발화, 청정 검정력의
   58%)을 포함한 14-셀 사전등록 배터리로 검증.
3. *산업 표 ML의 사전등록 식별가능성 지도* (§5): 감사 패널 기준 착취 가능 mean-rule drift
   0/8 — 인증서 없는 두 셀을 분리하면 0/5이며, 이 분리가 지도의 요점이지 단서가 아니다;
   기제가 규명된
   진단된 오탐 1건; 데이터셋별 인증서와 검출가능-효과 하한; 확증 안정성 10/10과 클래스 간
   (HGB↔RF) 일치 10/10, 그리고 선형 모니터가 정전 Electricity 데이터셋을 "concept drift"로
   뒤집는 실데이터 실증.
4. *앵커로 확립한 감도 프로파일* (§6): 이 렌즈가 볼 수 있는 것과 없는 것 — 설계된-drift
   스트림에서 확립("old 라벨이 *현재* 레짐과 모순일 때만 발화"), 재발 레짐의 음의-recency
   지문과 정직한 말웨어 null 포함.

**우리가 주장하지 않는 것.** 산업 표 데이터에 concept drift가 부재하다고 주장하지 않는다 —
여러 셀이 *실명*으로 인증되었고, 실명은 부재가 아니다. 식별가능성 이론이 새롭다고 주장하지
않는다 — overlap 실패가 비모수 식별을 막는다는 것은 고전이고(D'Amour et al., 2021; Ben-David
et al., 2010), 클래스-상대성 원리는 확립되어 있다(Hinder et al., 2023); 우리의 기여는 실행된
계기, 그 실패 해부학, 그리고 인증된 지도다. 그리고 model-agnostic함을 주장하지 않는다: 모든
판정은 배포된 가설 클래스에 상대적이며, 우리는 그것이 drift-유형 귀속을 진술하는 유일하게
정직한 방식이라고 논증한다. 끝으로, 이 지도는 *딥* 시간-인지 아키텍처가 무엇을 착취할 수
있는지를 판정하지 않는다: 지도가 고정하는 것은 지배적 배포 클래스에게 착취할 것이 있는가이고,
배터리(§4.3)는 더 풍부한 프로브가 지도에 합류하기 전에 통과해야 할 진입 관문이다.

## 2. 관련 연구

**표 데이터의 shift-유형 지도.** WhyShift(Liu et al., 2023)가 가장 가까운 선행이다: 표
데이터셋들에서 성능 하락을 Y|X- vs X-shift로 귀속하고 Y|X-shift가 만연하다고 결론짓는다 —
주로 공간/부분모집단 축에서, overlap을 가정하는 분해로. 우리의 델타는 셋이다: 배포된 산업
표현 위의 *시간* 축(positivity가 실제로 실패하고 그들의 렌즈가 퇴화하는 곳), 데이터셋별
인증서를 동반한 *기권 arm*(WhyShift는 분해가 측정 가능함을 가정한다), 그리고 계기-실패
해부학. DISDE(Cai et al., 2023)는 우리가 용어를 물려받은 분해 형식론을 제공한다; 그
공유-분포 항은 공통 support에서만 식별된다 — 우리의 지도가 운영화하는 경계다. TabReD 자체
(Rubachev et al., 2025)는 모든 것을 "점진적 시간 변화"로 서술하며 분해도 recency 실험도
없다; TableShift(Gardner et al., 2023)와 Wild-Time(Yao et al., 2022)은 drift-유형 귀속이
없는 shift 벤치마크다.

**식별가능성과 클래스-상대성.** support 밖에서 P(y|x)가 가정 없이 식별되지 않는다는 것은
교과서적 positivity다(고차원 overlap 실패는 D'Amour et al., 2021; 학습이론적 불가능성은
Ben-David et al., 2010; 학습된 표현 내부의 support 실패는 Johansson et al., 2019). Hinder et al.(2023)은 손실 기반 drift 검출이 양방향으로 클래스-
상대적임을 구성적으로 증명하고 — 손실을 움직이는 virtual drift, 손실에 보이지 않는 real
drift — 그들의 서베이(Hinder et al., 2024)는 "사용된 모델 클래스가 결정적"이라고 명시한다.
Loog et al.(2019)은 i.i.d.에서조차 ERM 위험이 표본 크기에 비단조일 수 있음을 보여, 어떤
무조건적 "옳은 데이터 추가는 해가 될 수 없다" 보조정리의 문도 닫는다. Gower-Winter et al.
(2026)은 윈도우 선택을 통해 drift 검출이 ill-posed라고 논증한다. 우리는 이 모두를 응용
drift-모니터링 스택이 아직 흡수하지 못한 확정 이론으로 취급하고, 실행된 실증 + 수리된
프로토콜을 기여한다; 우리 자신의 정리는 하나도 진술하지 않는다 — 논문이 형식적으로 들리는
곳(envelope, 인증서)의 내용물은 증명된 경계가 아니라 실측된 경계다.

**old 데이터의 해악.** Shimodaira(2000)는 misspecified 케이스의 기제다; Data Addition
Dilemma(Shen et al., 2024)는 임상 ML에서 혼합 해악을 경험적으로 기록한다; Klinkenberg &
Joachims(2000)는 후보 학습 윈도우들의 held-out 오차로 윈도우 크기를 *적응*시킨다. 우리가
아는 한, add-old-data 대비를 판정 의미론을 갖춘 drift-*유형* 식별자로 쓰거나 라벨-노이즈-감쇠
오탐 채널을 보고한 선행은 없다 — real drift를 P(y|x)의 임의 변화로 정의하는 필드 표준
(Gama et al., 2014; Webb et al., 2016) 아래에서 노이즈 drift는 drift*다*. 이것이 바로
*착취 가능한 규칙 변화*를 검출한다고 주장하는 계기가 둘을 분리해야 하는 이유다; 우리는 그
분리를 구성으로 달성하고 적대적으로 검증한다.

**Pseudo-label·denoising·교차적합.** denoised arm의 역학은 의도적으로 차용된 것이다: 라벨을
모델 예측으로 대체하는 것은 self-training(Scudder, 1965; Lee, 2013)이자 distillation(Hinton
et al., 2015)이고; 시끄러운 라벨이 적합된 모델로 복구 가능하다는 관점은 noisy-label 학습
(Natarajan et al., 2013; Han et al., 2018)의 기저이며; 자기-적합 오염을 피하는 엄격한
out-of-fold 예측은 교차적합(cross-fitting; Chernozhukov et al., 2018)이다. 우리가 이
문헌들에서 가져오는 것은 추정기다; 그 *용법* — 적대적으로 지도화된 유효 envelope와 기권
규칙을 갖춘 drift-유형 판정의 판별 arm으로서 — 이 우리가 주장하는 기여이고, 우리는 그것을
근거의 범위로 한정한다: 우리가 읽은 한, 이들 인접 문헌 어디에도 이 arm이 무력화하려 존재하는
실패 채널은 보고되어 있지 않다.

**drift 검출기와 그 평가.** 고전 검출기들(서베이: Gama et al., 2014; Lu et al., 2019)은
손실 또는 분포 통계량을 모니터링하며 스트림에 기지 drift를 주입해 평가된다(Poenaru-Olaru
et al., 2022); Detectron(Ginsberg et al., 2023)은 harmful shift를 모델-상대적으로 정의한다.
우리의 주입 컨트롤은 역할이 다르다: *in-situ, 데이터셋별 검정력 인증서*(실제 공변량 기하에
심고 학습가능성으로 게이트)로서, earned blindness와 계기 공허(vacuity)를 구별하는 데 쓰인다
— 실데이터에서 중요함이 드러나는 구별이다: 산업 "unidentifiable" 셀 4개 중 **둘**은 시도한
모든 신호 패밀리에서 인증서를 얻지 못하고, 나머지 둘은 기준 패밀리 밖으로 넓혀야만 해소된다. 우리는 이 검출기들을 감사 셀에 head-to-head로 돌리지 않는다: 손실-스트림
검출기는 "무언가 변했는가?"에 답하며 구성상 유형-맹목이라, 그 발화는 drift-*유형* 귀속에
대해 아무것도 말해주지 않는다; 같은 바이트 위에서 더 약한 *귀속* 계기가 무엇을 보고하는지의
동종 비교는 카나리아 패널(§4.3)이고, 인증서 프로토콜의 스트리밍 모니터 이식은 향후
과제다(§7).

**말웨어.** TESSERACT(Pendlebury et al., 2019)는 말웨어에서 시간적으로 정직한 평가를 확립했고
성능 열화를 기록한다. 우리의 EMBER 판독(old 데이터가 *도움*; staleness −0.008; 검출 하한
0.0013)은 그것을 반박하는 게 아니라 정제한다: 열화는 커버리지-기인(새 패밀리 등장 = 공변량
확장)이지 라벨 부패가 아니며(2017년 말웨어 표본은 여전히 말웨어다), 이것이 정확히 배포 렌즈가
그어야 할 구별이다.

## 3. 계기 (The Instrument)

**Figure 1.** 계기의 판정 캐스케이드 — 벡터 정본 = `paper/figures/fig1_cascade.tex`(TikZ,
컴파일 확인됨; `fig1_cascade.pdf`), 초안 스케치 = 영문본 §3의 mermaid. 캐스케이드는 §3.3과
PREREG §3에 원문 그대로 명세된다. 모든 실행에 주석으로 붙는 side channel: strict-rule 그림자
판정(rule-sensitive 플래그), 재발 레짐의 음의-recency 지문(§6), 카나리아-프로브 패널(§4.3),
그리고 provenance 스탬프.

*읽기 보조.* 실제 셀 하나가 아래 어휘의 대부분을 구동한다. sberbank-housing(§5.3)에서 raw
arm은 발화하지만(+0.024) 노이즈 게이트가 켜져 있고(2.1×) denoised arm은 음수다 — 그래서
캐스케이드는 concept이 아니라 NOISE-DRIFT-CONFOUNDED를 반환한다; 셋이 envelope 안에서
정렬했더라면 판정은 주입 양성대조를 조건으로 DEPLOYMENT-CONCEPT였을 것이다.

**용어 (각 한 줄).**

| 용어 | 의미 |
|---|---|
| raw staleness | 미래-윈도우 score(recent) − score(recent ∪ old); >0 = old 데이터가 해침 |
| denoised staleness | 동일하되 old 라벨을 old-윈도우 내부 교차적합 예측으로 대체; >0 = *규칙*이 변함 |
| noise gate | old-윈도우 노이즈 프록시 / recent 중앙값; >1.5 = 라벨-노이즈 drift 존재 |
| envelope | denoiser 유효성의 실측 경계인 노이즈비 4.7; 그 위에서 계기는 기권 |
| D | 윈도우 분리도 AUC(group-aware·크기맞춤); 라우팅 통계량, support overlap이 *아님* |
| injection | 데이터셋 자신의 기하에 심은 기지 규칙 회전; 데이터셋별 검정력 인증서 |
| earned blindness | 주입이 윈도우 내 학습가능한데도 회복 불능 → 기하가 이 신호 클래스를 진짜로 숨김 |
| injection-vacuous | 주입 학습불능 → 그 null은 아무것도 인증하지 않음 |
| no-evidence band | sub-floor CI-양성 읽기; no-drift 앵커에서 노이즈로 캘리브레이션 |
| canary probe | 오발이 보고되는, 의도적으로 misspecified된 프로브 클래스(선형/kNN) |
| rule-sensitive | 일차/엄격 판정 규칙 간 판정 불일치; 헤드라인 금지 |

### 3.1 설정과 estimand

행 (x_i, y_i, t_i)가 시간에 따라 도착한다; 감사자는 라벨된 전체 이력을 본다(회고적 감사,
온라인 예측 아님). K개 윈도우를 timestamp-값 랭크로 고정한다(동률은 경계를 넘지 않음; 시드별
경계 지터). 각 미래 윈도우 W_j(후반부)에 대해, old 앵커 W_0(행 ≥200이고 학습 가능한 라벨을
가진 최초 윈도우)와 recent 윈도우 W_{j−1}로부터 크기 맞춘 표본(N = min(|recent|, |old|,
6000))을 뽑아 배포 클래스의 세 모델(recent-only, old-only, recent∪old)을 학습한다. 미래
윈도우들에 대한 시드별 평균으로 보고한다:

- **decay** = score(old 모델, W_0 held-out) − score(old 모델, W_j) — 노화, 기제-맹목
  (Vela et al., 2022가 대규모로 기록한 현상);
- **recency gain** = W_j에서 score(recent) − score(old) — 적응 가치, 커버리지와 규칙 변화가
  섞임; *그 부호 자체가 진단적이다(§6): 음의 recency는 단방향 drift에서 불가능하며 재발
  레짐을 지문한다*;
- **raw staleness** = W_j에서 score(recent) − score(recent∪old) — 귀속 프로브;
- **denoised staleness** = W_j에서 score(recent) − score(recent ∪ (X_old, ĝ_old(X_old))),
  여기서 ĝ_old는 W_0 *내부* 2-fold 교차적합 모델이 산출하는 엄격히 out-of-fold인 hard
  pseudo-label.

점수: AUC(이진), accuracy(다중클래스), z-표준화 목표의 −RMSE(회귀). CI는 10 시드에 대한
Student-t; 각 시드는 90% 행 부표본을 재추출하고 윈도우 경계를 재지터하며 모든 학습 집합을
재표집한다. 이것들은 심하게 겹치는 부표본 위의 시드-수준 구간이며 미세한 효과에서는
반보수적(anti-conservative)이다(§6이 그 결과인 무증거 대역을 no-drift 앵커로
캘리브레이션한다); 판정-수준 추론은 이것에만 의존하지 않는다 — 작동하는 안정성 점검은 확증
fresh-seed 재현(§5.1)이고, 모든 실행은 시드별 원값을 함께 출력하므로 대안적 구성(윈도우-블록
부트스트랩, split-half)을 재실행 없이 적용할 수 있다. 사전등록된 estimand는 *착취 가능한
mean-rule drift*: 배포된 가설 클래스에 대해 old 라벨이 현재 규칙과 모순되게 만드는, P(y|x)의
의사결정-관련 함수의 변화다. 이는 필드 표준 "P(y|x)의 임의 변화"(Moreno-Torres et al.,
2012; Webb et al., 2016)보다 의도적으로 좁다 — 그 좁힘이 요점이다. 넓은 정의는 라벨-노이즈 drift를 concept으로 분류하고,
그로써 도움이 될 수 없는 재학습을 정당화하기 때문이다.

**denoised arm이 규칙 변화와 노이즈 drift를 분리하는 이유.** 노이즈-단독 drift에서 조건부
평균/Bayes 규칙은 불변이므로, ĝ_old는 시끄러운 라벨로부터 *같은* 규칙을 추정한다; 그
pseudo-label은 근사적으로 옳은 denoised 라벨이고, (X_old, ĝ_old(X_old))를 recent 집합에
추가하는 것에는 해칠 기제가 남지 않는다(§4.2가 경계를 실측하는 기대(expectation)이지
정리가 아니다) — 실행: +0.021의 노이즈-감쇠 오탐이 +0.004로
붕괴하고 게이트(아래)가 기제를 표지한다. 진짜 규칙 변화에서는 ĝ_old가 *옛* 규칙을 일관되게
추정한다; 그 pseudo-label은 여전히 현재 규칙과 모순되고 해악이 잔존한다 — 실행: 회전-규칙
스트림은 raw +0.546 중 +0.541을 유지하고, 규칙변화+노이즈감쇠 동시 발생도 +0.316으로
발화한다. denoiser는 불편(unbiased)이 아니다: pseudo-label 오차는 old 윈도우 노이즈와 함께
커지며, 우리는 *경계를 지도화했다* — old/recent 노이즈비 ≈5.7에서 denoised arm 자신이 순수
null 위에서 판정 floor를 넘는다(+0.026). 따라서 계기는 정리(theorem)의 가장(假裝) 대신
envelope 상수를 지니고 다닌다(비율 4.7 위 기권); Loog et al.(2019)에 따라, 얻어질 무조건적
soundness 보조정리는 존재하지 않는다.

**노이즈 게이트.** 윈도우마다 배포 클래스의 fresh 모델을 70%로 적합하고 held-out
비환원-오차 프록시를 기록한다(회귀: z-표준화 y의 MSE; 이진: 1−AUC; 다중: 1−accuracy). 게이트
통계량은 old 윈도우 프록시 / recent 윈도우 프록시 중앙값; 1.5 위에서 발화하고(안정 합성
컨트롤은 0.75–0.99로 캘리브레이션) 4.7 위에서 envelope를 정의한다. 게이트 단독은 충분한
수리가 *아니다* — 게이트-거부권 규칙은 노이즈 drift와 공존하는 진짜 규칙 변화를 오분류한다
(실행: 게이트 3.67에도 +0.316은 발화해야 한다) — 게이트는 raw arm이 발화하고 denoised arm이
발화하지 않을 때 *기제 라벨*(NOISE-DRIFT-CONFOUNDED)을 공급한다.

### 3.2 인증서: 분리도, 주입, 학습가능성

**윈도우 분리도 D ("overlap"이 아님).** D는 W_0와 각 후반부 윈도우를 분리하는 분류기의
held-out AUC 중앙값으로, proxy-strip된 피처 공간에서, 소박한 버전 대비 두 가지 수리와 함께
계산된다: *랜덤 부표본*에 의한 크기 맞춤(시간 정렬된 행에서의 head-slice는 윈도우-크기
불균형만으로 분리도를 제조한다 — 실행: 정적 피처가 전무한데 shuffle-D 0.94, 수리 후 0.50으로
붕괴)과 근접-중복 클러스터(0.1로 반올림한 z-표준화 행)를 키로 하는 *group-aware* train/test
분할: 다중도 5의 정확한 중복은 행-분할 D를 0.994로, entity cohort는 1.000으로 — 공변량 변화
전무 상태에서 — 부풀리고; group 분할은 정직한 drift의 높은 D는 유지한 채 둘 다 ≈0.50으로
되돌린다. 우리는 D를 *분리도*로만 보고하고 support overlap으로는 결코 보고하지 않는다:
진짜로 예측적인 drifting 피처 하나가 D를 포화시키므로, D ≥ D* = 0.96은 staleness null을
주입 컨트롤로 라우팅할 뿐 그 자체로는 아무것도 결론짓지 않는다.

**학습가능성-게이트 주입.** 모든 unidentifiable 후보 데이터셋(그리고 양성 대조로서, 모든
CONCEPT 판정)에 대해 기준 concept — 최고 분산 두 피처 위의 고정 강도 규칙 회전 — 을
데이터셋 자신의 (X, t) 기하에 심고 전체 staleness 파이프라인을 재실행한다(10 시드, 본판독과
동일 검정력). null이 어떤 의미든 갖도록 허용되기 전에, 주입된 규칙이 *윈도우 내에서 학습
가능*해야 한다(held-out AUC ≥ 0.65 / R² ≥ 0.20 / accuracy ≥ 다수클래스 + 0.10): 최고-분산
피처가 heavy-tail 잡동사니인 구성 기하에서 주입 규칙은 학습 불능이고(윈도우 내 AUC 0.506)
소박한 프로토콜은 "earned blindness"를 공허하게 부여한다; 게이트는 그것을 *injection-
vacuous* — 인증서 거부 — 로 전환한다. 결과: 회복(⇒ 실제 null은 유정보였음: 검증된
무-concept), earned blindness(학습 가능하나 회복 불가 ⇒ 기하가 이 신호 클래스를 진짜로
숨김), 또는 공허(인증서 없음). 실제 감사에서 이 구별은 하중을 진다: "unidentifiable" 산업
셀 4개 중 **둘**은 시도한 모든 신호 패밀리에서 학습가능성 게이트에 탈락해 인증서 없이 끝나고,
나머지 둘은 패밀리를 넓혀야 해소된다 — homesite_insurance는 기준 패밀리에서 게이트 ±0.05 안에
있어 시드 집합 간 뒤집히지만 저분산 캐리어에서는 여유 있게 학습가능하며 일관되게 미회복이고,
weather는 기준 패밀리에서 학습불능이지만 다른 두 패밀리에서 회복한다(§5.2).
인증서는 이 기준 패밀리에 명시적으로
상대적이다: 회복이 인증하는 것은 데이터셋의 고분산 방향이 실어 나르는 회전에 대한
검정력이지, 저분산 피처·상호작용·부분모집단에 사는 규칙 변화에 대한 것이 아니다(§7 한계 6).

**식별 경계 (선행연구의 재진술이며 신규 결과가 아님).** R을 미래 윈도우가 차지하고 old 앵커는
차지하지 않는 공변량 공간 영역이라 하자. R 위에서 P(y|x)는 관측된 윈도우들로부터 비모수적으로
**식별되지 않는다**: old 윈도우의 support에서 일치하고 R에서 다른 두 조건부는 동일한 관측
데이터를 유도하기 때문이다 — R에 old 행이 하나도 없으므로. 이는 DISDE가 명시하는 공유-support
요건(Cai et al., 2023, Prop. 1)의 **시간축 사례**이며, 우리는 우리 자신의 정리를 주장하지 않는다.
계기의 세 성질이 편의가 아니라 여기서 따라 나온다. 기권(abstention)은 소심함이 아니라
**정확성**이다. D는 패널이 그 경계에 얼마나 가까운지를 재는 **라우팅 통계량**이지 support
중첩의 측도가 아니며 — 그래서 D ≥ D\*는 그 자체로 아무것도 결론하지 않고 주입 컨트롤로 넘긴다.
그리고 식별가능성 인증서가 없는 셀은 *drift 없음*의 증거가 될 수 없고 오직 *측정 없음*의
증거일 뿐이며 — 그래서 우리는 사전등록된 0/8 옆에 인증된 0/5를 함께 들고 다닌다(§5.2).
반대 방향, 즉 *건전성*(staleness > 0이면 규칙 변화)에 대해서는 얻을 수 있는 정리가 **없다**:
Loog et al. (2019)이 같은 분포의 데이터를 더해도 ERM 위험이 비단조임을 보였으므로, drift가 0이고
이분산성이 0이어도 E[staleness] > 0이 가능하다. 이 비대칭이 §4.2가 건전성 보조정리가 있어야 할
자리에 *측정된* 유효 범위(envelope)를 놓는 이유다.

### 3.3 판정 캐스케이드와 범위

사전등록된 캐스케이드(PREREG §3; 아티팩트에 원문): DEPLOYMENT-CONCEPT는 노이즈 envelope
내에서 *denoised* arm의 발화(CI 하한 > 0 이고 평균 > 0.02)를 요구한다; 게이트 켜진 raw
발화는 NOISE-DRIFT-CONFOUNDED; 게이트 꺼진 raw 발화는 RAW-ONLY-POSITIVE(미해결, 결코
concept 아님); null은 D를 거쳐 주입 인증서로 라우팅되고; sub-floor denoised CI는 무증거
대역으로 보고된다(캘리브레이션: no-drift 앵커 스트림 5개 중 2개가 floor의 1/10–1/20에서
그곳에 착지 — CI-유의하나 의사결정-무관). 엄격한 이차 규칙(CI 하한 > floor)이 병행 계산되며;
두 읽기에서 판정이 갈리는 셀은 rule-sensitive로 표기되고 헤드라인에서 금지된다. 모든 판정은
배포된 가설 클래스로 범위 한정된다: ground-truth 스위트는 HGB와 랜덤 포레스트에서 통과하고
선형/kNN 프로브에서 실패하므로(§4), 트리-앙상블 판정이 의사결정 등급이고 선형/kNN은
카나리아로 달린다. 모든 실행은 provenance 메타데이터(커밋, argv, 라이브러리 버전, 시드)를
버전드 아티팩트에 기록하며; 판정 규칙·임계값·시드 프로토콜(탐색 0–9, 확증 100–109)·집계
읽기는 그것들이 지배하는 실행 이전에 커밋되었다. 부록 C가 모든 판정 상수를 캘리브레이션
출처·유효 범위와 함께 표로 제공한다.

## 4. 검증: 배터리, envelope, 클래스 매트릭스

### 4.1 14-셀 사전등록 배터리

계기는 어떤 실데이터 실행 전에도 합성 ground-truth 배터리를 통과해야 하며(PREREG §4);
배터리 자체가 실패 해부학의 실행 기록이다. 전 셀: n = 12,000, d = 10, K = 10, 5 시드
(실데이터의 10과 대비 — 심은 효과가 판정 floor의 10~27×이고 배터리는 추정이 아니라
pass/fail 관문이다); 전체 캐스케이드 하의 판정(아래 표).

| 셀 (진실) | raw | denoised | gate | 판정 |
|---|---|---|---|---|
| 회전 규칙 (이진) | +0.280 | +0.289 | 0.90 | **CONCEPT** ✓ |
| 회전 규칙 + drift하는 nuisance | +0.203 | +0.206 | 1.18 | **CONCEPT** ✓ (proxy-strip) |
| 회전 규칙 (회귀) | +0.546 | +0.541 | 0.91 | **CONCEPT** ✓ |
| **회전 규칙 + 노이즈 감쇠** | +0.357 | **+0.316** | 3.67 (발화) | **CONCEPT** ✓ — 게이트가 거부하면 안 됨 |
| 공변량 변화, 고정 규칙 | −0.001 | −0.000 | 0.74 | UNIDENT (실명 획득) ✓ |
| 정상(stationary) | −0.001 | −0.000 | 0.75 | NO-STRONG-CONCEPT ✓ |
| prior 변화, 고정 규칙 (다중클래스) | −0.003 | −0.004 | 0.84 | UNIDENT, injection-vacuous ✓ |
| 약한 공변량 변화 (D = 0.73, un-gated) | −0.003 | +0.001 | 0.99 | NO-STRONG-CONCEPT ✓ — null이 하중을 짐, 게이트로 치워지지 않음 |
| 정상 (회귀) | −0.012 | −0.002 | 0.99 | NO-STRONG-CONCEPT ✓ |
| 공변량 변화, 고정 *선형* 규칙 (회귀) | +0.002 | +0.004 | 0.91 | UNIDENT ✓ (결코 CONCEPT 아님) |
| 공변량 변화, 고정 *비선형* 규칙 (회귀) | −0.005 | −0.003 | 0.92 | UNIDENT ✓ |
| **라벨-노이즈 감쇠, 고정 규칙** (F1 킬러) | **+0.021 발화** | +0.004 | 3.54 | **NOISE-DRIFT-CONFOUNDED** ✓ |
| 라벨 노이즈 *증가*, 고정 규칙 | −0.023 | −0.019 | 0.14 | NO-STRONG-CONCEPT ✓ |
| **x-의존 노이즈, 감쇠 스케일** | **+0.025 발화** | +0.005 | 3.76 | **NOISE-DRIFT-CONFOUNDED** ✓ |

세 셀이 논문의 논증을 진다. 노이즈-감쇠 셀은 "옳은 라벨은 해가 될 수 없다"의 실행된 반박이다:
raw 프로브가 실제 산업 양성과 0.003 이내로 일치하는 크기(+0.021)로 발화한다. x-의존-노이즈
셀은 수리 자체의 적대 검토 중 발견된 *두 번째* raw 오탐 채널이다; 세 번째(규칙 피처와 상관된
노이즈 스케일 — pseudo-label 오차가 신호가 사는 곳에 집중되는 denoiser 최악 케이스)는 독립
red-team 패스가 구성했고 역시 무력화되었다(raw +0.026 발화; denoised +0.006, null).
concept+노이즈 셀은 두 기제가 공존할 때 수리가 검정력을 유지함을 확립한다: denoised 보존율은
청정 회전-규칙 신호의 58%(+0.316 vs +0.541)이고, 작은 규칙-변화 크기에서도 성립한다(0.8-rad
회전: 청정 denoised +0.108, 노이즈 감쇠 동반 시 +0.062 — 감쇠되나 발화). 작은 old 윈도우에
대한 denoiser 강건성도 직접 확인했다(old 윈도우 600행, fold 300): 오탐 없음(+0.004), 미탐
없음(+0.430).

**지표 간 floor 비교가능성.** 0.02 판정 floor는 AUC·accuracy·z-표준화 −RMSE에 공유되는데,
이들은 의사결정-등가 단위가 아니다. 두 완화 장치가 구조적이다: 모든 null은 자신의 검출가능-
효과 하한 δ(집계의 실질 작업을 수행하는 데이터셋별·지표별 검정력 진술)를 지니고 다니고,
CONCEPT는 결코 floor 단독에 의존하지 않는다(denoised CI·게이트·envelope가 모두 정렬해야
한다). 민감도 점검으로, 배터리의 강도-맞춤 concept 크기(이진 +0.28 vs 회귀 +0.55 ⇒ 회귀
floor 0.04)로 재스케일한 지표별 floor 아래 모든 커밋된 실행을 재역치화해도 concept/무-concept
경계를 넘는 셀은 없다; 유일하게 영향받는 셀은 진단된 회귀 양성(§5.3)의 기제 라벨인데, 이는
이미 rule-sensitive로 표기되어 헤드라인에서 금지되어 있다.

### 4.2 Envelope: 수리 자체가 깨지는 곳

denoiser는 편향되어 있고 — pseudo-label 오차는 old 윈도우 노이즈와 함께 커진다 — 그 편향을
무시할 만하다고 가정하는 대신 지도화했다: 순수 고정-규칙 null 위에서 denoised arm은 노이즈비
3.54에서 +0.0037, 3.76에서 +0.0048, 3.87에서 +0.0043, 4.72에서 +0.0140, 그리고 **5.71에서
+0.0259 — null 위에서 0.02 판정 floor를 돌파**한다. 따라서 계기는 비율 4.7 위에서 기권한다
(NOISE-AMBIGUOUS). envelope 너머에도 분리는 존재하지만(비율 ≈6에서 진짜 회전 규칙은 denoised
+0.086 vs null의 +0.026, CI 분리) 그곳의 어떤 임계값도 단일 노이즈 패밀리로 캘리브레이션된
것이므로 판정을 거부한다. 안정 컨트롤은 게이트 통계량을 0.75–0.99로 캘리브레이션하며(발화
임계 1.5에서 멀리), §5의 모든 실데이터 게이트 값은 1.5 아래이거나 2.0–2.9 — envelope 근처는
어디에도 없다.

### 4.3 model-class 매트릭스: 판정이 클래스-범위인 이유

프로브의 모델 클래스를 바꿔(계기의 모든 구성요소 — staleness arm들, 게이트, 분리도, 주입 —
가 스왑을 따른다) 핵심 컨트롤 5종을 재실행하면 아래 표를 얻는다.

| 컨트롤 (진실) | HGB | RandomForest | MLP (64,32) | 선형 | kNN |
|---|---|---|---|---|---|
| 회전 규칙 | ✓ (+0.280) | ✓ (+0.287) | ✓ (+0.305) | ✓ (+0.310) | ✓ (+0.295) |
| 공변량 변화, 고정 규칙 | ✓ (−0.001) | ✓ (+0.004) | △ (den +0.045, CI로 구제) | ✓ (−0.002) | **✗ CONCEPT (+0.098)** |
| prior 변화, 고정 규칙 | ✓ (−0.003) | ✓ (+0.003) | **✗ CONCEPT (den +0.042)** | **✗ CONCEPT (+0.026)** | **✗ CONCEPT (+0.048)** |
| 정상(stationary) | ✓ | ✓ | ✓ | ✓ | ✓ |
| 회전 규칙 + nuisance | ✓ (+0.203) | ✓ (+0.200) | ✓ (+0.228) | ✓ (+0.229) | ✓ (+0.219) |

concept *검출*은 클래스-견고하다(다섯 클래스 모두 진짜 회전에 발화); 판정이 의지하는 성질인
concept/covariate *분리*는 고정 규칙을 표현할 만큼 유연한 클래스에서만 성립한다. 이것이
Shimodaira(2000)의 경험적 실현이다: misspecification 하에서 혼합-ERM 최적해가 P(x)와 함께
움직이므로, 공변량 변화만으로 "old 데이터가 해친다"가 제조된다. denoising은 이 채널을
수리하지 못하며(kNN은 pseudo-label로도 +0.111로 오발 — misspecification은 라벨이 아니라
*프로브*에 있다), 이것이 계기의 판정이 트리-앙상블-범위로 진술되고 선형/kNN이 카나리아로
동행하는 이유다. 신경망 프로브는 강조할 가치가 있다 — 이 논문의 동기 질문이 딥 표
아키텍처에게 착취할 규칙 변화가 있는가이기 때문이다: **2-레이어 MLP 프로브는 분리 배터리에
실패한다** — 고정-규칙 prior 변화를 concept drift로 읽고(denoised +0.042, 발화) 순수 공변량
변화에서 양(+)으로 기운다(denoised +0.045, CI 폭 덕분에만 구제) — 따라서 의사결정-등급
클래스가 아니라 카나리아에 합류한다. 우리는 이것이 현대 딥 표 아키텍처의 프로덕션 스케일로
외삽된다고 주장하지 않는다; 우리가 주장하는 것은 입증 책임의 방향이다: 프로브 클래스는 이
배터리를 통과함으로써 drift-판정 권위를 획득하며, 우리가 시험한 첫 신경망 프로브는 통과하지
못한다. 필드를 향한 실용적 독해는 불편하다: **선형·국소·소형-신경망 프로브 위에 지어진
drift 모니터는, 트리-앙상블 모니터가 아무것도 보고하지 않는 같은 바이트 위에서 concept
drift를 보고할 수 있다** — 그리고 §5.4는 이것이 합성만이 아니라 정전 실데이터 drift
데이터셋에서 일어남을 보인다.

## 5. 지도 (The Map)

### 5.1 프로토콜

TabReD 8개(공식 시간 분할의 train 구간), Electricity(elec2), INSECTS(incremental-balanced)
— HGB 프로브, K = 10, 탐색 시드 10개(0–9), 이후 **fresh 시드(100–109)로 확증 재실행**; 둘
사이에 움직이는 판정은 unstable로 보고되고 주장에서 금지된다. 판정 캐스케이드·임계값·시드
프로토콜·집계 읽기는 실행 전에 커밋되었고; 과거(v2 시대) 산업 양성은 *예측이 붙은 철회 후보*
(NOISE-DRIFT-CONFOUNDED 또는 denoised-null)로 사전등록되었으며, 예측이 실패할 경우의 생존
배터리도 사전 규정되었다. 일차 규칙(CI > 0 ∧ 평균 > floor)과 엄격 규칙(CI > floor)이 모두
계산되고; rule-sensitive 셀은 표기된다.

### 5.2 결과: 확증 안정 10/10; 산업 mean-rule drift 감사 0/8, 인증 0/5

**Figure 2.** 한눈에 보는 지도 (벡터 소스 `paper/figures/fig2_map.tex`): 데이터셋당 타일
하나, 판정 클래스로 채색 — 초록 = 규칙-변화 판정 또는 검증된 무-concept, 주황 = 진단된
노이즈 기제, 파랑 = 식별가능 null, 연파랑 = earned blindness, 빨강 = 인증서 거부, 회색 =
불안정 — 각 타일 안에 하중을 지는 수치(denoised staleness 또는 주입 회복). 색은 중복
인코딩이다: 모든 타일이 판정 텍스트를 함께 인쇄한다(색각 안전). 아래 표가 정밀 기록이다.

| 데이터셋 | 판정 (= 확증) | raw | denoised | gate | D | 회복 | 인증서 |
|---|---|---|---|---|---|---|---|
| insects | **DEPLOYMENT-CONCEPT** | +0.135 / +0.129 | **+0.152 / +0.145** | 1.24 | 0.844 | +0.162 | 주입 회복 |
| sberbank_housing | **NOISE-DRIFT-CONFOUNDED** | +0.024 / +0.033 (발화) | **−0.015 / −0.011** | **2.11 / 2.21 (발화)** | 1.000 | — | — (진단됨, §5.3) |
| cooking_time | INJECTION-RECOVERED | −0.011 | −0.018 | 0.92 | 0.966 | **+0.546** | **검증된 무-concept** |
| delivery_eta | INJECTION-RECOVERED | −0.012 | −0.014 | 0.89 | 0.999 | **+0.332** | **검증된 무-concept** |
| maps_routing | NO-STRONG-CONCEPT | −0.008 | −0.010 | 1.01 | 0.578 | 해당없음 | 식별가능 영역 |
| elec2 | UNIDENTIFIABLE | +0.001 | +0.001 | 0.66 | 1.000 | +0.018 | **실명 획득** (주입 학습가능) |
| ecom_offers | UNIDENTIFIABLE | −0.001 | −0.007 | 1.25 | 1.000 | — | *공허* — 주입 학습불능 (0/4 패밀리) |
| homecredit_default | UNIDENTIFIABLE | −0.007 | +0.004 | 1.29 | 1.000 | — | *공허* (0/4 패밀리; 프록시 피처 80개 strip) |
| weather | INJECTION-RECOVERED † | −0.012 | −0.011 | 0.93 | 0.994 | **+0.183** | **검증된 무-concept** (학습가능 3개 중 2개 회복) |
| homesite_insurance | UNIDENTIFIABLE-INERT | −0.004 | −0.002 | 1.14 | 1.000 | −0.001 | **실명 획득** (학습가능 2/2 미회복, 안정) |

*D* = `D_strip`, group-aware 분리도 중앙값(D\* = 0.96이면 주입 컨트롤로 라우팅). *회복* = 심어진
기준 강도(2.5 rad) 심어진 규칙의 회복된 staleness. "—" = 시도한 모든 신호 패밀리에서 주입이
학습불능이라 회복 수치를 쓸 수 없음(공허 규율), *해당없음* = 그 셀은 주입 컨트롤로 라우팅되지
않음. 회복값은 전 셀에서 시드 집합 간 0.01 미만으로 움직인다. † weather는 기준 패밀리의 규칙이
그 기하에서 학습불능이며, 회복은 저분산 캐리어에서 측정된다 — 새 시드에서 재현되고
(+0.183 / +0.193) 상호작용 패밀리에서도 회복하며(+0.083 / +0.078) 둘 다 strict 규칙을 넘는다.
"(m/n 패밀리)" 표기는 몇 개 패밀리가 학습가능했고 그중 몇 개가 회복했는지를 보고하므로,
인증서가 실제로 검정된 규칙-변화 클래스보다 강하게 읽히지 않는다.
*회복*을 *D*에 대해 읽는 것이 이 절 마지막 문단의 주제다.

(두 수치가 있는 곳: 탐색 / 확증.) 사전등록된 집계 읽기: **트리-앙상블 클래스 기준, 데이터셋별
검출가능 하한을 넘는 착취 가능 mean-rule drift를 가진 산업 데이터셋 수 = 0/8.** 이 수는
*감사한* 여덟 데이터셋에 대한 것이다. *식별가능성이 인증된* 셀만 세면 **0/5**인데,
ecom_offers와 homecredit_default 둘만이 인증서 없이 끝나고(시도한 모든 신호 패밀리에서 심어진
프로브 규칙이 학습불능) 어느 방향으로도 증거를 보태지 않기 때문이다. 집계가 등장하는 모든 지점에 두 수를 함께 적는다: 앞의 것은 사전등록된 값이고,
뒤의 것은 그 값이 지니는 무게다. 유일한
concept 양성은 설계된-drift 스트림이며, 그곳에서 denoised arm이 raw를 *초과*한다 — 라벨
노이즈가 제거되면 pseudo-label이 옛 규칙을 깨끗하게 인코딩하는, 진짜 규칙 변화의 서명. 소박한
프로토콜이라면 부여했을 산업 실명 주장 네 건 중 **둘**이 인증서 없이 끝난다 — 심어진 프로브
규칙이 그 기하에서 학습 불능이기 때문이며(최고-분산 피처가 그것을 실어 나를 수 없다), 학습가능성
게이트 없이는 보이지 않는 구별이다. 나머지 둘은 가정이 아니라 **신호 패밀리를 넓혀서** 해소된다:
weather의 기하는 *저분산* 피처 위에서는 규칙을 잘 나르고, 그에 따른 검정력 인증서 아래에서도 실
null이 살아남는다; homesite_insurance도 거기서 학습가능하며 계속 미회복이어서, 첫 판독의
시드-불안정 라벨이 earned blindness로 확정된다. 지도에서 가장 강한 셀은,
반직관적이게도, 두 검증된 무-concept 인증서다: 기하가 검정력을 가짐이 입증되고(+0.55/+0.33의
심은-규칙 회복) 실제 staleness는 여전히 null이다.

**"인증됨"은 균일하지 않다: 라우팅 영역 *안에서* 검정력이 붕괴한다.** D\* = 0.96은 이분법
게이트이지만, 주입 컨트롤 — *고정* 강도(2.5 rad)의 심어진 규칙 — 은 그 안쪽에서 크게 다른
크기로 회복된다: cooking_time(D = 0.966) +0.546, delivery_eta(D = 0.999) +0.332,
Electricity(D = 1.000) +0.018, homesite_insurance(D = 1.000) **−0.052**. 학습가능성은 이 순서를
설명하지 못한다 — Electricity의 심어진 규칙은 패널에서 *가장* 학습가능한데(윈도우 내 AUC
0.976) 회복은 거의 없다. D에 따라 붕괴하는 것은 학습가능성이 아니라 **회복**이다. 결정등급에서
실행된 모든 학습가능 주입에 대해 D와 회복의 순위 상관은 ρ = −0.47(p = 0.037, n = 20)이다.
가장 깨끗한 데이터셋-내 대조는 Electricity 자신이다: 동일한 심어진 규칙이 선형 프로브의
D = 0.905에서 **+0.190**을 회복한다 — HGB의 D = 1.000에서의 회복의 **10배**다. 따라서 인증서가
인증하는 것은 *이 분리도에서* 기하에 검정력이 있었다는 것이지, 인증된 셀들 사이에서 검정력이
균일하다는 것이 아니다. 표의 *회복* 열이 그 편차를 드러내며, §3.2의 임계값은 절벽이 아니라
**연속적이고 가파른 감쇠 위에 얹힌 라우팅**으로 읽어야 한다.

### 5.3 진단: 계기가 자신의 과거 양성을 잡아낸 방법

v2 시대 계기(raw arm 단독)는 sberbank-housing — 패널의 유일한 회귀 데이터셋 — 을 유일한 산업
concept 양성(+0.024 [+0.018, +0.030])으로 보고했었다. 감사는 §4.1의 노이즈-감쇠 null을
일치하는 크기로 구성했고; v3 재실행이 진단을 배달했다. 윈도우 해상도 K ∈ {5, 8, 10, 12, 20}
전체에서: raw arm은 K ∈ {8, 10, 12}에서 발화하고(K = 10에서 v2 헤드라인을 **비트 단위로**
+0.0239로 재현 — 전체 정밀도 +0.023900573591226625는 조판본 각주와 아티팩트에 기록; raw
파이프라인의 RNG 스트림이 불변이므로 이것은 우연히 다르게
나온 재측정이 아니라 *같은* 신호의 재해석이다); old 윈도우의 실측 노이즈 프록시는 모든 K에서
recent 중앙값의 2.1–2.9×이며; **denoised arm은 모든 K에서 유의하게 음수다**(−0.014 ~ −0.018,
CI 전부 0 아래): old 라벨을 교차적합 pseudo-label로 대체하면 old 행이 *돕는다*. 규칙은
변하지 않았다; 이른 시기 라벨이 더 시끄러웠다 — 2011–2012 위기 시대 러시아 주택 가격과
정합한다. K = 20에서 주입 컨트롤은 같은 기하를 통해 심은 규칙을 회복하며(+0.101), 어떤
K에서도 어떤 규칙 읽기도 CONCEPT를 내지 않는다. (진단의 대상은 발급된 양성이다; 노이즈
감쇠와 공존하는 floor 이하의 잔여 규칙 변화를 배제하지 않는다 — sub-floor 주장은 어느
방향으로도 하지 않는다.) 이 진단은 이제 원래 없었던 검정력 인증서까지 갖는다. strict 판정
규칙에서 이 셀의 그림자 판정은 unidentifiable 분기로 착지해 주입 단계에 도달하고, 거기서 기준
강도의 심어진 규칙이 이 기하에서 학습가능하며(윈도우 내 R² 0.93) +0.086으로 회복해 두 규칙을
모두 넘는다. 따라서 음의 denoised arm은 검정력 실패가 아니다 — 규칙 변화를 보여주지 못한 바로
그 윈도우들이, 넣어둔 규칙 변화는 보여준다. 주 판독과 strict 판독이 서로 다른 경로로 같은
결론에 이른다: 발급된 양성은 노이즈였고 그 아래의 null은 정보량이 있다. 우리는 drift-귀속 계기가 — 단순히 재현에
실패한 것이 아니라 — 자신의 과거 오탐의 *기제*를 실데이터에서 진단한 선행 사례를 알지 못한다.

### 5.4 실데이터 클래스 패널

의사결정-등급 클래스들은 일치한다: **HGB와 랜덤 포레스트 판정이 10/10 데이터셋에서 작동적으로
일치**(서브라벨 차이 1건). 카나리아는 그렇지 않다 — 그리고 문제의 flip은 두 독립 카나리아
클래스에 걸쳐 재현된다: **선형 프로브(raw +0.023, denoised +0.033, 주입 회복 +0.190)와 MLP
프로브(raw +0.007, denoised +0.025)가 둘 다 Electricity를 DEPLOYMENT-CONCEPT로 읽는다** —
트리-앙상블 프로브가 실명-획득 unidentifiable로 읽는 곳에서. Electricity는 문헌의 정전
concept-drift 데이터셋이다; 모니터가 그 평판을 확인해 주는지는, 같은 데이터 위에서, 프로브
클래스에 달려 있다. MLP 패널은 카나리아 판정을 직접 소비하면 안 되는 이유도 보여준다: 회귀
데이터셋들에서 그 staleness 추정치는 수치적으로 불안정하고(sberbank CI가 ±15 z-단위에 걸침;
weather CI 폭 0.5) — 소수의 발산 적합이 원인이다: sberbank에서 10개 시드 중 3개가
|staleness| > 10 z-단위에 착지하고 나머지 7개는 ±2 이내다(시드별 값은 커밋된 아티팩트에
있음), 즉 신호가 아니라 미튜닝 프로브의 간헐적 최적화 실패 — 어떤 TabReD 셀도 그 아래에서 새
양성을 내지 않는다 — 의사결정-등급 지도는 불변이다. 경고로 보고할 가치가 있는 두 번째 카나리아 거동: ecom_offers에서 선형
프로브는 raw arm이 null(−0.002)인 채 *denoised* arm만 발화시킨다(+0.028) — misspecified
프로브 특유의 denoiser-아티팩트 채널(선형 ĝ_old의 체계적 오차가 선형 다운스트림 모델에게는
그 자체로 유익 정보다). denoised arm의 의미론 역시 클래스-범위임을 재확인한다.

## 6. 앵커와 감도 프로파일

지도의 신뢰성은 계기가 무엇을 *볼 수 있는지* 아는 데 달려 있다. 23개 합성 river 스트림
(SEA/Agrawal/STAGGER/Sine/Hyperplane; no-drift / 단발 abrupt / gradual / reoccurring 변종)과
INSECTS 7개 변종 전부(실제 센서 데이터, 실험실 통제 온도 drift; Souza et al., 2020) 위에서
실행했다.

**단조·단발 규칙 전환은 발화한다: 9/9.** river: agrawal_abrupt +0.045, agrawal_gradual
+0.047, stagger_abrupt, sine_abrupt +0.031, hyperplane_incremental +0.113(게이트가 그 노이즈
성분을 올바르게 공동 표지), sine_reoccur2 +0.047 [이 렌즈의 지평에서는 이름만 재발형이다:
이 스트림은 마지막 ~22%에서만 초기 레짐으로 돌아오므로, 평가되는 후반부 윈도우 대부분이 중간
레짐 안에 있고 그 규칙은 앵커 규칙의 근사 라벨-반전이다 — recency gain이 강한 *양수*(+0.30)
로 재발 지문의 정반대이며, 기하가 같고 중간 레짐이 라벨-반전이 아닌 자매 스트림들은 ≈0으로
읽힌다(sine_reoccur −0.003, sine_reoccur3 −0.003)]. INSECTS: gradual-balanced +0.092,
gradual-imbalanced +0.069, incremental +0.135 — 발화하는 모든 셀에서 denoised ≥ raw이고 주입
양성대조가 회복된다. 약한 전환(SEA의 threshold 이동)은 일관된 부호로 무증거 대역에 착지하며
검출 하한과 함께 보고된다.

**재발 레짐은 올바르게 침묵한다 — 그리고 지문이 남는다.** INSECTS abrupt 변종들(진동하는
온도)은 *음의* staleness(old 데이터가 도움: −0.070, −0.027)와 **음의 recency gain**(−0.058,
−0.023)을 읽는다: 시험 윈도우에 인접한 윈도우가 가장 오래된 윈도우보다 그것을 *더 나쁘게*
예측한다. 음의 recency는 단방향 drift에서 불가능하다; 그것은 레짐이 돌아왔다는 서명이다. 같은
패턴이 river의 reoccurring 셀들과 INSECTS incremental-reoccurring에서 성립한다 — 세 번의
독립 확인. 이것은 결함이 아니라 범위 진술이다: 렌즈는 배포 질문("오늘 학습하는 모델에 old
데이터가 해가 되는가?")에 답하고, 옛 레짐이 재발할 때 old 데이터는 진짜로 해가 되지 않는다 —
해가 되는 것은 recency다. 이 렌즈 위의 모니터는 재발 concept drift를 *검출*하지 않을 것이다;
old 데이터를 보관해도 안전하다고 올바르게 말할 것이고, 그 음의-recency 플래그가 이유를
말해준다. 감사된 패널에서 이 플래그는 침묵한다: 모든 산업 셀이 recency gain ≥ 0을
읽으며(maps_routing +0.000부터 sberbank-housing +0.325까지), 진동 스트림들의 재발
서명(−0.023 ~ −0.058) 근처에도 가지 않는다. 이는 의도적으로 범위 한정된 진술이다: 음의
recency가 인증하는 것은 평가 지평을 지배하는 규모의 재발뿐이다 — 우리 자신의 sine_reoccur2
앵커가 늦게 돌아오는 레짐(마지막 ~22%)이 recency +0.30으로 읽힘을 보이고, 윈도우 폭 이하
시간 규모의 drift는 평균되어 사라진다(§7 한계 2). 따라서 침묵하는 플래그가 배제하는 것은
감사 패널 위의 지평-지배적 재발이며; 늦거나 짧거나 sub-window 주기적인 회귀(일중·요일
계절성 포함)에 대해서는 아무것도 말하지 않는다 — 이 점검이 커버하지 못하는 영역이다.

**무증거 대역의 캘리브레이션.** no-drift 앵커 스트림 5개 중 2개가 판정 floor의 1/10–1/20
크기에서 "CI-유의한" sub-floor denoised 양수를 낸다 — 겹치는 부표본 위 시드-수준 CI는 미세한
크기에서 반보수적이다. 따라서 sub-floor 대역은 이 논문 전체에서 *무증거*로 읽힌다 — 앵커
스위트가 강제했고 사전등록이 기록하는 캘리브레이션이다.

**말웨어 (EMBER).** 2018-조밀 월별 윈도우에서 이 셀은 인증서-등급이다: DEPLOYMENT-DECAY-
COVARIATE, D = 0.834(식별가능 — null이 전체 하중을 진다), raw staleness −0.008 [−.009,
−.007](old 데이터가 도움), denoised −0.004, 게이트 조용, recency gain +0.031(floor 위),
검출 하한 0.0013. ("인증서-등급"은 크기가 아니라 식별가능성에 대한 서술이다: 기제 라벨은
floor 위의 recency gain과 유의하게 음수인 staleness에 의지하며, 무증거-대역 규율은 sub-floor
*양성*에 적용되는 것이라 여기서 발동하지 않는다.) 말웨어의 시간적 열화(Pendlebury et al.,
2019)는 실재하고 recency로 회복
가능하지만, 커버리지-기인이다 — 새 패밀리가 등장한다; old 라벨은 부패하지 않는다 — 그리고
배포 렌즈는 정확히 그 구별을 긋는다: old 데이터를 보관하라, 그래도 열화는 예상하라,
커버리지를 위해 재학습하라. (126개 희소 월의 전체-이력 윈도잉은 더 낮은 검정력에서 같은
null을 주고, 19행 윈도우 위의 잘못-윈도잉된 시도는 판정 발급을 올바르게 거부했다.)

**WhyShift 브리지 (연도별 ACS).** WhyShift는 ACSIncome에서 *주(state)들* 간 Y|X-shift가
만연하다고 보고했다; 같은 과제를 *연도들* 간(캘리포니아, 2014–2018, 연 단위 윈도우, 10
시드)으로 계기에 돌리면 지도의 최고-검정력 null이 나온다: NO-STRONG-CONCEPT, raw −0.008·
denoised −0.007(둘 다 CI 음수 — old 연도들이 돕는다), 게이트 조용, D = 0.515(연도들이 피처
공간에서 거의 분리 불가 — 공변량 이동 미미, 완전히 식별가능), recency ≈ 0, 검출 하한 0.0008.
공간 축에서 Y|X-shift가 만연한 바로 그 과제가 5년의 시간 축에서는 조용하다 — 데이터셋만이
아니라 축이 shift 유형을 결정한다. (같은 셀에 overlap-기반 분해를 돌려도 일치한다:
within-overlap gap −0.009 ≈ placebo −0.008, cov-AUC 0.68 — 부록 B.2.) 이 셀은 prior-변화 컨트롤의 실데이터 유사물도 통과한다:
인플레이션 아래 고정 $50k 소득 임계값은 단조 양성률 램프(0.36→0.42)를 만들지만 계기는 그
prior drift를 concept으로 오독하지 않는다. (범위: 한 개 주, 5개 pre-COVID 연도; 다중-주
확장은 향후 과제.)

## 7. 토론

**실무자가 얻는 것.** raw staleness는 "old 데이터가 해가 되는가?"에 답한다; 수리된 계기는
"*왜*, 그리고 무엇을 할 것인가"에 셀 유형별 판단 규칙과 함께 답한다: envelope 내 denoised
발화 ⇒ 당신의 모델 클래스에 대해 규칙이 변했다 — old 라벨은 독이다, recent-only로
재학습하거나 시간 구조를 추가하라; 게이트 켜진 raw 발화 ⇒ 라벨의 노이즈 프로파일이 drift했다
— old 행은 self-training으로 *재사용 가능*하다(denoised arm이 문자 그대로 그 처방이며, 측정
되어 있다); 회복 인증서를 가진 null ⇒ 재학습은 규칙 변화가 아니라 커버리지를 쫓는다; 음의
recency ⇒ 레짐이 재발한다 — 당신의 친구는 recency가 아니라 보존이다; 공허하거나 불안정한
인증서 ⇒ 이 데이터셋의 drift 유형에 대해 모니터가 아예 발언하지 못하게 하라.

**지도가 의미하는 것과 의미하지 않는 것.** 0/8 집계는 산업 표 ML에 concept drift가 부재함을
말하지 *않는다*: **여덟 셀 중 둘이 인증서가 없고**(시도한 모든 신호 패밀리에서 주입 학습불능),
여덟 중 정확히 하나가 실명-인증되었다(homesite_insurance); 패널의 다른 earned blindness인
Electricity는 산업 8셋에 속하지 않는다. 따라서 집계는 감사 기준 0/8이되 정보량 기준
0/5이다; 실명은 부재가 아니고, 공허도 부재가 아니다. 그것이
말하는 것은 더 좁고, 우리가 논증하기로는, 더 유용하다: 시간-인지 표 아키텍처를 옹호하는 데
쓰이는 바로 그 벤치마크에서, **그 벤치마크를 지배하는 모델 클래스에 대해 측정 가능한
mean-rule drift는 존재하지 않으며, 지금까지의 모든 외견상 반례는 컨트롤 아래에서 용해되었다**
— 마지막 것은 라벨-노이즈 감쇠로, 단언이 아니라 진단으로. "시간 아키텍처 X가 TabReD류
데이터에서 concept drift를 착취한다"는 주장의 입증 책임에는 이제 식별가능성 인증서가 포함
되어야 한다.

**한계.** (1) 판정은 트리-앙상블-범위다; 우리는 이 범위 한정이 필요함을 보였지 모든 배포
시스템에 충분함을 보이지 않았다. 패널의 신경망 프로브(2-레이어 MLP)는 분리 배터리에 실패해
카나리아로 분류된다(§4.3); 현대 딥 표 아키텍처(FT-Transformer급, 프로덕션 스케일)는 미검이다
— 패널 확장이 가장 명확한 다음 단계이며, 그런 프로브의 drift 판정이 신뢰받기 전에 통과해야
할 사전등록 진입 관문이 배터리다. (1b) **스케일.** 프로브는 arm당 N ≤ 6,000행으로 학습한다;
산업 모델은 자릿수가 다른 규모로 학습한다. 훨씬 큰 N에서만 착취 가능한 규칙 변화는 여기서
보이지 않으므로, 모든 δ 하한과 0/8(인증 0/5) 집계는 *프로브 스케일에서의* 진술이다. 1차 δ(N) 스윕
(부록 B.1)은 가장 큰 null 셀 3개에서 arm 캡을 윈도우-기하 상한(N ≈ 14k–24k, 헤드라인
스케일의 최대 4×)까지 올렸다: 모든 판정이 불변이고 어떤 읽기도 floor로 향하지 않는다. 그
너머의 프로덕션 스케일 N은 향후 과제로 남으며, 그곳에서 지도가 달라질 가능성은 일축하지
않고 표기한다. 스케일은 두 번째 경로로도 들어온다: arm 크기가 다르므로(N 대 2N) 모든 staleness
읽기에 표본크기 이득이 접혀 들어간다. 직접 측정하면(부록 B.3) 이진 셀에서는 무시할 수준이나
회귀에서는 판정 floor의 61%에 이르며, 이는 지도의 회귀 셀 5개의 검출가능 하한을 실질적으로
올린다; 그 null들은 이 변위를 흡수하지 않고 명시한 채로 보고한다. (2) 고정
초기 앵커를 가진 K = 10 롤링 윈도우: 윈도우 폭보다 훨씬 짧은 시간 규모의 drift는 평균되어
사라지며(주입 컨트롤이 이를 부분적으로 측정한다 — 그 회복은 K에 따라 변한다), TabReD 지도는
공식 분할의 train 구간을 다루지 배포 held-out 구간을 다루지 않는다 — 다만 부록 B.4의
full-span 감사가 그 갭을 가로지르며 어떤 판정도 움직이지 않는다. (3) 유일한 견고 양성은
실험실-설계 drift다; 크기를 캘리브레이션할 자연 발생 산업 양성을 찾지 못했다 — 그것이 지도의
발견이자 약점이며, 그것을 닫으려는 시도가 한계 7이다. (4) envelope 상수(4.7)는 Gaussian 노이즈
패밀리로 캘리브레이션되었다;
heavy-tail 라벨 노이즈는 정확한 경계가 아니라 기권 규율만 상속한다. (5) TabReD는 시간 분할이
가능하도록 큐레이션되었다; 산업 데이터 일반으로의 외적 타당성은 우리가 내딛지 않고 표기만
하는 귀납적 걸음이다. (6) 주입 인증서는 단일 기준 패밀리로 캘리브레이션되었고, 여기서 빚졌던
그 스윕을 이제 실행했다(부록 B.5): 검정력은 **저분산 회전과 상호작용 규칙에 대해 인증**되고
**부분모집단-국소 규칙 변화에 대해서는 반증**된다 — weather에서 심어진 부분모집단-국소 규칙은
윈도우 내에서 여유 있게 학습가능한데(R² 0.560) 회복에 실패하며(−0.050) 확증 시드에서 재현되는
반면, 같은 기하가 저분산(+0.128)과 상호작용(+0.046) 규칙은 회복한다. 따라서 "검증된
무-concept"는 §5.2 전반에서 명시적 패밀리 분모를 달고 있으며, 인증서의 상대성은 단서가 아니라
**측정된 성질**이다.

(7) **계기는 순위를 바꾸지 않고 문턱만 옮기는 규칙 변화에 눈이 먼다.** 우리는 계기를 외부
문서로 확정된 규칙 변경에 걸어 시험했다: ACA 메디케이드 확대로, 펜실베이니아는 2015-01-01에
시행했고 텍사스는 채택한 적이 없으며, 과제는 ACS 공적보험 2014–2018이다(부록 B.6). 확대는
펜실베이니아의 양성률을 0.234에서 0.306으로 올렸고 계단은 시행 연도에 섰으며, 텍사스는 0.2pp
안에서 평평했다. 계기는 **두 주 모두 null**로 읽었다 — 완전 식별가능 영역(D = 0.53)에서, 이
논문 전체에서 가장 타이트한 검출 하한(δ = 0.00083)으로. 저전력 null이 아니라 자신 있는 0이며,
그래서 이 미검출은 더 날카롭다. 기제는 실행 전에 등록되었고 실행이 그것을 확인했다: 이진
점수가 AUC이고 AUC는 순위 기반인데, 자격 문턱은 아무의 순위도 바꾸지 않으면서 많은 사람을
결정 경계 너머로 옮길 수 있다. 같은 행을 proper score로 다시 재면 처치-대조 분리가 AUC의
3.1×에서 Brier 6.3×, log-loss 6.6×, 규칙-이동 발산 15×로 벌어진다. 그러나 크지는 않다: 모든
읽기가 두 계기의 결정 floor 아래에 남고 대조 주도 placebo-유의한 gap을 내므로, 해석 가능한
것은 처치/대조 **비율**뿐이다. 따라서 정직한 진술은 "지표가 미검출을 설명한다"가 아니라
"지표가 신호를 6~7배 압축하며, 압축을 풀어도 이 정책 변화는 우리 floor 아래에 있다"이다. 두
가지가 따라온다. §5의 모든 null은 **순위 기반 점수에 범위 한정된 것**으로 읽어야 하며, 그것은
순위·트리아지 시스템에는 올바른 범위이고 이 과제가 대표하는 고정-문턱 자격 심사 시스템에는
틀린 범위다. 그리고 심어진 효과와 no-drift 앵커로 캘리브레이션된 결정 floor 0.02는 **실제
인구 규모의 정책 규칙 변화보다 크다** — 부록 C에 그 상수에 대한 실측 비판으로 기록하되 조치하지
않는다. 결과를 본 뒤 사전등록 임계값을 바꾸는 것이야말로 이 논문이 반대하는 관행이기 때문이다.

**과정에 관하여.** 이 프로젝트의 장부에는 현재 결과 이전에 정밀 검토 아래 용해된 아홉 건의
양성 발견이 기록되어 있다; 아홉 번째 용해가 §5.3이고, 그것은 측정 계기 스스로가 진단한 유일한
것이다. 우리가 지지하는 방법론적 주장은: 마침내 안정된 결과를 산출한 조합 — 실데이터 주장에
앞선 실행된 적대 null, 커밋-타임스탬프된 예측을 가진 사전등록 캐스케이드, 확증 fresh-seed
재현, 가정 대신 인증서, 그리고 카나리아 프로브 — 이 그것이 예방하는 용해들의 비용에 비해
저렴하다는 것이며, 우리는 감사 추적을 아티팩트의 일부로 공개한다.

**향후 과제.** ACS 브리지의 다중-주·2019-이후 확장(§6); denoised arm의 클래스-불변 조건
(어떤 프로브 패밀리가 *어떤* 건전한 staleness 읽기든 허용하는가?); δ(N) 스케일링 연구;
그리고 인증서 프로토콜의 스트리밍 모니터 이식 — 그곳에서 재발-레짐 지문(음의 recency)은
직접 행동 가능하다.

## 8. 재현성

모든 것이 하나의 저장소에 있으며, 심사용으로는 익명화된 링크로 제공된다. 계기는 단일
sklearn-전용 스크립트
(`scripts/run_deployment_decay.py`)다; 모든 확률적 단계는 시드되고, 모든 출력 아티팩트는
자신의 커밋 해시·argv·라이브러리 버전·UTC 타임스탬프를 내장한다. 이 스탬핑은 의례가 아니며,
그것을 원칙이 아니라 사고로부터 말할 수 있다: 한 번의 배터리 실행이 잘못된 인터프리터에서
수행되었다 — 셸 프롬프트는 의도한 conda 환경을 표시했으나 `PATH`는 무관한 프로젝트의
virtualenv로 해석되고 있었다. 판정은 통과했으므로 화면상으로는 아무 이상이 없었다; 아티팩트에
기록된 라이브러리 버전이 실데이터 실행 환경과 일치하지 않았고, 그 불일치만이 이것을 잡아냈다.
그 실행을 신뢰했다면 한 환경에서 인증된 게이트가 다른 환경의 측정을 허가했을 것이다 —
사전등록이 이미 한 번 기록하고 수리한 바로 그 실패다(§4.1). 이후 모든 실행은 인터프리터를
절대경로로 호출하고 다른 어떤 작업보다 먼저 그 버전을 assert한다. **컴퓨트.** 논문의 모든
실행은 CPU 전용이다 — 계기와 모든 프로브 클래스가 scikit-learn 모델이며 GPU는 어디에도
쓰이지 않는다 — 공유 멀티코어 Linux 서버 1노드에서 실행되었다(Python 3.11.15, scikit-learn
1.9.0, NumPy 2.4.6; 환경 freeze 커밋됨). wall-clock은 커밋된 phase 로그에서 재구성 가능하다:
주 사전등록 phase들은 하루에 phase-병렬로 ≈17시간, model-class 패널이 ≈2시간, 선택
셀들(EMBER, ACS 브리지, 카나리아 재실행)이 이틀에 걸쳐 ≈13시간; 어떤 단일 데이터셋 셀도 몇
CPU-시간을 넘지 않는다. 소스별 데이터 접근과 라이선스 조건은 부록 A에 표로 정리했다. 합성 배터리는 byte-재현
가능하고(같은 환경에서 `--synth` 재실행이 커밋된 아티팩트를 SHA-동일하게 재생성), raw arm의
RNG 스트림은 계기 버전 간 안정적이다(v2 헤드라인 수치가 v3 아래에서 비트 단위로 재현 — §5.3을
re-roll이 아닌 재해석으로 만드는 사실). 사전등록(`PREREG_DEPLOYMENT_V2.md`)은 임계값·
캐스케이드·시드 프로토콜·집계 읽기를 동결하며, 결과는 git 이력이 순서를 강제하는 읽기-전용
섹션으로 추가된다; 모든 서버 실행은 하나의 재개 가능 드라이버(`scripts/run_prereg_phases.sh`,
marker 기반, phase-병렬)로 재현되고, 헤드라인 수치를 산출한 머신의 환경 freeze가 커밋되어
있다. 등록된 절차와의 이탈 한 건을 공시한다: 패밀리 스윕은 구현 대조 뒤에 사람이 판단하는
체크포인트를 규정했는데, 그것을 실행한 배치가 체크포인트를 읽기 전에 네 신호 패밀리를 모두
계산해 버려 게이트가 실행-시점 필터가 아니라 판독-시점 필터가 되었다. 실제로 배제된 패밀리는
없었으나(대조가 4개 모두에서 회복) 순서가 커밋된 것과 달랐고, 기록은 그렇게 말한다. 데이터 접근: TabReD는 Kaggle 인증과 대회별 규칙 동의를 요구한다; elec2는 OpenML에서
받는다; INSECTS와 river 패널은 `river`로 설치된다; EMBER-2018은 공개 아카이브에서 내려받아
의존성 없는 어댑터로 파싱된다. sklearn 릴리스 간 HistGradientBoosting의 정확한 cross-version
bit-재현성은 주장하지 *않는다*; 시드 집합 간·HGB/RF 간 판정-수준 안정성은 주장한다(각각
10/10, §5).

## 부록 A. 데이터 접근과 라이선스

각 배포처 기준 2026-07-18 검증:

| 소스 | 파이프라인 내 접근 경로 | 라이선스 / 조건 |
|---|---|---|
| TabReD (산업 데이터셋 8개) | Kaggle, TabReD 전처리 도구 경유 | 대회별 Kaggle 규칙(동의 필요); TabReD 도구 Apache-2.0 |
| Electricity (elec2) | OpenML dataset 151 | OpenML 표기 "Public" |
| INSECTS (7 변종) | `river` 패키지로 fetch | `river` BSD-3-Clause; 데이터셋 출처 Souza et al. (2020) |
| river 합성 스트림 (SEA/Agrawal/STAGGER/Sine/Hyperplane) | `river`로 생성 | BSD-3-Clause |
| EMBER-2018 | 공개 아카이브 다운로드; 자체 무의존 파서 | 데이터 파일 MIT (ember *코드*는 AGPL-v3, 미사용) |
| ACS (folktables 브리지) | `folktables` | folktables MIT; ACS PUMS = 미국 센서스국 공개 데이터 |

## 부록 B. 강건성·경화, 그리고 외부 시험 하나

B.1–B.3은 사전등록 감사가 동결된 *이후* 실행되었고, 계기·캐스케이드·임계값은 불변이다
(커밋-스탬프 아티팩트); 사전등록 셀이 아니라 강건성 점검이다. B.4–B.6은 **실행 전에
사전등록**되었으며 — 판독 규정과 셀별 예측이 타임스탬프와 함께 실행 앞에 커밋되었다 —
어느 쪽으로 나왔든 그대로 보고한다. 어느 것도 임계값·캐스케이드·시드 프로토콜을 바꾸지 않는다.

**B.1 δ(N): N이 커져도 지도는 floor 쪽으로 움직이지 않는다.** 프로브의 헤드라인 스케일은
N ≤ 6,000이다(§7 한계 1b). 가장 큰 null 셀 3개를 arm 캡 {1,500, 24,000, 96,000}으로
재실행했다(각 10시드). 윈도우 기하가 실현 가능한 arm 크기를 제한하므로(N = min(|recent|,
|old|, cap), |window| ≈ n/K) 실현 상한은 homecredit_default ≈17,200(96,000 캡도 동일 N),
weather 24,000(캡-제한), maps_routing ≈14,300이다. 판정은 모든 N에서 불변이고, 어떤 읽기도
0.02 floor로 향하지 않는다:

| 셀 | 실현 N | raw staleness | denoised | 판정 |
|---|---|---|---|---|
| homecredit_default | 1,500 | −0.015 [−.018, −.012] | +0.007 [+.005, +.008] | UNIDENT (공허) — 불변 |
| homecredit_default | 6,000 (§5.2 표) | −0.007 | +0.004 | 불변 |
| homecredit_default | ≈17,200 (캡 24k) | −0.013 [−.016, −.011] | +0.006 [+.004, +.007] | 불변 |
| homecredit_default | ≈17,200 (캡 96k) | −0.010 [−.012, −.009] | +0.005 [+.003, +.006] | 불변 |
| weather | 1,500 | −0.017 [−.019, −.015] | −0.022 [−.024, −.020] | 불변 |
| weather | 6,000 (§5.2 표) | −0.012 | −0.011 | 불변 |
| weather | 24,000 | −0.005 [−.006, −.005] | −0.002 [−.002, −.002] | 불변 |
| maps_routing | 1,500 | −0.009 [−.010, −.009] | −0.017 [−.017, −.016] | NO-STRONG-CONCEPT |
| maps_routing | 6,000 (§5.2 표) | −0.008 | −0.010 | 불변 |
| maps_routing | ≈14,300 (캡 24k) | −0.008 [−.009, −.008] | −0.008 [−.009, −.008] | 불변 |

raw staleness는 세 셀 모두 모든 N에서 유의하게 *음수*다(old 데이터가 돕는다); 유일한
sub-floor 양수(homecredit의 denoised arm)는 판정 floor의 1/4 수준에서 11×의 N 범위에 걸쳐
평탄하고 범위 상단에서 축소된다; weather의 denoised arm은 아래에서 ≈0으로 수렴한다. 주입
인증서도 재현된다(homecredit/weather의 심은 규칙은 여전히 학습불능 — 공허; maps는 식별가능).
범위는 불변: K = 10 윈도우 기하가 실현 N을 여기서 ≈14k–24k로 제한하므로, 그 너머의 프로덕션
스케일 N은 미검으로 남는다.

**B.2 overlap이 성립하는 곳에서의 렌즈 간 일치.** 분리도가 overlap-기반 분해를 적용 가능하게
남겨두는 지도의 두 셀(maps_routing D = 0.58; ACS 브리지 D = 0.515)에서, WhyShift-식
within-overlap concept 분해(ess-게이트)를 배포 렌즈 옆에 나란히 돌렸다. 두 계기는 일치한다:

| 셀 | 배포 렌즈 | within-overlap gap [CI] | 비고 |
|---|---|---|---|
| maps_routing (full, 986피처) | NO-STRONG-CONCEPT (den −0.010) | −0.003 [−.004, −.002] | 5/5 시드 measurable, ESS 93% |
| maps_routing (sparse MI@5/10/20/50) | 불변 | −0.005 … −0.005, 전부 ≈0 | 모든 표현에서 measurable |
| ACS CA 2014→2018 | NO-STRONG-CONCEPT (den −0.007) | −0.009 (placebo −0.008) | cov-AUC 0.68; gap ≈ placebo |

overlap이 성립하는 곳에서 overlap 렌즈와 배포 렌즈는 같은 판정을 돌려준다; 이 논문이 기록한
계기 불일치(§3–4)는 정확히 overlap이 무너지는 곳에서 발생한다 — 판정이 인증서를 지녀야 하는
이유다.

**B.3 표본크기 항, 논증이 아니라 측정.** staleness는 N개 최근 행으로 학습한 모델과 2N개
(최근 ∪ 옛)로 학습한 모델을 비교하므로, 관측되는 차이는 두 효과의 합이다: 학습셋이 두 배가
되는 *이득*(drift와 무관하게 staleness를 음으로 밀고)과 모순되는 옛 라벨의 *해악*(양으로 민다).
관측되는 것은 합뿐이다. red-team의 지적은 보고된 모든 staleness — 따라서 모든 δ — 가 첫 항만큼
어긋나 있다는 것이다. 이전 판본은 산문으로만 기록된 편향 스윕으로 답했다. 이제 계기가 arm별
*절대* 점수를 방출하므로, 규칙이 고정이고 **동시에** 옛 행이 최근 행과 동분포인 배터리 셀에서
이 항을 직접 잴 수 있다 — 그 셀에서는 관측된 차이가 곧 표본크기 항이다:

| 배터리 셀 | task | recent → recent ∪ old | 크기 항 | floor 대비 | 윈도우별 (n = 25) |
|---|---|---|---|---|---|
| synth_stable | binclass (AUC) | 0.9895 → 0.9902 | **+0.00074** | 4% | SD 0.0016, 부호 혼재 |
| synth_covariate | binclass (AUC) | 0.9577 → 0.9583 | +0.00064 | 3% | SD 0.0019, 부호 혼재 |
| synth_covariate_mild | binclass (AUC) | 0.9631 → 0.9665 | +0.00340 | 17% | SD 0.0033 |
| **synth_reg_stable** | **regression (z-RMSE)** | −0.1343 → −0.1222 | **+0.01212** | **61%** | SD 0.0048, **25개 전부 양수** |

두 가지 읽기가 있고, 불리한 쪽을 먼저 적는다. **회귀에서 이 항은 실재하고 크다**: 판정 floor의
61%이며 25개 윈도우 전부에서 양수이므로 단일-윈도우 아티팩트가 아니다. 진짜 규칙-변화 해악이
+0.012인 회귀 셀은 ≈0으로 관측되어 null로 읽힌다. 따라서 지도의 회귀 셀들(sberbank_housing,
cooking_time, delivery_eta, maps_routing, weather)의 검출가능 하한은 실질적으로 이만큼 올라가
있으며, 그 null들은 이 변위를 감안해 읽어야 한다(§7 한계 1b). **이진 분류에서는 무시할
수준이다** — floor의 3~4% — 그러나 얼버무리고 싶지 않은 단서가 있다: 배터리의 이진 셀은
AUC ≈ 0.99로 천장에 충분히 가까워 크기 이득이 압축되는데, 산업 이진 셀은 거기 있지 않다.
**따라서 이진 하한은 측정된 그대로 이전되지 않는다.** arm별 절대값은 이제 모든 실행에서
방출되므로, 같은 양을 다음 실행 때 산업 패널에서 배터리로부터 논증하는 대신 직접 측정한다.

**B.4 배포 갭: 지도는 그것을 건너도 움직이지 않는다.** 본 지도는 TabReD 공식 시간 분할의
train 구간을 다룬다(§7 한계 2). 여덟 셀 전부를 train+val+test를 공유 정규화 timestamp로 연결해
재실행했고 — 그러면 윈도우가 공식 분할이 hold-out 하는 갭을 가로지른다 — 탐색 후 확증 시드로
재확인했다(**8/8 판정 동일**):

| 셀 | train-span 판정 | full-span 판정 | full-span raw / denoised | D |
|---|---|---|---|---|
| sberbank_housing | NOISE-DRIFT-CONFOUNDED | UNIDENTIFIABLE (raw 미발화) | +0.006 / −0.022 | 1.000 |
| cooking_time | INJECTION-RECOVERED | NO-STRONG-CONCEPT (D가 게이트 아래로) | −0.009 / −0.015 | 0.927 |
| delivery_eta | INJECTION-RECOVERED | INJECTION-RECOVERED | −0.014 / −0.016 | 0.999 |
| maps_routing | NO-STRONG-CONCEPT | NO-STRONG-CONCEPT | −0.007 / −0.011 | 0.630 |
| ecom_offers | UNIDENTIFIABLE *(공허)* | UNIDENTIFIABLE *(공허)* | −0.009 / −0.009 | 1.000 |
| homecredit_default | UNIDENTIFIABLE *(공허)* | UNIDENTIFIABLE *(**실명**)* | −0.003 / **+0.018** | 1.000 |
| homesite_insurance | UNIDENTIFIABLE-INERT *(불안정)* | UNIDENTIFIABLE-INERT *(**실명**)* | −0.004 / −0.001 | 1.000 |
| weather | UNIDENTIFIABLE *(공허)* | UNIDENTIFIABLE-INERT | −0.014 / −0.013 | 1.000 |

배포 갭을 가로질러도 산업 셀은 하나도 CONCEPT를 발화하지 않으므로 집계는 train 구간으로
재스코프되지 않는다. 셋은 묻지 말고 기록할 만하게 움직인다. sberbank는 더 긴 구간에서 raw arm이
floor를 못 넘어 §5.3의 노이즈 진단이 train 구간에 범위 한정된다. cooking_time의 분리도는 더 긴
구간이면 오를 것이라는 예상과 **반대로 게이트 아래로 떨어져**, 인증서가 필요 없는 식별가능 null이
된다. 그리고 homecredit_default의 denoised는 **+0.018** — floor의 89%이자 이 논문 전체에서 가장 큰
양수 방향 수치 — 인데 인증서가 거부가 아니라 *실명*이라, "안 봤다"가 아니라 "재봤고 floor 아래"로
읽힌다.

**B.5 패밀리 × 캐리어: 인증서가 무엇에 대해 인증되었는가.** 기준 주입은 최고 분산 두 피처에
회전을 심는다. 시험하려던 네 규칙 기하 중 셋이 그대로면 같은 캐리어를 물려받았을 것이고 —
ecom/homecredit/weather의 주입 공허 원인으로 진단된 것이 바로 그 열들의 두꺼운 꼬리이므로 —
스윕은 규칙 기하와 캐리어를 교차한다: `topvar@hi`(기준), `lowvar@lo`, `interaction@lo`,
`subpop@lo`. 설계된-drift 스트림 위의 구현 대조가 네 조합 모두에서 회복하므로
(+0.201 / +0.211 / +0.156 / +0.077), 아래의 미회복은 코드가 아니라 셀의 성질이다.
회복된 staleness, "vac" = 주입 학습불능(사용 가능한 수치 없음):

| 셀 | topvar@hi | lowvar@lo | interaction@lo | subpop@lo | 판독 |
|---|---|---|---|---|---|
| cooking_time | +0.546 | +0.467 | +0.357 | +0.131 | 검증됨, 4/4 |
| delivery_eta | +0.332 | vac | +0.274 | vac | 검증됨, 학습가능 2/2 |
| elec2 | +0.018 | −0.003 | −0.001 | −0.002 | 실명 획득, 4/4 |
| homesite_insurance | vac | −0.001 | vac | −0.003 | 실명 획득, 학습가능 2/2 |
| weather | vac | **+0.183** | **+0.083** | 학습가능, **−0.009** | 검증됨, 학습가능 3개 중 2개 |
| ecom_offers | vac | vac | vac | vac | 인증서 없음 |
| homecredit_default | vac | vac | vac | vac | 인증서 없음 |
| insects (양성대조) | +0.162 | +0.265 | +0.229 | +0.047 | 회복, 4/4 |

기록을 넘는 결과가 둘이다. 첫째, weather의 기준-패밀리 규칙은 그 기하에서 학습불능인데
(윈도우 내 R² −0.022) 저분산 규칙은 R² 0.674로 학습가능하며 회복한다 — 단일 패밀리 프로토콜이면
미인증으로 남겼을 셀이 캐리어를 넓히자 인증되고, 그래서 인증 셀 수가 4가 아니라 5가 된다.
둘째, 반대 방향으로: 같은 셀의 full-span 변종에서 부분모집단-국소 규칙은 여유 있게 학습가능한데
(R² 0.560, 게이트 0.20에서 멀다) 여전히 회복에 실패하고(−0.050) 확증 시드에서 재현되는 반면,
저분산(+0.128)과 상호작용(+0.046) 규칙은 동일한 윈도우를 통해 회복한다. 따라서 인증서의
상대성은 우리가 주장하는 단서가 아니라 **우리가 측정한 경계**다: 이 계기는 피처 *방향*이 실어
나르는 규칙에는 검정력이 있고 *부분모집단 소속*이 실어 나르는 규칙에는 없다.

**B.6 계기가 보지 못하는, 외부 문서로 확정된 규칙 변화.** ACA 메디케이드 확대는 날짜와 범위가
데이터 밖에서 고정되고 채택이 주마다 다른 규칙 변화를 준다 — 즉 처치와 대조가 같은 과제·같은
연도·같은 계기 안에 존재한다. 펜실베이니아는 2015-01-01 시행, 텍사스는 2018년까지 미채택이다.
ACS 공적보험 과제(folktables, 2014–2018, 연 단위 윈도우)에서 설계는 데이터에 잡힌다 —
펜실베이니아 양성률이 0.234 → 0.268 → 0.293 → 0.305 → 0.306으로 시행 연도에 계단이 서고,
텍사스는 전 구간 0.183–0.185에 머문다:

| | 판정 | raw | denoised | recency | D | δ |
|---|---|---|---|---|---|---|
| 펜실베이니아 (처치) | NO-STRONG-CONCEPT | −0.011 | −0.009 | **+0.009** | 0.534 | **0.00083** |
| 텍사스 (대조) | NO-STRONG-CONCEPT | −0.015 | −0.011 | −0.001 | 0.523 | 0.00200 |

둘 다 null이다. 둘을 가르는 유일한 arm은 recency gain으로, 처치 주에서 양수이고 CI가 0을
제외하며 대조 주에서는 0과 구분되지 않는다 — 방향은 맞고, 여전히 floor 아래여서 판정은 움직이지
않는다. 이 null은 기본 행 솎기를 제거해도 유지되고, model-class 패널은 여섯 라벨 중 셋을
sub-floor 대역으로 옮기지만 어떤 판독도 바꾸지 않는다(대조 주의 선형-프로브 sub-floor 값이 실은
처치 주보다 *크므로* 그 패널에서는 아무것도 주장할 수 없다).

같은 행을 proper score로 다시 재면 미검출의 **일부**가 국소화된다. B.2의 within-overlap 렌즈를
깨끗한 pre/post 쌍(2014 vs 2018)에 순열 placebo와 15시드로 적용하면, placebo-보정 gap의
처치-대조 분리가 AUC에서 3.1×, Brier 6.3×, log-loss 6.6×, 규칙-이동 발산 15×다; 펜실베이니아는
지표 불변(네 지표 전부 양수)이고 텍사스는 아니다. 같은 렌즈 계열의 독립 실행이 시간 대비를
재현한다(+0.021 vs +0.003). 그러나 효과가 커지지는 않는다: 모든 값이 두 계기의 결정 floor 아래에
남고 대조군도 자체의 placebo-유의 gap을 내므로, 해석 가능한 것은 비율뿐이다. 따라서 결론도 그만큼
경계된다 — 순위 기반 점수가 이 신호를 6~7배 압축하며, 압축을 풀어도 이 변화는 우리 floor 아래에
남는다. 이에 따르는 범위 진술과 floor 상수에 대한 함의는 §7(한계 7)과 부록 C에 있다.

## 부록 C. 판정 상수: 캘리브레이션 출처와 유효 범위

캐스케이드의 모든 판정 상수를, 어디서 캘리브레이션되었고 어디서 유효성이 끝나는지와 함께.
계기는 숫자를 재사용하는 게 아니라 이 캘리브레이션 레시피(§4.1 배터리 + §6 no-drift 앵커)를
재실행함으로써 새 도메인에 이식된다.

| 상수 | 값 | 역할 | 캘리브레이션 | 유효 범위 / 밖에서의 거동 |
|---|---|---|---|---|
| 판정 floor | 0.02 | CONCEPT 크기 문턱 (AUC/accuracy/z-RMSE 공유) | 배터리 concept 크기(심은 효과 = floor의 10–27×); 지표별 재스케일에 셀 이동 0 (§4.1) | sub-floor CI-양성 = 무증거 대역 (no-drift 앵커로 캘리브레이션; 2/5가 floor의 1/10–1/20에 착지). **실측 비판:** 실제 인구 규모 정책 규칙 변화보다 크다 (§7 한계 7) — 아래 주석 참조 |
| 노이즈 게이트 | 1.5 | 라벨-노이즈-drift 플래그 (old 프록시/recent 중앙값) | 안정 합성 컨트롤 0.75–0.99; 노이즈-drift 셀 3.5–3.9 | 실데이터 값 전부 1.5 미만 또는 2.0–2.9 — envelope 근처 없음 |
| envelope | 4.7 | denoiser 유효 경계; 위에서 기권 | 고정-규칙 null 실측: 4.72에서 +0.014, 5.71에서 +0.026(floor 돌파) | Gaussian 패밀리 한정; heavy-tail은 기권 규율만 상속 (§7 한계 4) |
| D* | 0.96 | 주입 컨트롤로의 분리도 라우팅 | group-aware·크기맞춤 수리 (중복 0.994→≈0.50; shuffle-D 0.94→0.50) | 라우팅 통계량일 뿐, support overlap 아님; D ≥ D*는 단독으로 아무 결론 없음 |
| 학습가능성 게이트 | AUC 0.65 / R² 0.20 / acc 다수+0.10 | 주입-인증서 유효성 | junk-기하 컨트롤 실행 (윈도우 내 AUC 0.506 → vacuous) vs 학습가능 컨트롤 (0.964 → 회복 +0.195) | 학습불능 ⇒ 그 패밀리에 대한 인증서 거부 — 실명이 아니라 공허 |
| 주입 강도 | 2.5 rad | 기준 회전 크기 | 학습가능 기하에서 floor 통과 (회복 +0.16 ~ +0.55) | 패밀리 스윕 실행됨(부록 B.5): 저분산·상호작용 규칙에 대해 인증, **부분모집단-국소에 대해 반증** (§7 한계 6) |
| 시드 프로토콜 | 10 (0–9) + 확증 (100–109); 배터리 5 | 검정력·안정성 | 배터리 효과가 floor의 10–27×라 5로 충분한 pass/fail 관문 | 시드 집합 간 판정 이동 시 배제 (unstable) |

**판정 floor에 관한 주석 (기록하되 조치하지 않음).** 사전등록은 floor가 실제 정책 규모의 규칙
변화보다 크다고 판명되면 그 사실을 이곳에 상수에 대한 실측 비판으로 남기도록 규정했다.
그렇게 되었다. ACA 메디케이드 확대는 감사 대상 인구에서 펜실베이니아의 공적보험률을 7.2pp
움직였고 계단은 시행 연도에 섰는데, 우리가 돌린 어떤 지표·어떤 프로브 클래스에서도 0.02를
넘는 읽기를 내지 못했다(§7 한계 7; 부록 B.6). floor는 그 값의 10–27× 크기의 심은 효과와
1/10–1/20 크기의 no-drift 앵커로 캘리브레이션되었고, 이 사건은 **두 캘리브레이션 영역 사이의
빈 구간**에 떨어진다 — 결정 상수가 가장 방어하기 어려운 지점이다. 우리는 그것을 바꾸지 않는다:
뒤집었을 결과를 본 뒤에 사전등록 임계값을 고치는 것이 이 논문이 반대하는 관행이다. 대신 따라오는
것은 범위 진술이다 — 이 논문의 모든 CONCEPT 판정은 "대규모 실제 정책 변화가 도달하지 못하는
크기 이상"을 뜻한다 — 그리고 계기를 이식하려는 사람을 위한 구체적 재캘리브레이션 표적이다:
floor를 심은 회전과 null 스트림만이 아니라 **대상 도메인의 문서화된 규칙 변화**에 앵커하라.

## 참고문헌

영문본(PAPER_DRAFT_V4.md §References) 참조 — 서지 정본은 영문본에서 관리 (검증 완료
2026-07-15; BibTeX = `paper/main.bib`).
