# 리뷰 라운드 6 대응 기록 (2026-07-18) — "리뷰어 2" 적대 리뷰 (Reject 권고)

> 입력 = 실전형 적대 리뷰 (6개 리젝 사유 + 세부 코멘트, "1·5는 프레이밍 수정 없이 못 막는다"
> 주장). 대응 수위 = **B (문장급 수정 + 실험 2건)**, 사용자 선택. 반영 = **v4.5** (EN→KO→tex,
> 재컴파일 클린) + `run_deployment_decay.py`에 실험 옵션 2종 구현·스모크 통과.

## 사유별 최종 판정

| # | 리뷰어 주장 | 판정 | 대응 |
|---|---|---|---|
| 1 | 재발-레짐 맹목 ↔ 시간-아키텍처 동기 불일치 (치명적) | **절반 반박 / 절반 수용** | 반박(신규 실측): 산업 10셀 전부 recency ≥ 0 (+0.000 maps ~ +0.325 sberbank; phase234 아티팩트) — 재발 지문(진동 스트림 −0.023~−0.058로 검증된 신호)이 **어디서도 발화 안 함** → "맹점이 곧 논쟁 지점"의 경험 근거 부재. §6에 이 문장 추가. 수용: "주장하지 않는 것"에 딥-아키텍처 비판정 문장 + **배포 held-out 구간 미감사는 진짜 구멍 → 실험 [C]** (`--tabred-span full` 구현) |
| 2 | estimand 3중 협소화가 결론 선결 | **부분 수용** | 본문은 조건화돼 있었으나 초록 볼드에 클래스 스코프 부재 → "for the deployed tree-ensemble class" 삽입. "단조 한정"은 정의가 아니라 공개된 감도 프로파일이므로 그 부분은 기각 |
| 3 | 주입 인증서 = 단일 신호 패밀리 | **수용 (전 라운드 최강 지적)** | 즉시: §3.2+§7(6)에 reference-family 상대성 명시("verified no-concept = 그 패밀리에 대한 검증"). 실험 [D]: `--inj-family {lowvar, interaction, subpop}` 구현·스모크(4패밀리 전부 학습가능 AUC 0.95±, subpop 라벨 20% 분기, topvar 회복 재현) → 인증서 셀 전체 sweep |
| 4 | 자가 캘리브레이션 상수 탑·다중성·자가 타임스탬프 | **대부분 기지** | 논문이 자백(§4.2 envelope, §3.1 CI, §8) + OSF/서명태그 큐 존재. 다중성: 판정 = CI+floor+게이트+확증재현 합의, p-값 의존 아님 (rebuttal 논거). 이식성: 배터리 자체가 재캘리브레이션 레시피 |
| 5 | 9건 용해 = "양성 죽이는 최적화" 역독해 | **대부분 반박** | ① 사전등록이 sberbank 철회-예측 실패 시 *생존 배터리*를 사전 규정(§5.1) — 정반대 설계 증거, ② FNR은 합성서 정량(9/9, 0.8-rad +0.108, 공존 58%), ③ INSECTS = 실데이터 양성. 자연 발생 산업 양성 부재는 이미 한계 (3) |
| 6 | 신규성 수렴 | **기각** | TMLR 기준(novelty 문턱 없음); novelty 자가 한정 완료 |
| 세부a | EMBER "인증서-등급" ↔ 무증거 대역 긴장 | **오적용이나 명확화** | 규율은 sub-floor *양성* 대상; EMBER staleness는 음수. §6에 "식별가능성이지 크기가 아님" 괄호 추가 |
| 세부b | sberbank 대안가설(sub-floor 공존) | **수용(헤지)** | §5.3에 "sub-floor 주장은 어느 방향으로도 안 함" 괄호 추가 |

## 반영된 문장급 수정 (v4.5, EN·KO·tex 일관)

초록 0/8 볼드 클래스 스코프 / "주장하지 않는 것" 딥-아키텍처 문장 / §6 재발-지문 산업 셀
전부 침묵(실측 수치) / §3.2 인증서 family-상대성 / §7 한계 (6) 신설 / §5.3 sub-floor 헤지 /
§6 EMBER 명확화.

## 실험 2건 (코드 구현·스모크 완료, 서버 실행 대기 — 커맨드는 NEXT_TAB)

- **[C] 배포 held-out 구간 감사** (사유 1의 잔여 절반): `--tabred-span full` — train+val+test를
  공유 정규화 timestamp로 연결·정렬, 윈도우가 배포 갭을 가로지름. 행 태그 `_fullspan`.
- **[D] 주입-패밀리 sweep** (사유 3): `--inj-family lowvar|interaction|subpop` — 저분산 2피처
  회전 / 상호작용항 회전 / 부분모집단(z(f2)>0)-국소 회전. 학습가능성 게이트 불변(학습불능
  패밀리는 그 패밀리에 대해 VACUOUS). 행에 `injection_family` 기록. 사전등록 배터리(--synth)
  경로는 불변(비트-재현 보존).

판독 계획: [C] = 지도 판정이 배포 구간 포함 시 유지되는가(부록 B.3 후보). [D] = 인증서
삼분법이 패밀리에 걸쳐 유지되는가 — 유지되면 §7(6) 한계가 실측 방어로 격상, 흔들리면
"인증서는 패밀리-상대적"이 본문 주장으로 승격(어느 쪽도 논문이 정직해짐).
