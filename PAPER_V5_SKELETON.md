# PAPER V5 — 계기 우선 재구성 골격 (⚠ 2026-08-03부로 **역사 문서**)

> **이 파일은 "왜 재구성했나"의 기록으로만 읽을 것.** V5는 채택됐고 원고는
> `PAPER_DRAFT_V5.md`(EN)·`PAPER_DRAFT_V5_KO.md`(KO)가 정본이다.
> **특히 §5의 실험 표(E1~E4)는 실행된 것과 다르다** — 실제 실행은 `run_day4.sh` 헤더의 E1~E4이고
> **E4의 정의가 바뀌었다**(여기: ADWIN/DDM/KSWIN 범위 진술 / 실제: 앵커×주입 패밀리 스윕).
> 판독 정본 = `PREREG_DEPLOYMENT_V2.md` §19 · `PREREG_ACS_EXTENSION_2026-07-31.md` §12.
> 열린 항목 정본 = `REVIEW_LEDGER.md` §4.

**작성 2026-08-01.** 이것은 **골격이지 원고가 아니다.** `PAPER_DRAFT_V4.md`는 손대지 않았고,
V5를 채택하지 않기로 하면 이 파일만 버리면 된다. 새 실험도, 새 주장도 없다 —
**같은 사실을 다른 순서로 놓는다.**

---

## 0. 왜 순서를 바꾸는가 (한 문단)

V4는 두 개의 발견을 갖고 있는데 약한 쪽이 헤드라인에 있다.

**약한 쪽 (현 헤드라인)** — "산업 표 데이터에 착취 가능한 mean-rule drift가 없다(0/8, 인증 0/5)."
놀랍지 않고(TabReD 원저자가 시간 인지 방법의 무용을 이미 보고), 제약이 약하며, 이번 주에 한정어가
둘 더 붙었다: **"순위 기반 지표에서"**(ACS 반증), **"부분모집단 국소 변화 제외"**(weather_fs
subpop@lo). 헤드라인이 감당할 수 있는 무게를 넘었다.

**강한 쪽 (현재 §5.3에 묻혀 있음)** — "라벨 노이즈가 시간에 따라 *줄기만* 해도 손실 기반 drift
검출은 '규칙이 바뀌었다'는 판정을 낸다. 규칙이 증명적으로 고정된 데이터에서 +0.021을 만들어
냈고, 이는 실제로 보고되었던 산업 양성 +0.024와 통계적으로 구분되지 않는다." 놀랍고, 전달
가능하고, 다른 도메인으로 옮겨 간다.

그리고 **이번 주에 계기 쪽이 결정적인 것을 얻었다.** 재구성의 가장 큰 약점은 *"필드의 기본값은
신뢰할 수 없다"의 유일한 실증이 자기 카나리아인데 아무도 그걸로 drift를 검출하지 않는다*는
것이었다. ACS가 그 구멍을 메운다 — **이제 그 주장의 실증이 자기 자신이다.** 외부 문서로 확정된
실제 정책 규칙 변경을, 완전 식별가능 영역에서, 가장 타이트한 null로 놓쳤다. 자기 계기를 외부
정답으로 반증하고 맹점을 국소화한 논문은 그러지 않은 논문보다 강하다. **같은 사실인데
프레이밍에 따라 부호가 바뀐다** — 지도가 헤드라인이면 약점, 계기가 헤드라인이면 핵심 기여.

---

## 1. 제목·초록의 주어

| | V4 | **V5** |
|---|---|---|
| 제목 | *Is There Exploitable Concept Drift in Industrial Tabular Data? A Pre-Registered Identifiability Audit* | *Label-Noise Decay Mints Concept Drift: A False-Positive Channel in Loss-Based Drift Attribution, and What a Certified Instrument Looks Like* |
| 초록 주어 | "산업 데이터에 drift가 없다" | "**drift 검출이 노이즈에 속는다**" |
| 지도의 위치 | §5 = 결과 | §6 = 적용 사례 |

**초록 골자 (V5)**

1. 손실 기반 drift 귀속은 **규칙이 고정된 채 라벨 노이즈만 감쇠해도** concept 판정을 낸다.
   구성된 null에서 +0.021 — 실제로 발표되었던 산업 양성 +0.024와 통계적으로 구분 불가.
2. 수리: 교차적합 **denoised arm** + 노이즈 게이트 + **측정된** abstention envelope.
   14셀 사전등록 배터리로 검증(규칙 변화와 노이즈 감쇠 공존 셀 포함, 청정 검정력의 58%로 발화).
3. 인증서: 분리도·주입·**학습가능성**. 판정이 할 수 있는 말과 없는 말을 가른다.
4. **계기가 자기 과거 양성의 기제를 실데이터에서 진단한다** — 재현 실패가 아니라 원인 지목.
   선행 사례를 찾지 못했다.
5. **그리고 우리 계기의 맹점을 외부 정답으로 실측했다**: 문서화된 정책 규칙 변경(ACA 메디케이드
   확대)을 식별가능 영역에서 놓쳤고, 원인은 지표가 순위 기반이라는 것이며, 그 위험은 실행 전에
   문서화돼 있었다.
6. 적용: TabReD 산업 패널 감사. 인증서로 판독 가능한 5셀에서 이 클래스·이 지표가 볼 수 있는
   규칙 변화 0건, 2셀은 측정 불가로 **인증**.

---

## 2. 절 구조 — 무엇이 어디로 가는가

```
1  Introduction                      노이즈 채널을 첫 문단에. 지도는 마지막 기여로.
2  Related work                      V4 §2 거의 그대로 (검출기·DISDE·Hinder·Loog)
3  The failure channel               ★ 신규 절 = V4 §5.3 + §4.1 노이즈 셀을 승격
   3.1 노이즈 감쇠가 concept을 주조한다   구성된 null +0.021 vs 실제 양성 +0.024
   3.2 실데이터 자기진단 (sberbank)       + 신규: 검정력 인증서 (+0.086, strict 그림자)
   3.3 왜 게이트만으로는 부족한가         공존 셀이 여전히 발화해야 함
4  The repaired instrument           V4 §3 (estimand·denoised arm·게이트·envelope)
   4.1 estimand + 식별 경계            V4 §3.1 + DISDE Prop 1 재진술 (V4.7에서 복원)
   4.2 denoised arm과 노이즈 게이트     V4 §3.1 후반
   4.3 인증서 3종                      V4 §3.2
   4.4 캐스케이드                      V4 §3.3
5  Validation                        V4 §4 (배터리·envelope·클래스 매트릭스)
6  ★ Where the instrument is blind   ★ 신규 절 = 이 논문을 다르게 만드는 부분
   6.1 클래스 상대성                   V4 §4.3 + elec2 flip (linear·MLP)
   6.2 분리도 안에서의 검정력 붕괴       V4.7 §5.2 문단 (ρ=−0.47, elec2 within-dataset)
   6.3 신호 패밀리 상대성 — 측정됨       ★ weather_fs subpop@lo: 학습가능 0.560, 미회복 −0.050
   6.4 ★ 지표 상대성 — 외부 정답으로 반증  ★ ACS PA/TX. 여기가 절정
7  Application: the TabReD map       V4 §5 전체를 여기로 강등. 표·수치 그대로
   7.1 프로토콜 + 결과 (0/8 감사, 0/5 인증)
   7.2 full-span (배포 갭) — 판정 불변
   7.3 앵커와 감도 프로파일           V4 §6
8  Discussion                        V4 §7. 한계가 §6으로 승격됐으므로 대폭 축소
9  Reproducibility                   V4 §8 + 두 공시(환경 사고·사전등록 이탈)
Appendices                           V4 A/B/C + B.4 패밀리×캐리어 + B.5 full-span + B.6 ACS
```

**핵심 이동 3건**

| 무엇 | V4 위치 | V5 위치 | 이유 |
|---|---|---|---|
| 노이즈 오탐 채널 | §5.3 (결과 안의 한 문단) | **§3 = 독립 절** | 이게 논문이다 |
| 한계 1·4·6 (클래스·envelope·패밀리) | §7 한계 목록 | **§6 = 독립 절** | 한계가 아니라 **계기 특성의 지도**다 |
| 0/8 지도 | §5 = 결과 | §7 = 적용 | 놀랍지 않고 한정어가 무겁다 |

---

## 3. §6이 이 논문을 다르게 만드는 이유

V4는 "인증서 없는 drift 유형 귀속은 신뢰할 수 없다"고 주장하면서, 그 실증을 **한계 목록**에
흩어 놓았다. V5는 그것을 **하나의 절로 모으고, 자기 계기를 그 대상에 포함시킨다.**

§6의 네 항목은 전부 **실측**이며 전부 다른 축이다:

| 축 | 실증 | 가장 강한 증거 |
|---|---|---|
| **클래스** | 같은 데이터, 다른 프로브 → 다른 판정 | Electricity가 linear·MLP에서 CONCEPT, 트리에서 UNIDENT |
| **분리도** | 게이트 안쪽에서 검정력이 연속·급격히 붕괴 | ρ=−0.47 (p=.037); elec2 동일 데이터 D 0.905 → +0.190 vs D 1.000 → +0.018 |
| **신호 패밀리** | 같은 강도, 다른 규칙 클래스 → 다른 결과 | weather_fs: lowvar +0.128 ✓ / interaction +0.046 ✓ / **subpop 학습가능 0.560인데 −0.050 미회복** |
| **지표** ★ | **외부 정답이 있는 실제 규칙 변경을 놓침** | ACS PA(확대 2015-01-01) D=0.534, δ=0.00083 → null. 원인: AUC가 순위 기반 |

마지막 줄이 결정적이다. 앞의 셋은 **자기 도구로 자기를 잰 것**이고, 넷째는 **바깥에서 온 정답**
이다. 그리고 §5의 위험 1번에 실행 전 기록돼 있다 — 사후 변명이 아니다.

**§6.4가 반드시 지켜야 할 문언** (PREREG_ACS_EXTENSION §6(b) 그대로):
> "ACS에 drift가 없다"가 아니라 **"이 계기는 이 크기의 실제 규칙 변경을 못 본다"**이다.
> 사후에 데이터 탓으로 재해석하지 않는다.

---

## 4. 이 재구성이 해결하는 것 / 해결 못 하는 것

**해결**

| 문제 | 어떻게 |
|---|---|
| 분모 (0/8에 인증 없는 셀) | 0/8을 주장하지 않는다. 인증서 없는 2셀이 약점이 아니라 **§6의 사례**가 된다 |
| 딥 아키텍처 미답 | 아키텍처를 묻지 않으므로 답 안 한 질문이 없다 |
| "놀랍지 않다" | 노이즈가 규칙 변화로 위장한다는 것은 아무도 보고하지 않았다 |
| ACS 반증이 약점으로 읽힘 | §6.4의 **절정**이 된다 |
| 한정어의 무게 | 한정어가 헤드라인의 짐이 아니라 **§6의 내용**이 된다 |

**해결 못 함 (정직하게)**

- **§7 한계 3(자연 발생 산업 양성 부재)은 그대로 남는다.** ACS는 그것을 닫지 못했다.
- **필드의 실제 도구와의 head-to-head는 여전히 없다.** 초록의 마지막 주장("현재 모니터링의
  기본값은 신뢰할 수 없다")의 실증은 아직 자기 카나리아 + 자기 계기다. → **§5 남은 실험**
- **딥 프로브는 여전히 카나리아 등급.** 그 사실을 §6.1에 명시하고 넘어간다.

---

## 5. 이 골격이 요구하는 남은 실험 (선택, 우선순위 순)

| | 실험 | 비용 | 무엇을 닫나 |
|---|---|---|---|
| **E1** | **문턱 민감 지표로 ACS 재실행** (Brier/log-loss) | 코드 ~10줄 + 배터리 재게이트 + ACS 2셀 | §6.4를 "맹점 실증"에서 **"맹점을 국소화하고 수리 경로를 보였다"**로. 예측은 `PREREG_ACS_EXTENSION` §10.6에 커밋됨 (PA 발화 55%) |
| **E2** | **WhyShift·DISDE를 배터리 노이즈 셀에 통과** | 코드 0줄(스크립트 존재) + 수 시간 | 초록 마지막 주장의 실증을 **유형 귀속을 주장하는 실제 도구** 위로 옮김. 지금 유일하게 빈 곳 |
| E3 | within-cell D 사다리 (sparse-MI × 고-D 셀) | 코드 ~15줄 + 재게이트 | §6.2를 상관관계에서 **통제된 사다리**로 |
| E4 | ADWIN/DDM/KSWIN 범위 진술 | 수 시간 | 보조. 유형 귀속을 안 하는 검출기는 애초에 이 질문에 답하지 않는다는 범위 명시용 |

**E2가 가장 싸고 가장 크다.** 저장소에 `run_whyshift.py`·`run_disde_degeneration.py`가 이미
있고, 배터리의 `reg_early_noisy`(den +0.0045 @gate 3.53)·`reg_xdep_noise`(+0.0063 @3.72)에
통과시켜 **Y|X-shift로 귀속하는지** 보면 된다. 발화하면 초록의 마지막 문장에 처음으로
자기 도구 밖 근거가 생긴다.

---

## 6. 작업량 추정

| 단계 | 내용 | 기간 |
|---|---|---|
| A | §3 신설 (V4 §5.3 승격 + §4.1 노이즈 셀 흡수) | 2일 |
| B | §6 신설 (한계 3건 승격 + ACS 절 신규) | 3일 |
| C | §7 강등 (V4 §5+§6을 적용 사례로 재작성, 수치는 그대로) | 2일 |
| D | §1 초록·서론 재작성 | 2일 |
| E | 부록 B.4/B.5/B.6 신설 | 2일 |
| F | KO 미러 + LaTeX 재컴파일 + 전수 수치 대조 | 3일 |
| | **합계** | **약 2.5주** |

E1·E2를 넣으면 +1주.

**투고 목표에 대한 영향**: V4의 판정("지도로는 1티어 어렵다")은 지도에 대한 것이었다. V5는 다른
논문이므로 그 판정이 그대로 적용되지 않는다. 다만 **E2 없이는 초록의 마지막 주장이 여전히
자기 도구에만 서 있으므로**, 1티어를 노린다면 E2가 사실상 필수다.

---

## 7. 되돌리는 법

이 파일 하나 삭제. `PAPER_DRAFT_V4.md`/`_KO.md`/`paper/main.tex`는 V4.8 상태 그대로이며,
지금까지의 모든 사실 갱신(0/5, weather, homesite, sberbank 검정력 인증서, 두 공시)은
**프레이밍과 무관하게 살아남는 것들만** 반영했다. 재구성을 안 하기로 해도 손실이 없다.
