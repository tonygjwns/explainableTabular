# CLEANROOM_PROTOCOL — 앵커 없는 제3자 평가 설계 (2026-07-18)

> 동기: 이 repo에서 작업하는 탭은 CLAUDE.md·NEXT_TAB·리뷰 장부가 자동/반자동으로 로드되어
> **"논문은 자산, 지적은 방어 대상"이라는 앵커**를 갖고 시작한다. 실증: 채점 라운드에서 A3
> 방어의 sine_reoccur2 구멍을 방어자(이 탭)는 못 봤고 채점자가 잡았다. 또한 프롬프트 프레임이
> 결론을 결정하는 패턴이 관측됨 — "제대로 됐나?" → 긍정 편향, "기각해줘" → 좋은 비판이 나오나
> 억지 공격도 함께 제조(R6 사유 1 절반·R5 유령 인용). 이 프로토콜은 **양쪽 프레임 강요를 모두
> 피하는 대칭 설계**다.

## 팩: `C:\Users\joon\Desktop\paper_cleanroom\` (repo 밖, git 없음)

**포함 (평가 대상 그 자체만)**: main.pdf / PREREG_DEPLOYMENT_V2.md(§0–14) /
code 3파일(계기·분석·스트림) / artifacts(원시 json 전부 + kill-test + env freeze) /
MANIFEST.txt(중립 인벤토리).

**의도적 제외 (앵커)**: CLAUDE.md(자동 로드됨!), NEXT_TAB, RESULTS, REVIEW_ROUND2~6,
REVIEW_LEDGER, HANDOFF/PLAN류, KO본, .git(커밋 메시지가 서사 그 자체), tar.gz.
새 경로 = Claude 자동 메모리도 새 네임스페이스 = 이 대화의 판정 이력 미전달.

**운용 규칙**: 각 프롬프트는 **서로 다른 새 탭**에서. 탭 간 결과를 미리 알려주지 말 것.
저자 관계를 밝히지 말 것("나는 제3자" 프레임 유지 — 공손 편향 차단). 프롬프트에 이
프로토콜 파일을 보여주지 말 것.

## Stage 1 — 독립 3평가 (병렬 새 탭 3개)

### 탭 A: 백지 재구성 (프레임 강요 없음 — 핵심 탭)

```
이 폴더에는 어떤 연구의 산출물이 있다: 논문 PDF(main.pdf), 그것이 참조하는 사전등록 문서,
계기 코드(code/), 실행 원시 아티팩트(artifacts/). 나는 이 연구와 이해관계가 없는 제3자이고,
이 연구를 신뢰할 수 있는지에 대한 독립 판정이 필요하다. 아래 순서를 지키되, 각 단계의 답을
확정한 뒤에 다음 단계로 가라 (앞 단계로 돌아가 고치지 말 것):

1. 논문을 읽고 중심 주장을 너의 언어로 3문장 이내로 재진술하라. 논문의 용어를 그대로
   재사용하지 마라.
2. 논문을 덮고: 그 주장이 참이라고 믿기 위해 네가 요구할 최소 증거 목록을 독립적으로
   설계하라 (5~8항목).
3. 이제 실제 증거를 네 목록과 대조하라. 대조는 artifacts/의 원시 json을 직접 열어 수치를
   확인한 것만 인정한다 (논문 본문 인용만으로는 불인정).
4. 세 층위를 각각 독립 판정하라, 확신도(%)와 함께: (a) 이 질문은 물을 가치가 있는가
   (b) 실험 설계가 이 질문을 실제로 검증하는가 (c) 실행이 설계대로 수행되었는가.
5. 최종 분류: [그대로 제출 가능 / 부분 수정 / 프레임 재작성 / 폐기 후 재구축] 중 하나 +
   그 판단을 뒤집을 단일 관찰을 명시하라.

내 기분은 판정 대상이 아니다. 긍정 오판과 부정 오판의 비용은 나에게 대칭이다.
```

### 탭 B: 검찰 (폐기 케이스 최강화 — 단, 아티팩트 구속)

```
[폴더 설명 동일]. 너의 역할: 이 연구 프로그램의 '폐기 후 재구축'을 권고하는 최강의 케이스를
구축하라. 제약: (1) 모든 논거는 artifacts/에서 직접 확인 가능한 사실 위에만 세울 것 —
확인한 파일명·수치를 병기하라. (2) 각 논거마다 저자가 낼 수 있는 최선의 반론을 함께 쓰고,
그 반론이 왜 불충분한지까지 보여라. 반론을 이기지 못하는 논거는 스스로 버려라.
(3) 마지막에 정직하게 답하라: 이 폐기 케이스의 가장 약한 고리는 무엇이며, 전체 케이스는
[결정적 / 유력 / 정황적 / 빈약] 중 어디인가.
```

### 탭 C: 투자 심사 (행동 강제 — 말이 아니라 베팅)

```
[폴더 설명 동일]. 너는 연구 시간 1년을 배분해야 한다. 선택지: (A) 이 계기와 지도 위에 후속
연구를 쌓는다 (B) 이 주제를 유지하되 계기·프레임을 처음부터 다시 설계한다 (C) 이 주제를
떠난다. 하나만 골라라. 근거는 artifacts/ 수준에서 대고, 배분 비율(예: A 70/B 30)이 아니라
단일 선택을 강제한다. 선택을 바꾸게 만들 정보 1개를 명시하라.
```

## Stage 2 — 최종 판정 탭 (세 메모 수령 후)

```
독립 평가 메모 3개를 준다 [A/B/C 전문 붙여넣기]. 폴더는 같다. (1) 세 평가의 합의 지점과
충돌 지점을 분리하라. (2) 충돌 지점마다 artifacts/를 직접 열어 네가 판정하라 — 메모의 권위가
아니라 파일이 심판이다. (3) 최종 출력: "갈아엎어야 하는가?"에 예/아니오 + 확신도 +
'~가 관찰되면 뒤집힘' 목록. (4) 부칙: 세 메모 중 사실 오류(존재하지 않는 수치·파일 인용)가
있으면 적발하라.
```

## Stage 3 (선택) — 프로세스 감사

Stage 1·2가 "폐기 아님"으로 수렴한 경우에만: GitHub repo(읽기)와 RESULTS.md·리뷰 장부를
추가로 주고 "사전등록 주장·9건 용해 서사·6라운드 검증이 기록과 부합하는가"를 별도 탭에서.
(먼저 주면 '성실함 후광'이 Stage 1을 오염시킴.)

## 해석 지침

- **A·B·C가 모두 '폐기'** → 갈아엎기. 이 repo의 v4.x는 아카이브.
- **분산** (예: A 부분수정 / B 정황적 / C 선택 A) → Stage 2 판정 채택, 충돌 지점만 이 repo에
  환류해 개별 수정.
- **모두 '유지'여도** 각 탭의 "뒤집을 관찰 1개"들은 리버탈 카드 큐에 편입.
- 이 사이클의 기지 사실: 검증 *양식*을 바꿀 때마다 새 결함이 나왔다 (체크리스트 포화 →
  페르소나 전환에서 2건 → 채점 라운드에서 1건). Stage 1의 세 탭이 서로 다른 양식인 이유.
```
