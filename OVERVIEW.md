# OVERVIEW — 프로젝트 전체 흐름 (배경 지식이 적어도 이해되게)

> 이 문서 하나로 "무엇을 왜 했고, 무엇을 발견했고, 지금 어디에 서 있는지"를 잡을 수 있게
> 썼습니다. 용어는 처음 나올 때 풀어 설명하고, 맨 끝에 **용어집**을 둡니다.
> 더 압축된 사실/숫자는 `RESULTS.md`, 증거 사슬은 `FINDINGS.md`, 다음 행동은 `NEXT_TAB.md`.

---

## 0. 한 문단 요약 (제일 먼저)

표(엑셀 같은 행/열) 데이터에서 **시간이 흐르면 데이터 분포가 변하는 문제**(temporal
distribution shift)를 다룹니다. 우리는 "시간에 따라 진화하는 기억(memory)을 모델에 달면
이 변화를 잡아내고 *설명*까지 할 수 있다"는 아이디어로 출발했습니다. 결론부터 말하면:
**(A)** 현실의 표 시간데이터에서 분포 변화는 대부분 "입력이 변하는 것(covariate drift)"이고,
"입력→정답 관계가 변하는 것(concept drift)"은 너무 약하거나 **측정조차 어렵다**는 것을
~10개 데이터셋에서 보였고, **(B)** 측정이 되는 깨끗한 곳에서조차 우리의 *구조*(시간-인덱싱
검색)가 단순히 *시간을 입력 피처로 넣는 것*을 못 이긴다는 것을 확인했습니다. 메커니즘이
고장 나서가 아니라(합성 데이터에서 작동 입증), 단순한 방법이 이미 충분하기 때문입니다.
→ 정직하고 신규성 있는 **음성/분석(negative/analysis) 결과**.

---

## 1. 배경 — 무슨 문제인가 (비전공자용)

### 1.1 표 데이터와 "시간에 따른 분포 변화"
- **표 데이터(tabular data)**: 행=샘플, 열=피처(숫자/범주)인 가장 흔한 데이터. 예: 전력가격
  예측, 대출 부도 예측. 이미지/텍스트와 달리 딥러닝이 항상 이기지는 않는 영역.
- 모델은 보통 **과거로 학습 → 미래에 예측**합니다. 그런데 시간이 가면 데이터가 변합니다
  (시장 변화, 계절, 사용자 행동 변화 등). 이걸 **temporal distribution shift**라 합니다.
  과거에 잘 맞던 모델이 미래엔 틀리는 이유.

### 1.2 두 종류의 변화 (이 프로젝트의 핵심 구분)
분포 `P(x, y)` = `P(x)`(입력이 어떻게 생겼나) × `P(y|x)`(같은 입력이면 정답이 뭔가). 변화도 둘:
- **Covariate drift (입력 분포 변화)**: `P(x)`가 변함. 예) 작년엔 없던 종류의 고객이 올해 등장.
  *입력*은 변하지만 "이런 입력이면 정답은 이것"이라는 **규칙 자체는 그대로**.
- **Concept drift (개념/규칙 변화)**: `P(y|x)`가 변함. **같은 입력인데 정답이 달라짐**.
  예) 같은 시장 상태인데 작년엔 가격이 올랐고 올해는 내림. → *이게 진짜 어려운 변화*이고,
  "시간을 모델에 알려주면 도움이 될 법한" 부분.

> 직관: covariate drift는 "새로운 손님이 온다", concept drift는 "게임의 규칙이 바뀐다".
> 시간 정보가 정말 가치 있으려면 **concept drift**가 있어야 합니다(규칙이 시간 따라 변하니까).

### 1.3 우리가 쓴 벤치마크
- **TabReD**: 실제 산업 표 데이터 8개(시간 기록 포함) 모음. 현실적 시간 shift의 표준 벤치.
- **Elec2 (Electricity)**: 30분 간격 전력가격 UP/DOWN. concept drift 벤치의 고전.
- **Insects**: 곤충 광학센서 스트림, 종 분류(multiclass). 온도 변화로 **드리프트가 설계된**
  데이터(abrupt/incremental 등 변종). Elec2의 약점(아래)을 보완하는 둘째 벤치.

---

## 2. 우리의 원래 아이디어 (가설)

**백본**: TabM — 표 데이터용 강한 딥러닝 모델(여러 서브모델 앙상블). 우리가 새로 만든 게 아니라
검증된 백본을 가져와 그 위에 얹음.

**핵심 아이디어**: 모델 안에 **시간에 따라 진화하는 "프로토타입 메모리"**를 둔다.
- 프로토타입 = 데이터 공간의 대표점들(군집 중심 같은 것). 새 샘플이 오면 가까운 프로토타입을
  **검색(retrieval)**해서 예측에 활용.
- 이 프로토타입을 **시간의 함수**로 만든다: `P_k(t)` = 기본 위치 + 시간에 따른 이동.
  → 시간이 가며 "규칙"이 변하면 메모리도 따라 변해서 잡아낸다는 발상.
- 게다가 "어떤 프로토타입을 봤는지" 자체가 **사람이 읽을 수 있는 설명**(왜 이렇게 예측했나)이
  될 수 있다 → "해석 가능한(explainable) drift 처리".

차별점(왜 신규라고 봤나): 기존 연구는 시간을 출력단(head)에만 주입하거나(약함), 검색에 시간
좌표가 없었음. "**시간-인덱싱 + 검색**"의 교차점이 비어 있었음.

---

## 3. 연구 질문 두 개

- **Q1 (충실성, faithfulness)**: 이 메커니즘이 *진짜* 시간에 따른 규칙 변화를 **충실하게 복원**하나?
  (= 해석을 주장하려면 먼저 "설명이 거짓이 아님"을 보여야 함. 필요조건.)
- **Q2 (성능/구조)**: 이 **구조**(시간-인덱싱 검색)가, 단순히 **시간을 입력 피처로 넣는 것**보다
  실제로 예측을 더 잘하나? (= "복잡한 구조가 값을 하나?")

---

## 4. 발견의 흐름 (이 프로젝트의 줄거리)

각 단계는 "왜 이걸 했나 → 무엇을 봤나 → 그래서 다음에 뭘 했나"로 읽으세요.

### 4.1 Phase 0 — 토대: 백본 재현
- **왜**: 우리 결과를 믿으려면 파이프라인부터 신뢰돼야 함.
- **결과**: TabM을 TabReD 8개에서 공개 수치 ±1% 내로 재현. ✅ 파이프라인 신뢰.

### 4.2 합성 positive control — "메커니즘은 작동한다"
- **왜**: 메커니즘이 *원리적으로* concept drift를 잡는지 통제된 환경에서 확인(버그 배제).
- **방법**: 입력은 그대로 두고 정답 규칙만 시간에 따라 회전시킨 **순수 concept drift** 합성 데이터.
- **결과**: 시간-인덱싱 메모리 RMSE **0.13** vs 시간 무시 **1.03** = **+87%**. ✅
  → 메커니즘은 concept이 있으면 확실히 착취한다. **이후 나오는 음성은 "버그"가 아님이 보장됨.**

### 4.3 TabReD 실데이터 — "이득이 0이다. 왜?"
- **결과**: 시간-인덱싱이 실데이터 성능을 **전혀 못 올림**(통계적으로 0).
- **진단(중요한 도구 도입)**: 모델 내부를 계측해 보니 메모리가 **장식**(꺼도 손실 불변)이었음
  — 모델이 메모리를 우회. 그래서 "메모리를 강제로 쓰게" 해도 이득 없음.
- **드리프트 해부**: TabReD의 변화는 **압도적으로 covariate**(과거/미래 입력을 구분하는 분류기
  정확도 ≈ 1.0, 그것도 소수 피처 때문이 아니라 **전반적**), 반면 시간↔정답 관계는 약함(상관 ≤ 0.13).
  → **TabReD엔 착취할 concept drift가 사실상 없다.** "규칙이 안 변하니 시간 메모리가 할 일이 없다."

### 4.4 측정 문제 — "concept을 *잴 수* 있긴 한가?" (F3 / within-overlap)
- **왜**: "concept이 없다"가 진짜인지, 아니면 "측정을 못 하는 것"인지 구분 필요.
- **핵심 깨달음**: covariate가 너무 심하면 **과거와 미래의 입력 영역이 거의 겹치지 않는다**.
  겹치는 공통 영역이 없으면 "같은 입력에서 정답이 변했나(concept)"를 **물어볼 수가 없다**.
- **방법(within-overlap)**: 과거/미래 입력이 겹치는 영역으로 한정해 `P(y|x)`가 변했는지 측정.
- **결과**:
  - 고-covariate 5개 → 겹침 0 → **측정 불가**.
  - 저-covariate(cooking/maps) → 측정 가능하나 concept ≈ 0.
  - **Elec2(중간)** → 공통 영역 + **진짜 측정 가능한 concept = +0.132 AUC** (교차검증·층별 안정).
- **의미**: "TabReD-류 드리프트를 정의하는 그 강한 covariate가, 바로 그 때문에 concept을
  표준 도구로 측정 불가능하게 만든다" → 이게 **Claim A**의 핵심이자 신규 기여.

### 4.5 Q1 충실성 게이트 — "메커니즘은 거짓말하지 않는다"
- **결과**: 제대로 만든 메커니즘이 합성에서 진짜 규칙을 **복원도(recovery) 0.991** (10/10 시드)로
  복원. 천장(이상적) 0.990, 바닥(무작위) 0.894 → **PASS**. ✅
- **의미**: 메커니즘은 **충실·정확**. 즉 이후 음성은 "메커니즘이 고장 나서"가 아님.
- **남은 것**: 복원의 동적범위가 좁음(작은 회전만 테스트) → "충실하다"를 *헤드라인 논거*로 쓰려면
  큰-회전 robustness 1회 추가 필요(게이트로는 충분).

### 4.6 Q2b — 진짜 질문: "구조가 시간-피처를 넘나?" (Elec2)
- **왜 여기서 설계를 바꿨나**: 학습되는 프로토타입 메모리는 정보 한계가 있어(약한 프록시),
  **진짜 구조 테스트**는 "이웃의 *실제 정답*을 검색해 쓰는" **인스턴스 검색(TabR식, "time-TabR")**.
- **3-arm 비교**(공정하게 같은 인코더 공유):
  - `mlp_t` = 시간을 그냥 **입력 피처**로 (단순 baseline, "이겨야 할 상대")
  - `tabr` = 검색하되 **시간 안 씀**
  - `time_tabr` = 검색 + **시간 훅**(우리 구조)
- **결과(정규화+충분 학습, 10시드, 각 arm 최적 학습률)**: mlp_t **0.9027** > tabr 0.8955 > time_tabr 0.8848.
  → 구조(time_tabr)가 단순 피처(mlp_t)를 **못 넘음**.

### 4.7 "버그냐 진짜 drift냐" — 학습곡선으로 판별 (중요한 검증)
- **걱정**: 모델들이 너무 빨리(거의 1 epoch) 최고점에 도달 → 비교가 의미 없는 것 아닌가?
- **판별(사전등록한 지문)**: 두 종류 분할로 학습곡선 비교.
  - **temporal**(과거→미래): val 성능이 **일찍 최고점 후 하락** (과거 규칙에 맞출수록 미래엔 나빠짐).
  - **random**(시간 섞음): val이 **늦게까지 매끄럽게 상승** (정상 학습).
  - 그리고 **train loss는 모두 감소**(기울기 정상 = 학습 버그 아님).
- **결론**: 이 패턴은 교과서적 **concept drift 지문**. 코드/설계 버그가 아니라 *데이터의 진짜 성질*.

### 4.8 둘째 데이터셋 Insects — Elec2의 약점 보완
- **왜 필요**: Elec2는 **30분 간격이라 자기상관이 심함** → "직전과 같다"만 예측해도 점수가 높음
  (문헌의 알려진 비판). 즉 너무 쉽게 풀려 "구조가 보탤 여지"가 처음부터 없을 수 있음.
  또한 Elec2는 모델 *비교 기판*으로 노이즈가 큼(아래 4.9). → 성격이 다른 둘째 벤치가 필요.
- **Insects 결과**(designed drift, multiclass, 같은 프로토콜, 10시드): mlp_t **0.6704** >
  time_tabr 0.6594 > tabr 0.6320. → 여기서도 **구조 ≤ 피처**. 그리고 Elec2와 달리 **시간이
  매우 중요**(mlp_t가 tabr보다 +0.038 ↑) = 진짜 비-trivial concept인데도 구조가 못 넘음.

### 4.9 정밀 통계 — paired로 다시 보기 (비평 반영, 정직성)
- **왜**: 세 arm이 **같은 시드·분할·초기화를 공유**하므로, 각 arm의 산포(std)가 아니라
  **차이(difference)의 불확실성**으로 봐야 함(짝지은/paired 비교). 이게 진짜 척도.
- **결과(arm별 최적 학습률, 시드별 차이의 평균±신뢰구간):**
  | 대비 | 평균 차이 | 95% 신뢰구간 | 해석 |
  |---|---|---|---|
  | **Insects** time_tabr − mlp_t | **−0.011** | [−0.023, **+0.001**] | 구조<피처, **경계적 유의**(음성을 *나름*) |
  | **Elec2** time_tabr − mlp_t | −0.005 | [−0.013, +0.003] | **0과 구분 불가** = 무정보 |
  | Insects time_tabr − tabr | +0.028 | [+0.013, +0.043] | **시간-구조는 검색 안에서 작동함** |
  | Insects mlp_t − tabr | +0.038 | [+0.029, +0.048] | **검색 기판이 단순 MLP보다 훨씬 약함** |
- **두 가지 정직한 정정**:
  1. **Elec2는 깨끗한 음성이 아니라 "무정보 null"**. val(검증)↔test(시험) 상관이 0.07 —
     Elec2에선 정직한 모델 선택조차 안 됨(자기상관/regime 탓). 데이터엔 concept이 있지만
     (4.4의 +0.132) **모델 비교 기판으로는 노이즈**. → **음성의 무게는 Insects(상관 0.87, 깨끗)가 짊어진다.**
  2. **"구조 vs 기판" 분해**: 지는 건 *시간 메커니즘이 아니다*. 시간 훅은 검색 안에서 분명히
     값을 더한다(time_tabr − tabr = +0.028). 문제는 **검색이라는 기판(tabr)이 단순 MLP보다
     한참 약하다**(−0.038)는 것 → 보탬을 받아도 MLP를 못 따라잡음. → "시간 구조는 작동하나,
     검색 기판이 parametric을 못 넘어 결합 구조가 시간-피처를 못 이긴다" (redundancy 논증과 일관).

---

## 5. 지금의 결론

### Claim A (리드 — 견고하고 신규) ★
**현실의 표 시간데이터에서 드리프트는 압도적으로 covariate이고, 그 강한 covariate가 과거/미래
공통 영역을 무너뜨려 concept을 표준 조건부 렌즈로는 측정조차 어렵다.** (~10 데이터셋, F3/within-overlap
측정 프레임 + 진단 도구킷이 받침.) → "왜 단순 방법이 표 시간 벤치에서 이기나"라는 알려진 퍼즐에
**기전적 설명**을 준다.

### Claim B (보조 — 분리해서 제시) 
**측정 가능한 곳에서조차 시간-인덱싱 *구조*가 시간-*피처*를 못 넘는다.** 단:
- 음성은 **Insects(깨끗)**가 나르고(paired −0.011, 경계적), **Elec2는 무정보**(분리해 진술).
- 지는 건 **검색 기판**이지 시간 메커니즘이 아니다(분해로 선제 방어).
- 메커니즘은 **충실**(Q1 PASS) — "고장 나서 음성"이 아니라 "단순 방법이 이미 충분".
- "시간 방법 무용"(전칭) 주장 아님 — **시간 피처는 도움**이 됨. 거짓으로 밝혀진 건 "구조 > 피처".

### 한 줄
"메커니즘은 충실하지만, 현실 표 시간데이터는 covariate가 지배해 concept이 측정 난망하고,
측정되는 곳에서도 시간-인덱싱 구조가 단순 시간-피처를 못 넘는다 — 정직한 음성/분석 기여."

---

## 6. 정직한 한계 (지도교수/리뷰어가 찌를 자리, 미리 정리)

1. **음성의 범위가 좁다**: Claim B가 사실상 깨끗한 데이터셋 1개(Insects) + 무정보 1개(Elec2)에
   기댐. "방법 *부류(class)*가 안 된다"는 부류를 테스트해야지 인스턴스 하나로는 약함.
2. **"우리 방법이 실패"로 읽힐 위험**: 탑티어 음성 논문의 바는 높음 — *일반화 가능한 통찰*이나
   *재사용 산출물*을 줘야 함. (그래서 Claim A 리드 + 도구킷 1급화가 핵심.)
3. **redundancy 논증은 미발달**: "구조가 왜 못 돕나"의 이론적 척추인데 아직 주장 수준 →
   **경험적 일관성**으로만 제시(증명처럼 쓰면 외삽/귀납편향 반례로 공격받음).
4. **Q1 동적범위가 좁다**: 큰-회전 robustness 1회로 넓혀야 "충실하다"가 헤드라인 논거로 robust.

---

## 7. 어디로 가나 (다음 행동)

### 7.1 타깃 학회부터 합의 (작업량을 결정함)
- **워크숍/단편** = 지금 자산으로 충분(낮은 노력, 피드백 수집).
- **NeurIPS Datasets & Benchmarks 트랙** = 현 자산 최적핏(벤치/측정/분석/음성을 가치 있게 봄;
  적당 확장으로 현실권).
- **메인트랙 분석 논문** = 수개월: 표 시간 **15~20개**로 Claim A 일반화 + **여러 시간-인지 방법**을
  같은 렌즈로("내 방법 실패"→"분야 현상") + 도구킷 1급화 + redundancy 신중 전개.

### 7.2 정렬 전 정밀 재진술 (선제 방어)
(1) Elec2 = consistent-but-uninformative로 재진술, 음성은 Insects가 나름. (2) "구조 vs 기판"
분해 선제. (3) 통계는 paired CI로. → 이 셋을 들고 가면 찌를 자리를 미리 닫음.

### 7.3 싼 견고화 (권장)
- **Insects 변종 추가**(abrupt/gradual; 같은 로더, 거의 공짜) → Claim B를 n=1-clean → n=3-clean.
- (메인트랙 가면) 데이터셋·방법 확장.

---

## 8. 용어집 (Glossary)

- **covariate drift**: 입력 `P(x)`의 변화(규칙은 그대로). "새로운 손님".
- **concept drift**: `P(y|x)`의 변화(같은 입력, 다른 정답). "규칙이 바뀜". 시간 정보가 값을 하는 핵심.
- **TabM**: 표 데이터용 딥러닝 백본(서브모델 앙상블). 우리가 얹은 토대.
- **프로토타입 메모리 / 검색(retrieval)**: 데이터 대표점들을 두고, 새 샘플과 가까운 것을 찾아
  예측에 쓰는 구조. 우리는 이 대표점을 **시간의 함수**로 만들려 했음.
- **time-TabR / 인스턴스 검색**: 프로토타입 대신 **실제 이웃 샘플의 정답**을 검색해 쓰는 방식.
  진짜 "구조" 테스트에 사용.
- **mlp_t / tabr / time_tabr (3-arm)**: 시간=피처 / 검색-시간없음 / 검색+시간훅. 공정 비교용 세 모델.
- **temporal vs random split**: 과거→미래로 나눔 vs 시간 섞어 나눔. 둘을 비교해 drift를 판별.
- **val / test**: val=학습 중 모델/하이퍼파라미터 고르는 검증셋, test=최종 성능 보는 시험셋.
- **val→test 상관(Spearman)**: val로 고른 게 test에서도 좋은가. 낮으면(0.07) "그 데이터는 모델
  비교 기판으로 노이즈"라는 뜻(Elec2). 높으면(0.87) 신뢰 가능(Insects).
- **paired 비교 / 신뢰구간(CI)**: 같은 시드끼리 *차이*를 보고 그 불확실성을 추정. 모델들이 조건을
  공유할 때 진짜 척도(각자 산포보다 훨씬 tight).
- **faithfulness(충실성, Q1)**: 모델의 설명이 실제 내부 동작을 거짓 없이 반영하는가.
- **oracle lr**: 각 arm에 "시험 성능 기준 최적 학습률"을 준 상한 비교(강한 형태의 음성: 유리하게
  줘도 구조가 못 넘음). 단 이건 배포 성능 주장은 아님.
- **§6(다)**: 사전 계획 문서(PLAN_RESCUE)에서 정의한 "음성/분석 방향" 분기.

---

## 9. 코드 / 재현 맵

- **모델**: `src/models/tabr.py` (TimeTabR, TimeTabRModel — 3-arm, 시간 훅, dropout).
- **학습**: `src/training/tabr_trainer.py` (in-batch 검색 학습 / 고정-context 평가, train-loss·step-eval·min_epochs).
- **실험 러너**: `scripts/run_elec2_q2.py`
  - `--dataset elec2|insects` (메트릭 자동), `--diag`(학습곡선), `--report-grid`(결정표 + **paired 통계**).
  - 모든 진단은 `results/phase1/<dataset>_q2/diagnostics.jsonl`에 한 줄씩 누적.
- **데이터**: `src/data/elec2_loader.py`, `src/data/insects_loader.py`(river).
- **기록 문서**: `RESULTS.md`(숫자 ledger §9 concept/§10 elec2/§11 insects), `FINDINGS.md`(증거사슬
  + "Q2b ANSWERED"/"Precision pass"), `NEXT_TAB.md`(다음 행동·학회 타깃), `PLAN_RESCUE.md`(사전등록·결정규칙).

### 서버 재현(요지)
```
conda activate explaintab311 && cd ~/explainableTabular && git pull
python scripts/run_q1_faithfulness.py                       # Q1 PASS 재현
python scripts/run_concept_overlap.py --config configs/phase1.yaml --all --elec2   # elec2 +0.132
pip install river && python scripts/smoke_test_insects.py   # 둘째 데이터셋 배선
python scripts/run_elec2_q2.py --dataset insects --report-grid --n-seeds 10 \
    --splits temporal --bases trend --lr-grid 1e-3 5e-4 2e-4 --dropout 0.1 --weight-decay 1e-4 --min-epochs 20
```
